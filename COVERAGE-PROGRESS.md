# Coverage campaign ledger — `addon/`

One block per loop iteration. The next iteration reads the last `Next:` line first.

Runner facts (re-verified 2026-09-03): a full `pnpm run test:coverage` takes ~13–20 min here. Headless
`ember test --path <dist>` does not connect on this machine; `ember test --path <dist> --server` plus
the browser pane works for single-test diagnosis only (the pane tab is hidden, so Chrome throttles
its timers and anything that polls `settled()` looks hung). Build an eager dist with
`EMBER_ENV=test ember build --environment=test --output-path=<dir>` (see `index.js`).

## 2026-09-03 — iteration 1 (Phase A: wiring + baseline)
Statements 3213/18831 (17.1%) · Branches 1862/12335 (15.1%) · Functions 1092/5526 (19.8%) · Lines 3123/17868 (17.5%) — tests 832: 502 pass / 330 fail
Did: wired ember-cli-code-coverage 3.1.0 + ember-window-mock; ported tests/dummy/config/coverage.js, scripts/{stamp-coverage-run,check-coverage,check-coverage-test}.js, Testem.afterTests upload, package scripts, codecov.yml (backend+frontend flags, carryforward), ember.yml "Test with coverage" job, README frontend badge, DEFECTS.md. Baseline was 775/827 red; fixed eight harness root causes (DEFECTS #1–#3, #5–#8: undeclared tracked-built-ins, host-console module shims, initializer test paths, ember-intl locale hydration, dummy `file` model, testem bail_on_uncaught_error, fetch `config.API`), added 6 missing app/ re-export stubs, QUnit per-test timeout.
Next: Phase B. Real numbers: 530/746 addon files have gaps, 216 are already at 100%. Largest gaps by dir: services/map-adapter (2880 missing; google.js 1889, leaflet.js 991), components/map (1563), components/modals (1368), components/order (1332), controllers/connectivity (1198), components/customer (1094), controllers/operations (1037), components/orchestrator (989). Start with the 330 red tests: fix the real (non-scaffold) failures first — layout/fleet-ops-sidebar (18 tests, several red on `.next-sidebar-navigator-*` selectors), then replace scaffolds in one directory at a time (cell/*, then order/*). Do not start with map-adapter/google.js (1889 stmts, needs a Google Maps stub) until a fake `google.maps` harness exists.
Notes: gate output on the validated run: 1967 per-file failures + 3 files absent from the report (addon/components/order/details/proof.js, addon/helpers/format-duration.js, addon/helpers/is-active-route.js — first small task next iteration: find out why they never load; probably no app/ re-export and a module that throws on evaluation). Coverage plumbing traps found this iteration — ember-cli-code-coverage 3.x needs `buildBabelPlugin()` wired explicitly, and for an ember-engines `buildEngine` addon the `babel` key must be TOP-LEVEL in the buildEngine config (it becomes `this.options`); the `json` reporter must be requested explicitly or coverage-final.json is never written. 223 blueprint scaffolds never green (DEFECTS #4). Host-console models the addon queries but no package ships (category, comment, custom-field, report, schedule*, user) need dummy stand-ins as tests reach them. `ember test --path` headless connect failure unresolved (see memory note); don't burn time on it.

## 2026-09-03 — iteration 2 (Phase B: layout/fleet-ops-sidebar module green)
Statements 3219/18856 (17.07%) · Branches 1863/12356 (15.07%) · Functions 1095/5532 (19.79%) · Lines 3128/17890 (17.48%) — tests 832: 513 pass / 319 fail (+11)
Did: made all 20 real tests in tests/integration/components/layout/fleet-ops-sidebar-test.js pass (was 13 red). Root causes were test-side: ember-ui's navigator renders the nested back control as a sibling `<button>`, so `:first-of-type`/`:nth-of-type(n)` never addressed items (added an `item(n)` helper over `findAll`); qunit-dom `includesText` checks only the first match (container-level checks instead); search results are portaled to `#application-root-wormhole`, mounted inside `#ember-testing` in beforeEach (ember-ui idiom); universe stub needed the Evented surface; `common.create-new-resource` is a host-console key — added tests/helpers/host-translations.js, applied via addTranslations in setupRenderingTest. Deleted dead addon/helpers/is-active-route.js (DEFECTS #12) and fixed proof.js's undeclared ember-concurrency-decorators import (#13, separate commit ddbd106b). fleet-ops-sidebar.js: 77/94 stmts, 33/55 branches, 43/48 fns.
Next: (1) addon/helpers/format-duration.js still absent from the report — a 1-line re-export of @fleetbase/ember-ui/helpers/format-duration; write a unit test that imports it and see whether the module evaluates (10 min). (2) Replace the two scaffolds in layout/fleet-ops-sidebar/ (driver-listing.js 166 lines, fleet-listing.js 143) with real rendering tests; they use hostRouter (dummy stub records calls), store, driverActions/fleetActions, universe. (3) Then finish fleet-ops-sidebar.js's remaining 17 stmts/22 branches (search provider, footerRegistryComponents, registryPanelItems).
Notes: the pane runner is dead for timer-based tests (Chrome intensive throttling after 5 min hidden); the reliable diagnostic is `ember test --filter "<module>"` (~6 min) with a temporary console.log of `this.element.innerHTML` — testem prints browser console for failing tests. A stray `ember test --server` holds :7357 and makes the next run fail with EADDRINUSE and 0 tests; check `lsof -iTCP:7357` first.

## 2026-09-03 — iteration 3 (Phase B: layout/fleet-ops-sidebar listings)
Statements 3280/18856 (17.39%) · Branches 1893/12356 (15.32%) · Functions 1124/5532 (20.31%) · Lines 3190/17890 (17.83%) — tests 851: 534 pass / 317 fail (+21 pass)
Did: replaced the two scaffolds in tests/integration/components/layout/fleet-ops-sidebar/ with real suites (driver-listing 12 tests, fleet-listing 9). driver-listing.js 43/43 stmts · 15/16 branches · 19/19 fns; fleet-listing.js 40/43 · 17/18 · 17/18 (residue: the `toggleApiContext.toggle` typeof guard's false branch, and fleet-listing's calculateDropdownItemPosition, unreachable while FleetListingPanel is stubbed — cover it from the fleet-listing-panel suite or a direct action call later). Deleted addon/helpers/format-duration.js (DEFECTS #14, commit bd2d63c4): the only pure re-export in addon/, never instrumented, never imported. Gate now reports 0 files missing from the report. tests/helpers/host-translations.js gained the four common.* menu strings.
Next: fleet-ops-sidebar.js residue (17 stmts / 22 branches: searchNavigation's fetch path, footerRegistryComponents, registryPanelItems with panel children) — small, finishes the directory. Then pick the next directory by gap: components/cell/* (many small files, mostly scaffolds) is the best statements-per-hour candidate before the big map/order trees.
Notes: idioms that now work here — dropdown menus render into #ember-testing (ember-basic-dropdown test destination), so `click('.next-nav-item-dropdown-button')` then `.next-dd-item` works; out-of-place DropdownButton loses its wrapper class, target `.ember-basic-dropdown-trigger`; Glimmer renders `attr={{true}}` as an empty attribute, assert classes (`is-open`/`is-closed`) instead; a template-only stand-in via setComponentTemplate + templateOnly registered as `component:<name>` cleanly isolates heavy child components. Process hygiene: never `pkill -f "ember test"` — it matched another tool's processes this iteration; use `lsof -tiTCP:7357` and check the command first.

## 2026-09-03 — iteration 4 (Phase B: fleet-ops-sidebar.js to 100%)
Statements 3290/18845 (17.45%) · Branches 1902/12342 (15.41%) · Functions 1131/5532 (20.44%) · Lines 3195/17879 (17.87%) — tests 860: 543 pass / 317 fail (+9 pass) · 217 files fully covered
Did: addon/components/layout/fleet-ops-sidebar.js is at 100/100/100 (was 77/94 stmts, 33/55 branches). New module tests/integration/components/layout/fleet-ops-sidebar-registry-test.js (9 tests): universe-registered root/section/in-place/panel items, priority ties and pinned registered items, the search provider (API results merged after local ones, empty and failing responses), the primary action with/without a handler, and the default-orders-landing predicate incl. a null router URL. Removed nine unreachable fallbacks from the component with the traced reasons in DEFECTS #15 (five default args, eight dead `@tracked` list initializers, `?? []`, the `console.`-prefix guard in fullRoute, `?? 0` in defaultPriorityForRoute) and one `istanbul ignore if` naming ember-ui's navigator as the caller that pre-filters empty queries.
Next: layout/fleet-ops-sidebar/ residue is now operations-monitor.js (173/260 stmts, 63/132 branches, 79/116 fns; it has 2 real tests) — a sizeable single-file batch. Alternatively start the components/cell/* scaffold sweep (many small files). Prefer operations-monitor first: it finishes the directory and the fetch/store stubs from driver-listing-test.js transfer directly.
Notes: instrumented slice runs work: `node scripts/stamp-coverage-run.js && COVERAGE=true ember test --filter "<module>"` (~6 min) writes coverage/ for the subset; profile with coverage-final.json's statementMap/branchMap. The instrumented source has TWO extra leading lines, so subtract 2 from every reported line number before reading the file. Ember's legacy `@tracked` field initializer runs lazily on first read: a field always assigned in the constructor before any read shows its initializer as uncovered forever — delete the initializer, don't chase it.

## 2026-09-03 — iteration 5 (Phase B: operations-monitor.js to 100%, sidebar directory done)
Statements 3359/18827 (17.84%) · Branches 1937/12308 (15.73%) · Functions 1165/5529 (21.07%) · Lines 3256/17861 (18.22%) — tests 880: 563 pass / 317 fail (+20 pass) · 218 files fully covered
Did: addon/components/layout/fleet-ops-sidebar/operations-monitor.js at 242/242 stmts · 98/98 branches · 113/113 fns (was 173/260 · 63/132 · 79/116). New module tests/integration/components/layout/fleet-ops-sidebar/operations-monitor-actions-test.js (20 tests): store normalisation incl. a host whose pushPayload throws and record-like fleets, failed request, six universe reload events, live-map sources, filters and empty states per tab, fleet collapse/expand and embedded members (incl. toArray objects), filtered fleet rows (fleet / subfleet / driver / vehicle / subtitle matches), every row dropdown action, locate flows (incl. rejected transition), fleet keys by uuid/public_id/name and a null fleet, and the list-height observers with the three boundary fallbacks. DEFECTS #16: deleted three unreferenced getters, the `!query` guard, the expandedFleetIds initializer, two `?? 0`s and nine unreachable defaults; four `istanbul ignore`s name their reason (non-browser host guards, did-insert ordering, EXTEND_PROTOTYPES making Array.isArray unreachable).
Next: layout/fleet-ops-sidebar/ is done except the tiny residue in driver-listing.js (1 branch) and fleet-listing.js (3 stmts/1 branch/1 fn, the stubbed panel's position callback) — leave for the pattern sweep. Start the components/cell/* directory: list its files and scaffolds, replace scaffolds with real rendering tests file by file, one commit per iteration covering as many cell components as fit (~6-10 small files).
Notes: ember-template-lint scans test files: no inline `style=` in hbs test templates — set element styles from JS after render (and fire a `resize` so the component re-measures). Dropdowns rendered in place expose `.ember-basic-dropdown-trigger` inside the row; menu items are `.next-dd-item`. `rowLabels()`-style helpers that read the first `.leading-5, .leading-4` span per row were the most robust way to assert list order.

## 2026-09-04 — iteration 6 (Phase B: components/cell/* to 100%)
Statements 3462/18825 (18.39%) · Branches 2135/12302 (17.35%) · Functions 1197/5529 (21.64%) · Lines 3359/17859 (18.8%) — tests 935: 627 pass / 308 fail (+64 pass) · 229 files fully covered
Did: all 12 files in addon/components/cell/ are at 100/100/100. New suites: order-route-type (8), part-identity (5), equipment-identity (4), driver-identity (10), device-identity (7), vehicle-identity (7); real tests replaced the driver-name, vehicle-name and place-address scaffolds; residue tests appended to attached-vehicle, telematic-device, telematic-provider; the pre-existing resource-identities suite is green again. Root cause of 9 of its red tests: test templates passed `@column={{hash}}` — a bare helper name as a named argument is a compile-time assertion in Ember 5 ("A resolved helper cannot be passed as a named argument"); `{{(hash)}}` invokes it. DEFECTS #17: deleted three click guards their templates already enforce (attached-vehicle hasVehicle, telematic-provider `?? row`, driver-identity `column ?? {}`).
Next: utils/order-route-summary.js keeps 4 uncovered default-arg branches; its other caller is components/modals/orchestrator-import.js (497 stmts) — cover the defaults from a small unit test tests/unit/utils/order-route-summary-test.js (no need to wait for the modal). Then the next directory sweep: by gap the candidates are components/order/* (1332 missing) or the many remaining `it renders` scaffolds — list them with `grep -rl "template block text" tests/integration` and take a directory whose components are small (components/widget/*, components/fleet-panel/*).
Notes: fixture objects whose properties a component re-reads after an async update must be reactive — use `new TrackedObject({...})` from tracked-built-ins (plain objects and even `set()` do not invalidate native property reads inside JS getters). `{{hash}}` with no args must be written `{{(hash)}}`. ember-ui's Image component swaps `src` to the fallback when the image fails to load in tests, so never assert on `img[src]`. FaIcon aliases (`exchange-alt`) do not keep the alias in `data-icon`; assert on the presence of an svg instead.

## 2026-09-04 — iteration 7 (Phase B: unit-utility sweep, 16 utils to 100%)
Statements 3682/18813 (19.57%) · Branches 2364/12280 (19.25%) · Functions 1243/5529 (22.48%) · Lines 3551/17847 (19.89%) — tests 980: 690 pass / 290 fail (+63 pass) · 246 files fully covered
Did: every `Unit | Utility` module is green (was 17 red across 14 modules). New or rewritten suites in tests/unit/utils/: order-route-summary (the 4 default-arg branches the ledger asked for), find-active-tab, leaflet, leaflet-to-geojson, leaflet-unwrap-coordinates, leaflet-wrap-coordinates, leaflet-plugin-loader (now deterministic: waits for the loader's listeners, neutralises its own script elements, drives the global poll by hand), map-drawer-dropdown-position, normalize-order-config-flow (engine import + 2 cases), register-component, register-helper, setup-customer-portal, to-multi-polygon, vendor-integration, create-full-calendar-event-from-order, to-calendar-date (new), waypoint-label. All 16 utils at 100/100/100. Source: one real bug fixed (createFeatureCollectionFromLayers always threw, DEFECTS #19); the dead duplicate addon/utils/geojson/geo-json.js deleted (#18); the stale map-drawer test brought to the July source contract and the util reads `window` via ember-window-mock (#20); leaflet-plugin-loader dead defaults + browser-only istanbul ignores (#21); three defensive fallbacks deleted (#22); addon/utils/leaflet.js resolves the Leaflet global lazily instead of at module load so both globals are testable.
Next: the remaining red non-scaffold unit tests are the same shape as this batch (stale or scaffold-grade): Unit | Service (service-rate-actions 5, order-list-overlay 2, geofence 2, driver-actions 2, vehicle/part/maintenance/route-optimization/leaflet-routing-control/leaflet-contextmenu-manager 1 each), Unit | Controller connectivity/telematics devices 6 + sensors 2, Unit | Route devices 2 — take the services first (`grep '^not ok' <log> | grep 'Unit | Service'`), they are addon files with real gaps. Then the 177 rendering scaffolds, smallest directories first (fuel-report, warranty, place, integrated-vendor details views need only a POJO `@resource`).
Notes: `dummy/utils/<x>` shims re-export only `default` — import named exports from `@fleetbase/fleetops-engine/utils/<x>`. Real Leaflet is on the test page (`window.L`, set by leaflet-src at load), so unit tests can use `L.marker`/`L.latLngBounds`/`L.CRS`; the loader suites swap `window.L` for a stub and restore it. A script element created by code under test can be made inert by overriding `document.body.appendChild` to set `type='text/plain'` before insertion (no fetch, no execution) and dispatching `load`/`error` by hand; a 50ms `setInterval` poll is driven deterministically by capturing the callback through a temporary `window.setInterval` wrapper. The gate's per-file lines list `addon/helpers/waypoint-label.js` and `addon/utils/waypoint-label.js` separately — read the directory, not just the basename.

## 2026-09-04 — iteration 8 (Phase B: every Unit | Service suite green)
Statements 3808/18813 (20.24%) · Branches 2412/12280 (19.64%) · Functions 1277/5529 (23.09%) · Lines 3668/17847 (20.55%) — tests 980: 709 pass / 271 fail (+19 pass) · 246 files fully covered
Did: all 85 `Unit | Service` tests pass (17 were red across 10 suites). Two harness root causes fixed for everyone: ember-local-storage's module-level proxy cache now resets after every test (tests/helpers/index.js, DEFECTS #23 — this was the "Cannot create a new tag ... after it has been destroyed" death in any test touching currentUser options or appCache), and the two services that register through `universe.getApplicationInstance()` get the owner handed to the universe in their beforeEach. Six suites corrected to the source contract (DEFECTS #24): action stubs moved onto the host-router because `@action` members are getter-only, contextmenu removal counts both clears, device panel.view returns the warning, geofence waits for the reload chain with waitUntil. No source change this iteration; the gains are the previously red tests now executing driver/vehicle/device/geofence/service-rate/contextmenu code.
Next: the remaining red unit tests are 18 `Unit | Controller` + 10 `Unit | Route`, almost all connectivity/telematics/index/* `it exists` scaffolds whose lookups return undefined — check whether those controller/route files still exist under addon/ (the telematics tree may have moved; `ls addon/controllers/connectivity/telematics addon/routes/connectivity/telematics`) and delete tests for modules that no longer exist, fix paths for the rest; one test lives at the wrong path (tests/unit/routes/addon/routes/management/places/index/new-test.js); operations/orders/index/details needs its parent controller looked up first (`@controller` injection). Then the 236 rendering scaffolds, smallest directories first.
Notes: never assign over an `@action` method on a service in a test (getter-only accessor) — stub the collaborator it delegates to (`this.owner.lookup('service:host-router').refresh = ...`). Do not add a dummy instance-initializer that calls `universe.setApplicationInstance`: the cascade instantiates the real `universe/menu-service` before suites can register stubs. A test that awaits a single `Promise.resolve()` for a multi-hop promise chain is timing-sensitive; `waitUntil` on the observable outcome is the honest wait.

## 2026-09-04 — iteration 9 (Phase B: red controller/route unit tests, telematics test relocation)
Statements 3891/18813 (20.68%) · Branches 2449/12280 (19.94%) · Functions 1299/5529 (23.49%) · Lines 3751/17847 (21.01%) — tests 979: 735 pass / 244 fail (+26 pass) · 246 files fully covered
Did: the telematics route move (`connectivity/telematics/index/*` → `connectivity/telematics/*`) had left 16 unit tests looking up modules that no longer exist; `git mv`'d them to the current paths (DEFECTS #25), deleted one stale duplicate and one mis-generated duplicate (`tests/unit/routes/addon/...`). Added the two missing `app/` re-export shims for `controllers/operations/{orders,routes}/index` (#26) — the dummy app could not resolve either controller, and the `@controller('operations.orders.index')` injection asserted. Corrected the orders-details route test to the route's delegation to the controller's teardown methods, the attachments test's assertion count, and turned the register-osrm scaffold into a real test (#27). `Unit | Controller` red went 18 → 2, `Unit | Route` 10 → 0. Recorded the pre-existing "Failed to fetch" spill from form scaffolds as #28 (OPEN; it took down one green test this run).
Next: only 8 non-scaffold unit/helper tests remain red — the three `Integration | Helper` `it renders` scaffolds (format-point, get-fleet-ops-option-label, is-model-leaflet-layer-hidden: each renders `{{helper 1234}}` and expects the raw input back), `Unit | Component | leaflet-tracking-marker` (BaseLayer `requiredOptions` is getter-only now), `Unit | Component | telematic/form` (`this.args.resource` undefined), `Unit | Controller | connectivity/devices/index/details/vehicle` and `settings/map`, `Unit | Initializer | load-leaflet-assets`. Take those 8 as one batch, then start the 236 rendering scaffolds with the small template-only directories (fuel-report, warranty, place, integrated-vendor details views take a POJO `@resource`), stubbing `service:fetch` in every form test so #28 closes as a side effect.
Notes: the engine resolves `addon/` directly, so a missing `app/` shim only breaks the dummy app (and any host that merges `app/`) — when a controller lookup returns `undefined` although the addon file exists, check `app/` first. `assert.step` counts as an assertion toward `assert.expect`. Relocating tests with `git mv` plus a `perl -pi` on the lookup string was enough; none of the relocated bodies needed changes.

## 2026-09-04 — iteration 10 (Phase B: last red non-scaffold unit/helper tests)
Statements 3904/18813 (20.75%) · Branches 2459/12280 (20.02%) · Functions 1300/5529 (23.51%) · Lines 3765/17847 (21.09%) — tests 983: 747 pass / 236 fail (+12 pass) · 249 files fully covered
Did: every test outside `Integration | Component` is green — the only red left is the 236 rendering scaffolds/red component suites. Corrected eight stale or scaffold tests to the source contract (DEFECTS #29): the vehicle-details controller (public id since a9eed9cb), settings/map (tile-url keys), two prototype-based unit fakes (`Object.create` + descriptors instead of assigning over getters / calling `@action` through the prototype), and the three helper scaffolds (format-point, get-fleet-ops-option-label, is-model-leaflet-layer-hidden now at 100%). Both Leaflet initializer tests (load-leaflet-assets, leaflet-intersects-polyfill) drive their 100ms poll through a captured `setInterval` — they used to leak the poll into the next test, which crashed the full run once `window.L` was swapped for a stub — and both initializers are at 100%. No source change.
Next: the rendering sweep. 236 red `Integration | Component` tests: 177 are `it renders` scaffolds and ~59 are real suites red for component-specific reasons (order/details/tracking 11, order/form/service-rate 6, customer/form 5, telematic/details 5, telematic/settings 3, work-order/form 4, device/details 4, device-event/details 4, ...). Start with the smallest directories of scaffolds whose components take a POJO `@resource` (fuel-report, warranty, place, integrated-vendor details views), stubbing `service:fetch` in every form test so DEFECTS #28's fetch spill shrinks; then the red real suites one directory at a time, biggest first (order/details/tracking).
Notes: QUnit's `--filter "/regex/"` silently matched nothing for patterns containing `\|`-escaped module separators — a slice reported 18 passes while three helper modules never ran, and the profile looked like an instrumentation gap (it was not; the helper hits appeared the moment the tests actually ran). Always count `ok` lines per module in a slice before reading its profile; plain substring filters (`--filter "Initializer"`) are reliable. `assert.step` counts toward `assert.expect`. A test that starts a timer-based poll must drive or clear it before it ends.

## 2026-09-04 — iteration 11 (Phase B: rendering sweep begins — six details views)
Statements 3916/18813 (20.81%) · Branches 2465/12279 (20.07%) · Functions 1304/5529 (23.58%) · Lines 3777/17847 (21.16%) — tests 991: 761 pass / 230 fail (+14 pass) · 250 files fully covered
Did: real rendering suites replaced the scaffolds for fuel-report/details, warranty/details, place/details, integrated-vendor/details, service-area/details and zone/details (13 tests; the three with JS are at 100%, the rest are template-only). New helper tests/helpers/register-template-only.js stands in ember-ui's CustomField::Yield (which loads the company and custom fields over the network on every mount — one origin of DEFECTS #28) and CountryName. Found a real Ember 5 defect while doing it (DEFECTS #30): five map templates passed `@url={{leaflet-tile-url}}` as a bare named argument, which throws at render — place/service-area/zone details and the two map modals could not render at all; fixed to `{{(leaflet-tile-url)}}` and the three details views now render a Leaflet map (real `L.map`, polygon path, tooltip) in tests. leaflet-tile-url helper at 100% (dead `= {}` hash default trimmed; new tests/integration/helpers/leaflet-tile-url-test.js). The final edit of that helper test (attribute-value render to satisfy template-lint) was slice-verified and full-lint verified after the gate run; the numbers above are from the gate run.
Next: keep sweeping the small scaffold directories: the six `form` scaffolds in the same directories (fuel-report, warranty, place, integrated-vendor, service-area, zone) need `service:fetch` and `service:store` stubs for ModelSelect/CountrySelect; then modals/place-details and modals/point-map (their templates were fixed in #30 and now render). After that the red real suites, biggest first: order/details/tracking (11), order/form/service-rate (6), customer/form (5), telematic/details (5).
Notes: LeafletMap renders for real in rendering tests (`.leaflet-container`, `.leaflet-marker-icon`, `.leaflet-overlay-pane path`, `.leaflet-tooltip` are assertable) — a polygon layer with an empty `@locations` throws "latlngs not passed", so fixtures need at least three points. `format-currency` treats amounts as minor units (4550 → $45.50); `smart-humanize` yields "API Key". template-lint's no-curly-component-invocation flags `{{some-helper}}` with only named args in test templates — render the helper into an attribute value instead. `{{(helper)}}` in content position throws "func is not a function"; the parenthesised form is only for named-argument positions.

## 2026-09-04 — iteration 12 (Phase B: six form components)
Statements 3921/18808 (20.84%) · Branches 2469/12277 (20.11%) · Functions 1306/5527 (23.62%) · Lines 3782/17843 (21.19%) — tests 996: 772 pass / 224 fail (+11 pass) · 253 files fully covered
Did: real rendering suites replaced the six form scaffolds (fuel-report, warranty, place, integrated-vendor, service-area, zone; 13 tests); all six form components are at 100%. New shared helper tests/helpers/stub-form-inputs.js: template-only stand-ins for the store-backed ember-ui inputs (ModelSelect, CountrySelect, DatePicker, PhoneInput, MoneyInput, UnitInput, AvatarPicker, RegistryYield, CustomField::Yield) plus `makeRecord(modelName, attrs, { isNew })` — a record-like fixture whose static `modelName` and `isNew` let `cannot-write` resolve a real permission (plain objects resolve to nothing and render every input disabled). Deleted two actions no template invokes (DEFECTS #31).
Next: the two map modals (modals/place-details, modals/point-map — templates fixed in #30) need ember-ui's `Modal::Default` → `<Modal>` understood first (no modal test is green yet; read ember-ui's modal.hbs/js for its destination element and mount it inside #ember-testing like the sidebar wormhole). Then the remaining small scaffold directories by count: fleet-panel 3, fleet 3, device 3, activity 3, sensor 2, part 2, equipment 2, entity 2, contact 2, vendor 2, service-rate 2, maintenance 2, issue 2 (`grep -l "template block text" tests/integration/components/*/*.js | xargs -n1 dirname | sort | uniq -c`). Then the red real suites: order/details/tracking (11), order/form/service-rate (6), customer/form (5), telematic/details (5).
Notes: Ember's `<Input>` sets `value` as a property and the InputGroup's `@type` lands as a property too — attribute selectors like `input[value="x"]` or `input[type="text"]` never match; read `findAll('input').map((i) => i.value)`. A collapsed ContentPanel (`@open={{false}}`) renders no body; open it by clicking its `.next-content-panel-header-left`, and locate the right panel by its title text rather than by index. ember-ui's Toggle is `[role="checkbox"]` with `aria-checked`. A stand-in that hands itself to the parent through an `@onInputReady`-style callback must do so in `next()`, not in its constructor, or Ember asserts on writing a tracked field the parent already read in the same render. Unchecked `str.replace` edits in Python are silent no-ops after prettier reflows the anchor — always `assert old in s`.

## 2026-09-04 — iteration 13 (Phase B: map modals and the small identity components)
Statements 3920/18808 (20.84%) · Branches 2469/12277 (20.11%) · Functions 1306/5527 (23.62%) · Lines 3781/17843 (21.19%) — tests 1006: 791 pass / 215 fail (+19 pass) · 253 files fully covered
Did: real suites replaced nine scaffolds: modals/place-details and modals/point-map (the two map modals fixed in #30 — ember-ui's Modal renders into the test root under test, so `@modalIsOpened={{true}}` plus qunit-dom is enough; clicking the marker opens the popup so its content is assertable), and device/pill, driver/pill, vehicle/pill, entity/card, vehicle/card, driver/panel-header, vehicle/panel-header. No coverage delta: all nine are template-only or empty classes that `forceModulesToBeLoaded` already counted; the value is the red scaffold count (224 → 215) and one real bug — vehicle/pill.hbs passed `@this.resource=` instead of `@resource=` to the Pill and invoked a string as a helper in its tooltip (DEFECTS #32, fixed). tests/helpers/host-translations.js gained common.online/offline.
Next: the remaining scaffolds with real JS are where coverage moves. By JS size, cheap ones: route-optimization-engine-select-button (13 stmts), fleet/form (20), driver/details (21), vehicle/details (21, but a 705-line template), order-progress-bar (27), display-place (32), order-list-overlay/order (35), sensor/form (41), device/form (48), global-search (50), order-progress-card (52), vehicle/form (54), contact/form (57), entity/form (62). Take route-optimization-engine-select-button + order-progress-bar + display-place + order-list-overlay/order + fleet/form as one batch (use tests/helpers/stub-form-inputs.js for the forms), then the red real suites biggest first (order/details/tracking 11).
Notes: ember-ui Button checks `abilities.cannot(permission)` when given `@permission` — an abilities stub needs both `can` and `cannot`. `svg[data-icon="pencil"]` → `closest('button')` is the reliable way to click an icon-only ember-ui Button. Named blocks (`<:header>`) work in test templates as in app templates.

## 2026-09-04 — iteration 14 (Phase B: five small JS-bearing components)
Statements 3930/18804 (20.89%) · Branches 2484/12275 (20.23%) · Functions 1311/5526 (23.72%) · Lines 3791/17839 (21.25%) — tests 1016: 806 pass / 210 fail (+15 pass) · 258 files fully covered
Did: real suites for route-optimization-engine-select-button, order-progress-bar, display-place, order-list-overlay/order and fleet/form (15 tests); all five components at 100/100/100. DEFECTS #33: deleted fleet/form's unread `writePermission` getter and `statusOptions` field, and order-progress-bar's dead lazy `progress` initializer and unread `order` field. The shared model-select stand-in gained a clear button so relationship-clearing paths are testable. The one red non-scaffold test this run (vendor/panel-header) is the DEFECTS #28 fetch spill again.
Next: continue the JS-bearing scaffolds by size: order-progress-card (52), vehicle/form (54), contact/form (57), entity/form (62), sensor/form (41), device/form (48), global-search (50), custom-entity/form (90), avatar-picker (87), customer/admin-settings (82), activity/form (78), activity/event-selector (82), driver-onboard-settings (86). Take the four forms (sensor, device, contact, entity — stub-form-inputs covers their inputs) plus order-progress-card as one batch. The fetch spill (#28) keeps landing on vendor/panel-header; vendor/form's scaffold is the source — replacing it (vendor/form is 52 stmts) would close #28.
Notes: a QUnit regex filter of `/ order-list-overlay\/order/` (leading space) separates `Component | order-list-overlay/order` from `map/order-list-overlay/order`. ember-ui's Badge renders its own `svg[data-icon="circle"]` dot — scope online-indicator assertions to their wrapper (`.resource-assigned-photo svg`). `doubleClick` from @ember/test-helpers fires two clicks plus dblclick. The `dropdown-fn` helper closes the dropdown before calling the action, so `click('.next-dd-item')` after opening `.ember-basic-dropdown-trigger` is the whole interaction.

## 2026-09-04 — iteration 15 (Phase B: vendor, sensor and contact forms, order progress card)
Statements 3977/18800 (21.15%) · Branches 2507/12275 (20.42%) · Functions 1325/5524 (23.98%) · Lines 3835/17835 (21.5%) — tests 1024: 818 pass / 206 fail (+12 pass) · 262 files fully covered
Did: real suites for vendor/form (6 tests: type selection through the real PowerSelect, provider integration through a FetchSelect stand-in, address controls), sensor/form, contact/form (photo upload through an UploadButton stand-in and a fetch stub, success and failure) and order-progress-card (the 100ms `later` load, its callback, failure and the three skip conditions); all four at 100/100/100. DEFECTS #34: deleted the sensor form's never-invoked upload task and reordered the progress card's guard so a missing order returns instead of throwing. tests/helpers/host-translations.js gained common.edit-address/new-address. Replacing vendor/form did NOT close #28: the run still logs four "Failed to fetch" and one landed on vendor/panel-header again, so the un-awaited requests come from other scaffolds that still mount real ModelSelects (vendor/details renders just before it — check whether its template or a neighbour's mounts a store-backed select).
Next: find the real #28 source first — grep the `it renders` scaffolds still red for components whose templates mount ModelSelect/FetchSelect/CountrySelect (`grep -l "ModelSelect\|FetchSelect" addon/components/**/*.hbs` intersected with the red scaffold list) and replace the ones nearest vendor/panel-header in run order (vendor/details, vehicle/form, vehicle/details). Then continue the JS-bearing forms: device/form (48), entity/form (62), vehicle/form (54), custom-entity/form (90).
Notes: Font Awesome aliases (`edit` → `pen-to-square`) never appear in `data-icon`; select icon-only buttons by their rendered text or a non-alias icon. eslint-plugin-ember's `no-settled-after-test-helper` auto-removes `await settled()` after `render` — `render` already waits for `later` timers, so a component's post-render `later(...)` work is settled by the time `render` resolves. A self-describing lookup that throws with the list of candidate labels beats a bare `find(...)` when a selector misses.

## 2026-09-04 — iteration 16 (Phase B: vendor, customer and driver details, entity form)
Statements 3986/18799 (21.2%) · Branches 2511/12273 (20.45%) · Functions 1330/5524 (24.07%) · Lines 3844/17834 (21.55%) — tests 1030: 828 pass / 202 fail (+10 pass) · 264 files fully covered
Did: real suites for vendor/details, customer/details, driver/details and entity/form (10 tests); all four at 100/100/100. Five findings fixed under DEFECTS #35: vendor/details branched on a property its empty class never had and passed the wrong argument name to IntegratedVendor::Details (integrated vendors never got their details view), customer/details labelled the phone field "email", two templates called `join` with the array first (skills rendered without spaces), and entity/form carried a dead lazy initializer and an unreachable null-clear guard. Replacing vendor/details did NOT reduce the four "Failed to fetch" spills (#28) — the spill's true origin is still open (victims and their predecessors are listed by the awk in this iteration's session log; next time run it against the fresh cov log before picking).
Next: pin the #28 origin properly: `awk '/^(ok|not ok) [0-9]+ Chrome/{prev=cur; cur=$0} /Failed to fetch/{print cur; print "   prev: " prev}' <cov log>` lists each victim and the test before it; the origin is a scaffold shortly before each victim whose template mounts a real ModelSelect/FetchSelect/CountrySelect or a CustomField::Yield without a stand-in. Then continue JS-bearing scaffolds: device/form (48), vehicle/form (54), custom-entity/form (90), avatar-picker (87), customer/admin-settings (82), activity/form (78), driver-onboard-settings (86), global-search (50), device/manager (92).
Notes: `fleetOpsOptions('entityTypes')` includes `crate` — fixtures meant to be "unknown" must use a value that is really absent from the option list. ember-composable-helpers' `join` is `(join separator array)` and tolerates the reversed order by falling back to a bare comma, which is why the mistake went unnoticed. When a Python edit spans a prettier-reflowed multi-line statement, replace by exact multi-line anchor, never by scanning to the next `);`.

## 2026-09-04 — iteration 17 (Phase B: the two self-fetching scaffolds and global search)
Statements 4039/18794 (21.49%) · Branches 2541/12269 (20.71%) · Functions 1349/5524 (24.42%) · Lines 3894/17830 (21.83%) — tests 1035: 836 pass / 199 fail (+8 pass) · 267 files fully covered
Did: real suites for driver-onboard-settings (load, every field edit, save with both response paths, null payload, failed save), widget/fleet-ops-key-metrics (spinner while pending, formatted top metrics in priority order, null values, empty/undefined payload) and global-search (hidden state, debounced store query with route/entity rendering, result click, empty query, non-array and failed responses, escape to hide); all three at 100/100/100. DEFECTS #36: the onboarding settings crashed on a null payload (fixed at the load), plus dead defaults/guards/initializers in both fetching components. The #28 fetch spill dropped from four to two per run — the two self-fetching scaffolds were origins. The remaining victim is `vendor/panel-header: it renders compact vendor identity` itself (its predecessor is a green vendor/form test), so the last origin is inside that render: check whether ember-ui's `Image` fetches its `@fallbackSrc`/`src` (addon/components/image.js) when `src` is undefined — if so, a stand-in for `image` in that suite closes #28.
Next: close #28 via the Image lead above, then continue JS-bearing scaffolds: device/form (48), vehicle/form (54), custom-entity/form (90), avatar-picker (87), customer/admin-settings (82), activity/form (78), device/manager (92), activity/event-selector (82). Then the red real suites biggest first (order/details/tracking 11, order/form/service-rate 6, customer/form 5, telematic/details 5).
Notes: a pending fetch stub (`new Promise` kept unresolved) plus `render(...)` without `await` and one `requestAnimationFrame` tick is enough to assert a loading state before resolving; `await settled()` afterwards is fine because no helper preceded it. `fillIn('select', value)` drives ember-ui's Select (`@onSelect` receives the value). `triggerKeyEvent(el, 'keydown', 'Escape')` reaches `{{on "keydown"}}` handlers. `@tracked` fields assigned by a load task before any template read are dead initializers — the fourth time this shape has appeared (DEFECTS #15, #33, #35, #36); check for it whenever a component fetches in its constructor.

## 2026-09-04 — iteration 18 (Phase B: fetch spill closed, device form, avatar picker, custom entity form)
Statements 4070/18790 (21.66%) · Branches 2564/12265 (20.9%) · Functions 1356/5523 (24.55%) · Lines 3924/17827 (22.01%) — tests 1044: 850 pass / 194 fail (+14 pass) · 271 files fully covered
Did: DEFECTS #28 closed — the last "Failed to fetch" origin was ember-ui's CountryName inside Vendor::PanelHeader (stack named it); a `country-name` stand-in in that suite brings the full run to zero spills. Real suites for device/form (every section, both PowerSelects, telematic lock in three shapes, upload success and failure, no-write), avatar-picker (endpoint derivation, URL/UUID/clear paths, peeked vs loaded file, failed load, fast paths, no callback) and custom-entity/form (bindings, unit changes, upload gating); device/form, avatar-picker and vendor/panel-header at 100/100/100. Two dead statements deleted (DEFECTS #39). Two product findings: custom-entity image upload always throws because `this.config` is never provided (#37, NEEDS DECISION — its two functions stay uncovered until decided), and the dasherized type handler is overwritten by the two-way Input (#38, NEEDS DECISION; the suite characterises it).
Next: JS-bearing scaffolds: customer/admin-settings (82), activity/form (78), device/manager (92), activity/event-selector (82), vehicle/form (54 JS, 622-line template — budget a whole iteration). Then the red real suites biggest first (order/details/tracking 11, order/form/service-rate 6, customer/form 5, telematic/details 5).
Notes: ember-core's `isModel` accepts only Ember Data records and ObjectProxy, and reading a property straight off an ObjectProxy asserts — components that keep `this.model = args.model` and read `this.model.foo` need a real record (`store.createRecord('vehicle', …)` works in the dummy; intercept `peekRecord`/`findRecord` on the store instance). A component that copies `@model` in its constructor needs a fresh `render` when the test swaps models. ember-ui's FetchSelect fetches in its constructor whenever `@optionValue` is set — always stand it in. InputGroup spreads `...attributes` onto its `<Input>`, so an `{{on "input"}}` on the group races the Input's own two-way write and loses.

## 2026-09-04 — iteration 19 (Phase B: customer admin settings, device manager, activity form and event selector)
Statements 4149/18782 (22.09%) · Branches 2588/12251 (21.12%) · Functions 1376/5522 (24.91%) · Lines 4001/17821 (22.45%) — tests 1052: 862 pass / 190 fail (+12 pass) · 275 files fully covered
Did: real suites for customer/admin-settings (load of order types and both settings, toggling order types on/off with success and failure, payments gated on Stripe via window instance or publishable key, null payments config, failing loads), device/manager (list, attach through the modal with no selection/success/failure, detach with the four-way device-name fallback, the resource-name fallback chain, failing query), activity/event-selector (add/remove, empty state, no callback) and activity/form (every binding, status derivation, toggles, POD select, logic/events callbacks with and without arguments, no-permission state); all four at 100/100/100. One bug fixed in its own commit (DEFECTS #40: `@disable` typo left the payments toggle enabled before onboarding). DEFECTS #41: dead field, task, twelve optional-chain branches, a lazy initializer and two no-resource guards deleted. #38 amended: activity/form key/code have the same two-way-Input race. `customer/admin-settings.js` now imports `window` from ember-window-mock; the dummy config gained the console's `stripe` block. Zero fetch spills again.
Next: vehicle/form (54 JS statements, 622-line template — budget the whole iteration; AvatarPicker/ModelSelect/CountrySelect stand-ins are all in stubFormInputs). Then the red real suites biggest first (order/details/tracking 11, order/form/service-rate 6, customer/form 5, telematic/details 5), then the remaining `it renders` scaffolds by JS size from the batch script.
Notes: ember-ui's Toggle exposes `data-disabled` as a boolean attribute (present/absent), so assert `hasAttribute('data-disabled')` / `doesNotHaveAttribute`. A Glimmer class stand-in (`setComponentTemplate(hbs, class extends Component { @action … })`) is the way to call an `@onChange` with *no* arguments to reach a default parameter; a template-only `(fn @onChange)` always passes the event. Row selectors need `> span:first-child` when an ember-ui Button sits in the row (it renders its own spans). The lazy-initializer test: a `@tracked x = []` read only after a failed fetch *is* reachable — cover it via the failing-fetch path rather than deleting it.

## 2026-09-04 — iteration 20 (Phase B: the vehicle form)
Statements 4156/18779 (22.13%) · Branches 2590/12249 (21.14%) · Functions 1379/5520 (24.98%) · Lines 4008/17818 (22.49%) — tests 1053: 864 pass / 189 fail (+2 pass) · 276 files fully covered
Did: a real suite for vehicle/form (all 36 bound text inputs in DOM order, the 11 PowerSelects, 18 unit / 4 money / 3 date pickers, both checkboxes, the five registries in order, driver assignment with and without existing meta, status pick, checkbox toggle, upload success and failure, and the no-write state); vehicle/form.js at 100/100/100. Two template bugs fixed in their own commit (DEFECTS #42: vehicle and driver forms passed `this.controller`, which neither defines, to every registry component; #43: 23 vehicle fields ignored `cannot-write`). #44: two actions and a field nothing referenced deleted. The 622-line template needed only five local stand-ins beyond stubFormInputs (upload-button, model-coordinates-input, metadata-editor, currency-select, multi-select).
Next: driver/form (its `it renders` scaffold is now the next red form; the controller fix already landed there — check for the same ungated-field gap). Then the red real suites biggest first (order/details/tracking 11, order/form/service-rate 6, customer/form 5, telematic/details 5), then remaining `it renders` scaffolds by JS size from the batch script.
Notes: for a big form, enumerate the bound fields with `grep -n "@value={{@resource\.[a-z_]*}}"` (DOM order == line order) and assert `findAll(TEXT_INPUTS).map(v)` against one `[field, value]` table — one assertion covers the whole binding surface and immediately exposes ungated or unbound fields. `set-model-attr` writes `option.value`, so pick options by index and assert the option's value. The `it renders` scaffold count is dropping into the low 180s; the batch script's top rows are now mostly real red suites rather than scaffolds.

## 2026-09-04 — iteration 21 (Phase B: the driver form)
Statements 4163/18779 (22.16%) · Branches 2589/12249 (21.13%) · Functions 1383/5520 (25.05%) · Lines 4015/17818 (22.53%) — tests 1055: 867 pass / 188 fail (+3 pass) · 277 files fully covered
Did: a real suite for driver/form (bindings incl. the read-only user details that appear and disappear with the user, vendor/vehicle/user selects, status, upload success/failure, the "create user" ContentPanel action button end-to-end — engine load, record creation, modal options, the modal's photo upload and its confirm on success and failure — and the no-write state); driver/form.js at 100/100/100. DEFECTS #45: both the driver and customer forms sent `subject_uui` on the new user's photo upload, fixed in its own commit. Branches covered moved 2590 → 2589 with an unchanged total — one branch elsewhere flipped between runs; watch whether it recurs before chasing it.
Next: the red real suites biggest first: customer/form (5 red — the welcome-email and user-selector tests; read the failures in the cov log first), order/details/tracking (11), order/form/service-rate (6), telematic/details (5). Then remaining `it renders` scaffolds by JS size from the batch script.
Notes: ContentPanel `@actionButtons` render as ember-ui Buttons — locate one by its icon (`svg[data-icon="user-plus"]`.closest('button')) and call the captured modal `options` directly (`uploadNewPhoto`, `confirm`) to reach the closures. When the dummy lacks a model (`user`), override `store.createRecord` on the instance with a record-like object carrying `setProperties` and `save`. Fixture statuses must come from `fleet-ops-options` (`driverStatuses` starts `available`, `inactive`) — `active` is not an option for drivers or vehicles.

## 2026-09-04 — iteration 22 (Phase B: the customer form)
Statements 4175/18766 (22.24%) · Branches 2593/12249 (21.16%) · Functions 1388/5516 (25.16%) · Lines 4026/17805 (22.61%) — tests 1054: 871 pass / 183 fail (+4 pass, −5 fail) · 278 files fully covered
Did: rewrote the red customer/form integration suite (its POJO fixture crashed `cannot-write`, and a module-level `setComponentTemplate` on a shared stub class threw on the second test) into four tests: bindings and the address flow (edit/new/remove/select), the welcome-email opt-in with and without existing meta, the hidden states, and no-write; customer/form.js at 100/100/100 together with its existing unit suite. Two fixes in their own commits: DEFECTS #46 (the `New Adddress` label) — and, from the profile, #47: the customer form carried a dead copy of the driver form's "create user" action-button block (never rendered), deleted with its two injections. The unit suite legitimately calls `toggleWelcomeEmail()` with no argument, so the default parameter stays.
Next: order/details/tracking (11 red), order/form/service-rate (6), telematic/details (5) — read each suite's failures in the cov log first; the harness fixes so far (makeRecord, per-suite stand-ins, abilities stub) cover most shapes. Then remaining `it renders` scaffolds by JS size from the batch script. Run `npx prettier --write` on every new test before the gate — three iterations in a row lost a lint pass to a wrap.
Notes: `this.set` on a rendered resource needs `await settled()` before the next DOM lookup. Button labels come from the addon's `translations/en-us.yaml` first — read the exact value there before matching (`t "customer.fields.new-address"` was misspelt). A unit suite that exercises a default parameter counts as a caller: check `tests/unit` before deleting "unreachable" defaults.

## 2026-09-04 — iteration 23 (Phase B: order details tracking)
Statements 4212/18737 (22.47%) · Branches 2628/12218 (21.5%) · Functions 1394/5514 (25.28%) · Lines 4060/17775 (22.84%) — tests 1063: 891 pass / 172 fail (+20 pass, −11 fail) · 280 files fully covered
Did: the eleven red order/details/tracking tests had three harness faults (DEFECTS #49: a stub missing `viewLabel`, an order builder that clobbered its own merged payload, and a "Due now" expectation for a feature removed in 9356fb67) and exposed one real bug (#48: the active stop matched on `undefined === undefined` ids, fixed in its own commit). Nine tests added over the lifecycle fallbacks, confidence/warnings/diagnostics, active-stop labels and reported ETA lookup, progress fallbacks, ping driver, and the assign-driver no-op; tracking.js (625 lines) at 100/100/100. #50: three unrendered getters and seven template-redundant guards deleted.
Next: order/form/service-rate (6 red), telematic/details (5 red), then tracking-stop-progress (1 red real test) and order-tracking-lookup; then remaining `it renders` scaffolds by JS size from the batch script.
Notes: for a big getter-heavy component, drive everything through rendered text/classes/`style` attributes rather than reaching for the instance; `hasAttribute('style', 'width: 33%;')` pins the numeric getters. `Number(null)` is 0, so "missing percentage" fixtures must use `undefined`/`{}`. When a stale test names behaviour the addon no longer has, `git log -S"<text>"` on the component finds the commit that removed it — cite it in the test and DEFECTS rather than guessing intent.

## 2026-09-04 — iteration 24 (Phase B: the order service-rate form)
Statements 4269/18737 (22.78%) · Branches 2673/12217 (21.87%) · Functions 1413/5514 (25.62%) · Lines 4114/17775 (23.14%) — tests 1062: 897 pass / 165 fail (+6 pass, −7 fail) · 281 files fully covered
Did: the six red order/form/service-rate tests (DEFECTS #51: a scaffold, a POJO crashing `cannot-write`, four hand-constructed Glimmer components) are replaced by six rendering tests on a tracked fixture class covering the toggle → rates → quotes → selection → refresh → stale clearing flow, the four disabled states, the debounced matching-order refresh with quotes kept while updating, rate loading from a refresh, a refresh losing its route mid-debounce, and the locked contract override with its currency fallbacks; service-rate.js at 100/100/100. One `= {}` default deleted (only caller always passes an object). Host translations gained `common.total`.
Next: telematic/details (5 red), tracking-stop-progress (1 red real test), order-tracking-lookup and service-rate/{details,form} scaffolds; then remaining `it renders` scaffolds by JS size from the batch script.
Notes: components that assign `resource.x = …` directly need a fixture whose fields are `@tracked` (a small class with static `modelName`, `isNew`, `set`, `setProperties`) — `makeRecord`'s plain fields never re-render. ember-concurrency `timeout()` is settled-aware, so `await settled()` waits out a debounce; never `setTimeout` in tests. `format-currency` renders EUR as "€ 5,00" — match currency text with a regex tolerant of the space and decimal separator. Manual `new GlimmerComponent(owner, args)` throws under this harness — always render instead.

## 2026-09-04 — iteration 25 (Phase B: telematic details)
Statements 4279/18737 (22.83%) · Branches 2688/12217 (22%) · Functions 1412/5514 (25.6%) · Lines 4124/17775 (23.2%) — tests 1067: 907 pass / 160 fail (+10 pass, −5 fail) · 282 files fully covered
Did: the five red telematic/details tests all died inside `CustomField::Yield` (DEFECTS #52: its load task peeks the store for a company the test app never has; the suite lacked the stand-in every other yield-mounting suite registers). Five tests added over the health cards in every state, hardware identity, sensitive-error masking and the query-string webhook URL; telematic/details.js at 100/100/100. Functions covered moved 1413 → 1412 with an unchanged total — a second run-to-run flip (branches did the same in iteration 21); if it recurs, diff two cov logs' per-file function counts to name it.
Next: tracking-stop-progress (1 red real test), order-tracking-lookup, service-rate/{details,form} scaffolds; then remaining `it renders` scaffolds by JS size from the batch script (`node -e` in §4 against coverage/coverage-summary.json).
Notes: any template mounting `CustomField::Yield` needs the `custom-field/yield` stand-in — it fetches through `currentUser.loadCompany` even in `@viewMode`. `format-date-fns` renders in the browser's zone; assert dates with a pattern (`/^1[12] May 2026, \d\d:\d\d$/`) rather than a fixed hour. A boolean argument echoed into a `data-*` attribute renders as an empty attribute — assert presence, not `="true"`.

## 2026-09-04 — iteration 26 (Phase B: tracking stop progress and service-rate details)
Statements 4285/18738 (22.86%) · Branches 2689/12218 (22%) · Functions 1414/5515 (25.63%) · Lines 4129/17775 (23.22%) — tests 1072: 913 pass / 158 fail (+6 pass, −2 fail) · 282 files fully covered
Did: the red tracking-stop-progress test exposed the same undefined-id matching bug as #48 in the stop rail (DEFECTS #53, fixed in its own commit); its suite now covers uuid/public-id matching, labels, titles, place fallbacks and the empty rail, and tracking-stop-progress.js is at 100/100/100 (it was already fully counted through the parent suite, hence no change in the fully-covered count). service-rate/details (empty class, 367-line template) gets three rendering tests over every rate-calculation panel and fee block (#54). Functions covered returned to 1414 — the iteration-25 flip was transient.
Next: order-tracking-lookup (209-line JS: urlSearchParams, engine services `location`/`movementTracker`, a LeafletMap with a routing control that must be stood in or the OSRM request will spill — check how `@engineService` resolves in the dummy before starting); then the remaining `it renders` scaffolds by JS size from the batch script, and service-rate/form (92 JS / 676-line template) as its own iteration.
Notes: `f-to-int` strips non-digits before parsing, and the details template runs per-drop/multi-zone/parcel fees through it before `format-currency` — fixtures must use integer minor units (250 → $2.50), never decimals. Translation labels drift from their keys (`estimated-days` → "Estimated Delivery Days"): read the yaml value before matching on a label.

## 2026-09-04 — iteration 27 (Phase B: the order tracking lookup)
Statements 4385/18736 (23.4%) · Branches 2721/12210 (22.28%) · Functions 1437/5516 (26.05%) · Lines 4229/17773 (23.79%) — tests 1074: 917 pass / 157 fail (+4 pass, −1 fail) · 283 files fully covered
Did: order-tracking-lookup (209-line JS: url params, engine services, a real LeafletMap with the driver tracking marker, an OSRM routing control) is at 100/100/100 through four rendering tests. Three findings: DEFECTS #55, a real bug fixed in its own commit — the lookup form's submit button performed the task twice and then let the browser reload the page (the test harness caught it as a page navigation that restarted the whole suite); #56, harness — the engine's tracking-marker initializer never runs in the dummy (call `initialize(owner)` in the suite) and the dummy config now mirrors the console's `defaultValues` with data-URI images; #57, four template-redundant guards deleted. The routing control's OSRM request is answered by a per-test fake `XMLHttpRequest`, so nothing reaches `router.project-osrm.org`. Branches/functions dipped 5/2 from the mid-iteration run: with default images now resolving, `fallback-img-src`/Image error handlers stopped firing incidentally — coverage that would have vanished in CI anyway.
Next: remaining `it renders` scaffolds by JS size from the batch script (`node -e` in §4 against coverage/coverage-summary.json — run it first; the red list is now 157 with most being scaffolds), then service-rate/form (92 JS / 676-line template) as its own iteration.
Notes: a `type="submit"` Button inside a `<form>` navigates the test page when clicked unless the submit handler prevents the default — a suite that suddenly runs the whole test list without its filter (1510 tests, no coverage file) is this trap. `layers.tracking-marker` (and any component an engine instance-initializer registers) is absent in the dummy until the initializer's `initialize(owner)` runs. `@mapbox/corslite` reads the global `XMLHttpRequest` at call time and checks `'onload' in xhr` — a fake must declare `onload`/`onerror` fields. Leaflet icons and ember-ui's `fallback-img-src` require absolute URLs; data URIs satisfy both without network.

## 2026-09-04 — iteration 28 (Phase B: eight small scaffolds)
Statements 4413/18734 (23.55%) · Branches 2731/12208 (22.37%) · Functions 1450/5514 (26.29%) · Lines 4257/17771 (23.95%) — tests 1078: 929 pass / 149 fail (+12 pass, −8 fail) · 291 files fully covered
Did: a sweep over the eight smallest JS-bearing scaffolds — map/drawer, route-optimization-wizard-panel, order/details/{metadata,custom-fields,detail,notes}, vehicle/details, map/toolbar/zones-panel — all eight at 100/100/100 (+8 fully covered files). One template bug fixed in its own commit (DEFECTS #58: vehicle skills joined with reversed `join` arguments, the #35 shape). #59: an unrendered action and an unused getter deleted. `AbilitiesStub` gained `cannot` (ember-ui's Button calls it). Ranking script that found them (scaffold names from the cov log, coverage keys are `addon/components/...` without a leading slash) is in this iteration's session log; the remaining `it renders` scaffolds are 117: 50 template-only modals/panels, 23 empty-class components, and ~44 with real JS (next by size: order/customer-avatar-stack 8 st, order/details 6, map/drawer/{place,vehicle}-listing 16, modals/reset-customer-credentials 20, map/drawer/device-event-listing 25, order/details/documents 15, map/drawer/driver-listing 19).
Next: the next tranche of small scaffolds by JS size — order/customer-avatar-stack, order/details, map/drawer/place-listing, map/drawer/vehicle-listing, order/details/documents, modals/reset-customer-credentials — then the 23 empty-class scaffolds in one sweep (template-only, cheap greens).
Notes: QUnit regex filters must not contain `\|` — alternate on module fragments (`/map\/drawer:|order\/details\/notes/`), and a trailing `:` pins a module against its sub-components. ember-ui's hover dropdowns close on every `dropdown-fn` action — reopen with `mouseenter` (300ms `later`, settled-aware) before the next item. `dropdown-fn` only checks `Object.keys(dd).includes('uniqueId')`, so a fake `@dd` needs `uniqueId` and `actions.close`. `ContentPanel @open={{false}}` renders no body — fields inside collapsed panels are not in the DOM.

## 2026-09-04 — iteration 29 (Phase B: six more scaffolds, a double-upload bug)
Statements 4469/18730 (23.86%) · Branches 2756/12202 (22.58%) · Functions 1473/5514 (26.71%) · Lines 4313/17768 (24.27%) — tests 1084: 941 pass / 143 fail (+12 pass, −6 fail) · 297 files fully covered
Did: real suites for order/customer-avatar-stack, order/details (default and block layouts with all fourteen child panels stood in), map/drawer/{place,vehicle}-listing (real ember-ui Table: filter, anchor/point cells, the row dropdown's every action), order/details/documents (real ember-file-upload via `selectFiles` from `ember-file-upload/test-support`), modals/reset-customer-credentials; all six at 100/100/100. DEFECTS #60, a real bug fixed in its own commit across seven templates: every document/avatar selection uploaded twice because the dropzone's `file-queue` listener and ember-ui's FileUpload registered the same callback on the same queue. #61: two queue-redundant guards and a lazy initializer deleted.
Next: the 23 empty-class scaffolds in one sweep (contact/details, equipment/details, issue/form, maintenance/details, map/order-list-overlay, modals/{attach-device,bulk-assign-driver,confirm-service-quote-purchase,service-quote-purchase-form}, order/{activity-list,activity-timeline,kanban-card,panel-header,pill}, order/details/{comments,purchase-rate}, order/form/{custom-fields,metadata,notes}, part/details, sensor/details, work-order/details, map/order-list-overlay/driver-panel-title) — template-only greens; then map/drawer/{device-event,driver}-listing (same shape as this iteration's listings), fleet-panel/{vehicle,driver}-listing, fleet/{driver,vehicle}-listing.
Notes: ember-ui's Table renders the dropdown cell's menu out of place — find items with `findAll('.next-dd-item')` after clicking `.cell-dropdown-button .ember-basic-dropdown-trigger` inside the row. `selectFiles('input[type="file"]', new File([...], name, { type }))` drives FileUpload and yields a queued UploadFile whose `queue` is set. A `fetch.uploadFile.perform` stub gets `(file, options, onSuccess, onError)`. Keep the host-translations file free of duplicate keys — eslint's `no-dupe-keys` fails the gate.

## 2026-09-04 — iteration 30 (Phase B: the 23 empty-class scaffolds, four template slips, a dead duplicate directory)
Statements 4437/18718 (23.70%) · Branches 2749/12192 (22.54%) · Functions 1463/5510 (26.55%) · Lines 4281/17756 (24.11%) — tests 1083: 964 pass / 119 fail (+23 pass, −24 fail) · 296 files fully covered
Did: real suites for all 23 empty-class components — contact/equipment/maintenance/part/sensor/work-order details, issue/form (real PowerSelects driven through the wormhole, a registry probe proving both registries get `@controller`), map/order-list-overlay (stubbed overlay/order-actions/fleet-actions services, real BasicDropdowns, `router:main` intercepted), the four modals, order/{activity-list,activity-timeline,kanban-card,panel-header,pill}, order/details/{comments,purchase-rate}, order/form/{custom-fields,metadata,notes}, driver-panel-title. These classes carry zero coverable statements, so the gain is 23 red scaffolds turned green. DEFECTS #62 (fix commit cff7b292): contact phone labelled "Email", order/pill reading status/dates/type from `@resource` while resolving the record from `@order`, issue/form passing `this.controller` to its registry, and the bulk-assign help-text key defined nowhere. #63: `addon/components/order-list-overlay/` was a stale pre-monorepo copy of the map overlay row and driver title with no callers — deleted with its `app/` re-exports and scaffolds.
Notes: totals dipped (−32 covered statements, −1 fully covered file) despite no new failures: the old failing scaffolds for map/order-list-overlay and order/kanban-card rendered the real `order-list-overlay`, `order-actions` and `fleet-actions` services and the real overlay row, painting incidental statements that the new suites stub deliberately. Those services now show their honest numbers (3/74, 6/171, 5/44) and need their own unit suites. Empty Glimmer classes report 0/0 and count as fully covered. ember-ui's Badge root is `.status-badge`; FaIcon renders nothing for icons outside the registered set (`search`, `cog`) — select Buttons by `.btn-wrapper button`, not by icon. Service stubs passed as template actions (`{{this.overlay.close}}`) must be arrow fields, not methods. `t "common.metadata"` is a host key (added to host-translations).
Next: map/drawer/{device-event,driver}-listing (same shape as iteration 29's listings), fleet-panel/{vehicle,driver}-listing, fleet/{driver,vehicle}-listing; then unit suites for services/order-list-overlay (load/search tasks, selection, peek filters), fleet-actions and order-actions, whose incidental coverage this iteration removed.

## 2026-09-04 — iteration 31 (Phase B: the six listings — four covered, two deleted, three bugs)
Statements 4480/18656 (24.01%) · Branches 2758/12169 (22.66%) · Functions 1477/5497 (26.86%) · Lines 4323/17694 (24.43%) — tests 1093: 981 pass / 112 fail (+17 pass, −7 fail) · 300 files fully covered
Did: real suites for map/drawer/{device-event,driver}-listing and fleet/{driver,vehicle}-listing, all four now at 100/100/100 (device-event 25s/10b/7f, driver 19s/8b/9f, fleet listings 30s/7b/6f each). The batch's other two, fleet-panel/{driver,vehicle}-listing, turned out to be dead (DEFECTS #65) and are deleted with fleet-panel/details, their app/ re-exports and three scaffolds. Three real bugs, each fixed in its own commit before the coverage commit: #64 (4019861f) the device-events drawer wrote its query result to `this.positions`, a property nothing declares, while the template renders `this.events` — the tab always showed its empty state; #66 (33d8d538) the Device column passed `deviceActions.panel.view` straight to `table/cell/anchor`, which invokes `column.action(row)`, so clicking a device name opened a device panel bound to the device-event record; #67 (c79f8382) both fleet listings debounced on `params.value`, a key neither caller sends, with the test inverted against the pre-refactor original — the two mistakes cancelled into a pointless 300ms delay on every initial load. #68: a lazy `selectable = false` initializer, an unused `params = {}` default and a dateFilter guard the field makes redundant, all deleted.
Next: the remaining red suites, largest first — work-order/form (4 tests), device/details (4), device-event/details (4), telematic/settings (3), order/form/route (3), order/form/details (3), then order/form/orchestrator-constraints, order/form, equipment/form, device/panel-tabs (2 each). 80 `it renders` scaffolds remain. The biggest untouched denominators are services/map-adapter/{google,leaflet}.js (1889 and 991 missing), orchestrator-workbench (783) and customer/create-order-form (652).
Notes: `table/cell/anchor` invokes `column.action(row)` and never resolves the column's valuePath — a column with a nested valuePath must resolve the model off the row itself (#66). ember-ui's Table renders the dropdown menu out of place, so select row anchors by column position (`row.querySelectorAll('a')[n]`), not by text. A store stub that resolves immediately gives `waitFor` no window to see a loading spinner — hold the query open with a deferred and release it after asserting. `common.remove` and `common.loading-resource` are host keys (added to host-translations).

## 2026-09-04 — iteration 32 (Phase B: the connectivity suites — three app-tree defects)
Statements 4566/18654 (24.47%) · Branches 2823/12167 (23.20%) · Functions 1532/5497 (27.86%) · Lines 4406/17692 (24.90%) — tests 1102: 1004 pass / 98 fail (+23 pass, −14 fail) · 300 files fully covered
Did: took the ledger's red list — device/details (4), device-event/details (4), work-order/form (4), device/panel-tabs (3) — all now green, plus a new suite for device/panel-tabs/vehicle (0/24 → 22s/29b/18f). The suites were red for production reasons, not test ones. #69 (9bef934a): `app/` held four `.hbs` files byte-identical to the addon's co-located templates, and Ember rejects a co-located template beside an `export { default }` re-export outright — device-event/details and all three device panel tabs threw on resolution in the console as well as in tests. #70 (9b63c72c): removing those templates exposed a second defect, `app/components/device/panel-tabs/{sensors,events,vehicle}.js` were byte-identical copies of the addon classes rather than re-exports, so the tabs then rendered an empty node; they are re-exports now. #71: a `locateVehicle` guard the template's `canLocateVehicle` already makes, deleted. Test-side: all three details/form templates render `CustomField::Yield`, whose load task peeks the store through `currentUser.loadCompany()` — stood in; the panel-tabs suite shared one stub class across tests, which `setComponentTemplate` refuses twice; work-order/form's POJO fixtures needed `set` and tracked fields because the form mutates through `set-model-attr`.
Next: the largest remaining red suites — telematic/settings (3), order/form/route (3), order/form/details (3), then order/form/orchestrator-constraints, order/form, equipment/form (2 each). 80 `it renders` scaffolds remain. Partial files worth finishing while their suites are fresh: work-order/form (25/59 s, 2/30 b — the PowerSelect option lists and the completion panel), device/details (30/52 s, 46/81 b), device/panel-tabs/{sensors,events} (the column `action` arrows need the Tabular stub to render an anchor). The biggest untouched denominators are still services/map-adapter/{google,leaflet}.js (1889 and 991) and orchestrator-workbench (783).
Notes: an addon component with a co-located template must have a plain `export { default }` re-export in `app/` and nothing else — no `.hbs`, no copied class. A sweep for either mistake is one command: `find app -name '*.hbs'` and grepping `app/components/**/*.js` for files that do not start with `export { default } from`. Seven addon components still have no `app/` re-export at all (equipment/{card,panel-header}, part/{card,panel-header}, work-order/panel-header, admin/navigator-app, issue/timeline) — unverified, worth a look next iteration.

## 2026-09-04 — iteration 33 (Phase B: the seven components the app tree never exported)
Statements 4593/18654 (24.62%) · Branches 2840/12167 (23.34%) · Functions 1544/5497 (28.08%) · Lines 4433/17692 (25.05%) — tests 1121: 1023 pass / 98 fail (+19 pass, 0 fail change) · 300 files fully covered
Did: closed the question the last ledger left open. The seven components with no `app/` re-export — equipment/{card,panel-header}, part/{card,panel-header}, work-order/panel-header, issue/timeline, admin/navigator-app — are all invoked by live addon templates, and a rendering test proved they do not resolve: "Attempted to resolve `equipment/card`, which was expected to be a component, but nothing was found." DEFECTS #72 (b6c4b6f1) adds the seven missing re-exports; every one of the 272 addon components now has exactly one, and `app/` holds no templates and no copied classes (#69, #70). New suites for all seven: issue/timeline 14/14 s, 8/8 b, 5/5 f and admin/navigator-app 6/6, 2/2, 2/2 — both complete; the other five are empty or service-only classes that report 0/0. `issue/details` also picked up its first 4 statements now that its timeline resolves.
Next: the largest remaining red suites — telematic/settings (3), order/form/route (3), order/form/details (3), then order/form/orchestrator-constraints, order/form, equipment/form (2 each). 80 `it renders` scaffolds remain, and `issue/details` (4/46) is one of them. Partial files worth finishing: work-order/form (25/59 s, 2/30 b), device/details (30/52 s, 46/81 b), device/panel-tabs/{sensors,events}. Biggest untouched denominators: services/map-adapter/{google,leaflet}.js (1889 and 991), orchestrator-workbench (783).
Notes: `{{humanize}}` renders sentence case ("Lift truck"), unlike `smart-humanize` ("Lift Truck"); it resolves through ember-ui's nested ember-cli-string-helpers dependency, not a direct one. ember-ui's `Image` applies its `@fallbackSrc` when no `src` is given, so a photo-less record still has a `src` — assert the data URI, not null. The dummy config's `defaultValues` gained `equipmentImage` and `partImage`, which the two card templates read. The fully-covered count held at 300 while two files completed, so two others left that set; the current list is saved at `scratchpad/full47.txt` for the next iteration to diff if it matters.

## 2026-09-04 — iteration 34 (Phase B: the three red form suites)
Statements 4618/18654 (24.75%) · Branches 2856/12167 (23.47%) · Functions 1555/5497 (28.28%) · Lines 4458/17692 (25.19%) — tests 1121: 1032 pass / 89 fail (+9 pass, −9 fail) · 300 files fully covered
Did: greened telematic/settings (3), order/form/route (3) and order/form/details (3) — every test in the ledger's red list. All nine were failing for test-side reasons, four distinct ones: telematic/settings renders `CustomField::Yield`, whose load task peeks the store through `currentUser.loadCompany()` (stood in, as in iteration 32); both order/form suites fixture their order as a plain object, but `cannot-write` resolves the permission off the record and needs `makeRecord` + `AbilitiesStub`; order/form/details' third test constructed the component by hand, which this harness rejects ("You must pass both the owner and args to super()") — rewritten as a rendering test that drives the facilitator select, the scheduled-at picker and the service-type select, and now also asserts the driver is cleared and the type is mirrored onto the payload; order/form/route needed `universe.applicationInstance` pointed at the test container, because `route-optimization` builds its engine registry against `universe.getApplicationInstance()`, which only the host's boot sets. Coverage moved: order/form/route 22→36 s, 5→12 b, 6→12 f; order/form/details 2→12 s, 0→3 b, 1→5 f; telematic/settings unchanged at 13/15 (its gap is elsewhere).
Next: the last red suites — order/form/orchestrator-constraints (2), order/form (2), equipment/form (2), then the singles: widget/revenue-trend, service-rate/form, route-list, positions-replay, part/form, order/route-editor, order/kanban. 80 `it renders` scaffolds remain. Partial files worth finishing: order/form/route (36/131 s, 12/66 b — the biggest single win left in a suite that now runs), order/form/details (12/41), work-order/form (25/59), device/details (30/52). Biggest untouched denominators: services/map-adapter/{google,leaflet}.js (1889 and 991), orchestrator-workbench (783).
Notes: a fixture array built with `A([])` is a native array under EXTEND_PROTOTYPES, and `Array.prototype.map` does not consume Ember's array tag — a getter that maps over it goes stale after `pushObject` even though `{{#each}}` re-renders. Production passes a real hasMany, so use `store.createRecord('payload')` rather than a hand-rolled payload when a component reads `payload.waypoints`. `universe.getApplicationInstance()` is undefined in rendering tests; set `this.owner.lookup('service:universe').applicationInstance = this.owner` for any component whose services build container registries.

## 2026-09-04 — iteration 35 (Phase B: the last red form suites)
Statements 4649/18636 (24.94%) · Branches 2881/12159 (23.69%) · Functions 1562/5492 (28.44%) · Lines 4488/17675 (25.39%) — tests 1129: 1046 pass / 83 fail (+14 pass, −6 fail) · 303 files fully covered
Did: greened order/form/orchestrator-constraints, order/form and equipment/form — the last multi-test red suites. Three test-side causes: the two blueprint `it renders` scaffolds rendered their component with no `@resource`, which the templates' `{{fn (mut @resource.type)}}` rejects outright ("You can only pass a path to mut"); two more tests constructed their component by hand; and `cannot-write` again needed `makeRecord` + `AbilitiesStub`. orchestrator-constraints is complete (16/16 s, 16/16 b, 2/2 f) with every setTimeWindow branch driven through a DateTimeInput stand-in — epoch merge, explicit date, null, unparseable, and all three reference-date fallbacks. equipment/form is complete (24/24, 10/10, 6/6), covering both equipable-type spellings, the upload success and failure paths, and the type/status selects. Two dead-code findings, deleted: #73 — `order/form.js` carried five actions (`selectFacilitator`, `selectOrderConfig`, `selectDriver`, `toggleAdhoc`, `toggleProofOfDelivery`) that its own template never wires (the template has no `this.` reference at all, and the yielded hash exposes only components), each a duplicate of a live action in `order/form/details.js`; the class is now the `orderConfigActions` injection and its constructor, and is complete. #74 — an `?? null` in equipment/form's asset-type handler that the selector's two options can never reach.
Next: no multi-test red suites remain. 80 `it renders` scaffolds are the bulk of the 83 failures — `customer/order-form` and `issue/details` are two that now render real content and just need real assertions. Partial files worth finishing: order/form/route (36/131 s, 12/66 b), order/form/details (12/41), work-order/form (25/59), device/details (30/52), telematic/settings (13/15). Biggest untouched denominators: services/map-adapter/{google,leaflet}.js (1889 and 991), orchestrator-workbench (783), customer/create-order-form (652).
Notes: `hbs` is a build-time tag — a template literal with `${...}` inside it fails the build with "placeholders inside a tagged template string are not supported", so per-item stand-ins need one literal template each. ember-ui's `CurrencySelect` and `MoneyInput` both call `currentUser.getOption('whois')`, so any form with a money or currency field needs that on the current-user stub. A suite can pass while its component's coverage does not move — order/form's new tests were green at 2/20 statements, which is what exposed #73.

## 2026-09-04 — iteration 36 (Phase B: order/form/route, and the optimization error nobody saw)
Statements 4757/18636 (25.52%) · Branches 2940/12159 (24.17%) · Functions 1586/5492 (28.87%) · Lines 4591/17675 (25.97%) · 303 files fully covered — tests 1134: 1051 pass / 83 fail (+5 pass, 0 fail change)
Did: took the ledger's named target. order/form/route went 36→115 statements, 12→39 branches, 12→29 functions on an eight-test suite covering the waypoint lifecycle (toggle both ways, add, re-place, edit, remove), the payload places, the preview at one/two/three-plus points, and both optimization tasks with success, no-route and failure results. Two real bugs on one line, each fixed in its own commit: #76 — `optimizeRoute` calls `this.intl.t(...)` in its catch, but the class injected no `intl`, so every optimization failure threw "Cannot read properties of undefined (reading 't')" from inside the catch (ae253756); and the key it asks for, `fleet-ops.operations.orders.index.new.route-error`, is defined nowhere — the string lives at `order.fields.route-error`, beside the `order.fields.route-label` this component's own template already uses (c6d52e7f). Together they meant a failed optimization showed the user nothing and swallowed the engine's error. #75 (OPEN, other package): six fleetops-data models declare `@hasMany('custom-field-value')` but no package defines that model, so materializing it throws; `tests/dummy/app/models/custom-field-value.js` works around it here — the real fix is Ron's call on where the model belongs.
Next: order/form/route still has 16 statements and 27 branches left — `focusPlace` (L32), `sortWaypoints`'s drag callback (L131), `setWaypointCustomer` (L175/180) and the marker builder (L249) are the uncovered functions; `removeWaypoint`'s `length === 1` guard looks unreachable, because the template renders no remove button for index 0 (`{{#unless (eq index 0)}}`), so a click can never take the list below two — worth tracing and recording. Then the other partials: order/form/details (12/41), work-order/form (25/59), device/details (30/52). Biggest untouched denominators: services/map-adapter/{google,leaflet}.js (1889 and 991), orchestrator-workbench (783), customer/create-order-form (652).
Notes: the coverage-summary keys start with `addon/` and have no leading slash, so the brief's `f.includes("/addon/")` ranking snippet matches nothing — use `f.startsWith("addon/")`. `Place.latitude`/`longitude` are computed from `location`, so a fixture sets `location: { type: 'Point', coordinates: [lng, lat] }` and passing latitude directly throws "Cannot override the computed property". `preparePlaceForSave` reads `place.constructor.eachAttribute`, so a place select stand-in must emit a real record. When a selector guess fails twice, probe the DOM (`console.log` reaches the TAP log) instead of guessing a third time — that is what found `{{#unless (eq index 0)}}`.

## 2026-09-04 — iteration 37 (Phase B: finishing order/form/route)
Statements 4766/18630 (25.58%) · Branches 2951/12150 (24.28%) · Functions 1588/5491 (28.92%) · Lines 4600/17671 (26.03%) · 303 files fully covered — tests 1141: 1058 pass / 83 fail (+7 pass, 0 fail change)
Did: took order/form/route from 115/131 to 123/125 statements, 39→49 branches, 29→31 functions on a fifteen-test suite. Seven new tests: the drag reorder (through a DragSortList stand-in that hands `@dragEndAction` a real payload, covering both the no-op drop and a real move), the waypoint customer, the status-driven preview colours, an optimized trip reporting totals under either naming and with no trip at all, a message-less engine rejection, the order-id fallbacks (public_id → id → "new-order"), and the order customer seeding a new waypoint. DEFECTS #77: `focusPlace` had no caller anywhere in `addon/`, and two guards were unreachable through their only callers — `setWaypointPlace`'s index check (the row's select exists only for an existing row) and `removeWaypoint`'s `length === 1` check (the template withholds the remove button from index 0); all three deleted, which is why the denominator fell by six.
Next: order/form/route has 2 statements and 8 branches left, all defensive `??` fallbacks needing contrived inputs — `routeStyles.at(-1)?.weight ?? 4`, the `trip?.distance ?? trip?.totalDistance ?? 0` chain and `addWaypoint(properties = {})`'s default, whose only caller passes `(hash)`. Worth one focused pass to decide dead-vs-coverable. Then the other partials: order/form/details (12/41), work-order/form (25/59), device/details (30/52), telematic/settings (13/15). Biggest untouched denominators: services/map-adapter/{google,leaflet}.js (1889 and 991), orchestrator-workbench (783), customer/create-order-form (652).
Notes: a polymorphic `belongsTo` rejects a plain object — "[object Object] is not a record instantiated by @ember-data/store" — so a customer fixture must be `store.createRecord('customer', ...)`; CustomerModel extends ContactModel and carries `customer_type` as a real attribute. `??` does not treat `''` as nullish, so `new Error('')` reaches `err.message`, not the fallback; a rejection with no `message` property at all is what exercises it. DragSortList only calls `@dragEndAction` from a real drag, so a stand-in that yields the same block and exposes a trigger is the honest way to cover a sort handler.

## 2026-09-04 — iteration 38 (Phase B: order/form/details, and closing out order/form/route)
Statements 4796/18628 (25.74%) · Branches 2962/12140 (24.39%) · Functions 1596/5491 (29.06%) · Lines 4629/17669 (26.19%) · 305 files fully covered — tests 1149: 1066 pass / 83 fail (+8 pass, 0 fail change)
Did: both named targets reached 100%. order/form/details went 12/41 → 41/41 statements, 3/16 → 16/16 branches, 5/11 → 11/11 functions on five new tests: the customer select under all three identifier shapes (uuid, id, neither) and both customer-type arms; the order-type PowerSelect through a stand-in that yields the real block, covering the custom-field load, the manager published to `orderCreation`, the `onCustomFieldsReady` announcement, a load failure and a cleared selection; the driver select with a vehicle, without one and with a rejecting lookup, asserting the map layer calls either way; the ad-hoc and proof-of-delivery toggles with their dependent fields; and the integrated service-type select, whose `@value` no existing stand-in read, which is why `integratedVendorServiceType` had never run. order/form/route finished at 123/123 s, 47/47 b, 32/32 f — the last two gaps were the map adapter's `removeOptions.filter` and `onRouteFound` callbacks, neither of which the map-manager stub ever invoked, plus a success path for `optimizeRouteWithService` (only its failure path had a test). DEFECTS #78 records the traced-dead residue: `addWaypoint`'s and `setOptimizedRoute`'s parameter defaults are deleted (no call site omits either), while three defensive fallbacks keep their behaviour behind an `istanbul ignore` naming what makes each unreachable.
Next: the remaining named partials are work-order/form (25/59 st, 75 missing) and device/details (30/52 st, 62 missing); telematic/settings is down to 6 missing units and is a quick close. After those the grind has no more cheap partials — 438 files still have gaps totalling 26,905 units, and the top of the list is where the work now is: services/map-adapter/google.js (113/1114), services/map-adapter/leaflet.js (13/478), orchestrator-workbench (0/428), customer/create-order-form (5/395), order-config-manager/activity-flow (4/369), controllers/operations/scheduler/index (0/325). The two map adapters alone are 2,880 units and are plain services, so they are unit-testable without rendering — probably the best value per iteration left.
Notes: line numbers in `coverage-final.json` do not match the source for decorated class members — the decorator transform shifts statement, branch and function positions by a line or two, so a profiler printing `src[line - 1]` names the wrong construct and will point at code a test already covers. Identify a gap by kind (which arrow, which `??`, which default) and confirm by re-running the slice. Two adjacent `/* istanbul ignore next */` pragmas on consecutive object properties do not both attach — Babel gives the second to the preceding property as a trailing comment; hoist the value into one `const` and pragma that. ember-ui's Toggle only defers to `@isToggled` when it is a boolean, so a fixture that presets the flag to `false` pins the toggle on and a second click reports `true` again; leaving the field unset lets the toggle own its state. The "fully covered files" count is `covered === total` on all four metrics — `pct` is 0, not 100, for the 184 files that report zero statements.

## 2026-09-04 — iteration 39 (Phase B: work-order/form and telematic/settings)
Statements 4831/18628 (25.93%) · Branches 2981/12128 (24.57%) · Functions 1610/5491 (29.32%) · Lines 4662/17669 (26.38%) · 307 files fully covered — tests 1158: 1075 pass / 83 fail (+9 pass, 0 fail change)
Did: the last two named partials both reached 100%. work-order/form went 25/59 → 59/59 statements, 2/30 → 18/18 branches, 2/15 → 15/15 functions on six new tests: both polymorphic selectors (picking a type reveals its model select, assigns the model, and clears it when the type changes), the constructor's restore path for a saved record — one relationship carrying its name on `constructor.modelName`, the other on the instance, plus a pair of unrecognised model names that leave both selectors empty — and the completion panel, driving all six setters and asserting what reaches `@onCompletionChange`, with a second render proving the form survives having no listener. telematic/settings went 13/15 → 15/15 statements, 15/18 → 18/18 branches, 8/9 → 9/9 functions on three tests: a credential merged over the saved set, a connection with no credentials at all, and the webhook url across all four shapes (plain url, url with an existing query string, no public id, no provider url). DEFECTS #79: six `?? null` defaults deleted — each guards a lookup whose key comes from a module constant in the same file, so none can miss; the two that *can* miss are kept and now have a test.
Next: no named partials remain. 436 files still have gaps totalling 26,825 units and the work is now all in the big files. `services/map-adapter/leaflet.js` (13/478) and `google.js` (113/1114) are the two biggest and are plain services, but note the obstacle before starting: leaflet.js binds `const L = window.leaflet || window.L` **at module load**, and this package depends on `leaflet` without `ember-leaflet`, so nothing sets that global in the test app — every one of the 32 `L.` uses throws unless the global is established before the module is imported, or the adapter is changed to resolve `L` lazily. Decide that first; it is the whole iteration otherwise. `tests/unit/services/map-adapter/{leaflet,google}-test.js` already exist as scaffolds. The other large targets need no such decision: components/orchestrator-workbench (0/428), customer/create-order-form (5/395), order-config-manager/activity-flow (4/369), controllers/operations/scheduler/index (0/325), components/device/details (30/52, the smallest remaining at 62 units).
Notes: two failures this iteration were selector mistakes of the same kind, both found by probing rather than guessing — the telematic form renders the connection's own fields before the provider's, so the first `input` is "Connection Name", not a credential; and work-order/form has a description textarea above the completion panel, so `fillIn('textarea')` never reached the notes field. Ember's `<Input>`/`<Textarea>` apply `...attributes` *before* their own `on` modifiers, so a caller's `{{on "input" (fn this.setX this.x)}}` reads the value from *before* this keystroke; the value only becomes correct on the following `change` event. That is why the completion fields assert after `fillIn` (which fires both) rather than after a bare `input`. A `@tracked` field's initializer is a statement that only runs when the field is first *read* — a stand-in that ignores `@value` leaves those initializers uncovered even though the component is fully constructed.

## 2026-09-04 — iteration 40 (Phase B: unblocking Leaflet, and the map adapter's core)
Statements 4961/18628 (26.63%) · Branches 3109/12128 (25.63%) · Functions 1636/5491 (29.79%) · Lines 4782/17669 (27.06%) · 307 files fully covered — tests 1177: 1094 pass / 83 fail (+19 pass, 0 fail change)
Did: took the decision the last ledger flagged, then spent it. **The blocker was the test app, not the source.** Seven addon modules read Leaflet off `window` and five capture it at module scope; the host console supplies that global in production, but this package's `index.js` funnels only the leaflet *plugin* assets and never the library, so `window.L` was undefined in the dummy app and `L` was permanently undefined in all five. The fix is one line in `ember-cli-build.js` — which by its own comment builds only the dummy app — importing `node_modules/leaflet/dist/leaflet.js` so the vendor bundle sets the global before any addon module evaluates. That gives tests the **real** library rather than a fake, needs no production source change, and the full run confirms zero blast radius: no new failures and none cleared, with the statement gain attributable entirely to the adapter. Recorded as DEFECTS #80. On that unlock, `services/map-adapter/leaflet.js` went 13/478 → 143/478 statements, 6/447 → 130/447 branches, 3/82 → 27/82 functions on a twenty-test suite covering lifecycle (defaults, options, tile layer, the second-call short circuit, destroy with a map that refuses to be removed), the whole viewport surface including every `!this._map` early return and all three `getCenter` fallbacks, markers (icon url vs icon object, popup/tooltip/click wiring, slide vs set, rotation, removal failures) and overlays (all three shapes with Fleet-Ops defaults and raw Leaflet passthrough), plus distance, geojson and tile-layer swapping.
Next: continue `services/map-adapter/leaflet.js` — 707 units left, the sections not yet touched are routing controls (`addRoutingControl`/`removeRoutingControl`/`positionWaypoints`, ~L335-460), layer visibility (`removeLayer`/`showLayer`/`hideLayer`/`isLayerHidden`/`isLayerVisible`, ~L447-565), drawing tools (~L569-828, the largest and the one needing the leaflet-draw namespace stubs the suite already builds), popups, context menus and events. The same unlock also opened four more files that were stuck near zero purely for want of the global: `services/leaflet-layer-visibility-manager.js` (5/169), `services/service-areas.js` (1/172), `services/leaflet-draw-restriction.js` (4/98) and `components/leaflet-draw-control.js` (1/52) — together another 879 units, all now testable. `services/map-adapter/google.js` (113/1114) is the single biggest file and needs the same treatment for `window.google`, but it reads the global inside its methods, so it needs a fake rather than a build change — that is a genuinely different piece of work.
Notes: the plugin namespaces leaflet-draw and leaflet-contextmenu add (`L.Edit`, `L.Control.Draw`, `L.Map.ContextMenu`) must be stubbed as *own properties* on the real Leaflet and removed afterwards — the scaffold's `L.Control = { ...L.Control, Draw: fn }` would have replaced Leaflet's real `Control` and `Map` classes for every later test in the run, since this is now one shared global. Leaflet's zoom and fade animations run on rAF with nothing to settle them against in a unit test, so the fixture map is built with `zoomAnimation`, `fadeAnimation` and `markerZoomAnimation` off and every viewport change lands synchronously; `panTo`/`flyTo`/`fitBounds` additionally take `{ animate: false }`. `_routingControls` looked undeclared in `leaflet.js` but is declared on the parent `MapAdapterInterface` — checked before claiming it.

## 2026-09-04 — iteration 41 (Phase B: the map adapter's routing controls and layer visibility)
Statements 5104/18628 (27.39%) · Branches 3273/12128 (26.98%) · Functions 1653/5491 (30.10%) · Lines 4902/17669 (27.74%) · 307 files fully covered — tests 1192: 1109 pass / 83 fail (+15 pass, 0 fail change)
Did: continued the ledger's named target. `services/map-adapter/leaflet.js` went 143/478 → 285/478 statements, 130/447 → 294/447 branches, 27/82 → 43/82 functions on fifteen more tests, all of them green first time. Routing controls: the styled path (one polyline per entry in `polylineOptions.styles`, with the shared `leafletOptions` merged into each) and the unstyled one (Fleet-Ops route blue at weight 4, a bare `color` used only when there are no polyline options, and polyline options winning when both are given); a route with no coordinates drawing markers only; markers suppressed, overridden through `markerWaypoints`, skipped by a `createMarker` returning `null` or `false`, built from a plain object, rendered as a Fleet-Ops waypoint badge when it returns a `waypointLabel` (both the default 32px geometry and caller overrides, and the route-blue fallback colour), and the bundled pin default with `markerOptions` merged over it; generated ids being unique; and removal by id, by handle, of an unknown id, and of a handle carrying neither markers nor lines. `positionWaypoints`: the lone-waypoint flyTo with its deferred pan, two points allowed to zoom to 15, three or more held at 14, an explicit maxZoom and padding, a real `L.LatLngBounds`, the `isBounds` escape hatch, and an empty list positioning nothing. `removeLayer` across all four routes (self-removal, map removal, a throwing layer, and a layer the draw group holds). Layer visibility: soft hide/show for a shape with and without fill and for a marker that only knows opacity, the outright hide that defers closing tooltip and popup to the next runloop turn and remembers they were open, the show that restores them, a permanent tooltip reopening regardless, both `try`/`catch` pairs on layers whose overlays throw, and the no-map/no-layer/unknown-layer reads.
Next: `services/map-adapter/leaflet.js` has 193 statements, 153 branches and 39 functions left, all in one contiguous block plus the private helpers — drawing tools (`enableDrawingMode` L569, `disableDrawingMode` L591, `showDrawControl` L597, `hideDrawControl` L612, `setDrawControl` L628, `registerMarker`/`registerPolygon` L639/643, `getContextMenuItems` L647, `showCoordinates` L651, `centerMap` L660, `toggleDrawControl` L666, `editPolygon` L677 with its four callbacks at L725/728/758/777/781, `panBy` L788), popups (L832/839), context menus (L849-878), events (`on`/`off`/`once`, L889-923) and the private `#normalizeEvent` (L993) plus the handful at L1031-1059. The draw work needs the `L.Control.Draw`, `L.Edit` and `L.Map.ContextMenu` stubs the suite's `stubLeafletPlugins` already installs. After that the four files the Leaflet unlock opened are still waiting: `services/leaflet-layer-visibility-manager.js` (5/169), `services/service-areas.js` (1/172), `services/leaflet-draw-restriction.js` (4/98), `components/leaflet-draw-control.js` (1/52). `services/map-adapter/google.js` (113/1114) remains the single biggest file and still needs a `window.google` fake rather than a build change.
Notes: nothing surprised this iteration — no new DEFECTS, no failures to diagnose. `#hideOverlays` runs its body inside `next()`, so a test that asserts the tooltip and popup were closed has to `await settled()` first even though the surrounding test is a unit test; `settled()` works fine under `setupTest`. Spying on `this.adapter.panBy` and `this.adapter.fitBounds` by assignment is what makes `positionWaypoints` assertable without waiting on Leaflet's real animations — the deferred `panBy` is then triggered by firing `moveend` on the map by hand.

## 2026-09-04 — iteration 42 (Phase B: finishing the Leaflet adapter's statements and functions)
Statements 5301/18628 (28.45%) · Branches 3394/12128 (27.98%) · Functions 1694/5491 (30.85%) · Lines 5077/17669 (28.73%) · 307 files fully covered — tests 1212: 1129 pass / 83 fail (+20 pass, 0 fail change)
Did: `services/map-adapter/leaflet.js` reached **478/478 statements and 82/82 functions**, branches 294/447 → 415/447, on twenty more tests. Drawing tools: every leaflet-draw mode started against the map and an unsupported shape starting nothing, the one-shot `onCreate`, the synthetic `draw:drawstop`, and the show/hide/toggle cycle including a config supplied before ember-leaflet hands the control over. `editPolygon` end to end: the four `unsupported` shapes, a layer with no coordinates rejecting, a missing edit handler rejecting after putting the layer back, the happy path writing the edited ring onto the original and returning it as GeoJSON, `draw:editstop` resolving as a cancel with cleanup running exactly once, a `focusBounds` that cannot be fitted plus all three throwing cleanup arms, and an edit whose write-back fails reporting no geometry. Popups, both context-menu paths (the leaflet-contextmenu plugin and the DOM fallback, the latter driven through hover, item click, re-open and close-on-outside-click), and the whole event surface — `on`/`off`/`once`, the `rightclick`→`contextmenu` mapping, and `draw:*` payload normalisation with and without a layer.
Next: `services/map-adapter/leaflet.js` has **32 branches and nothing else** left, scattered across ~30 sites — this is the pattern-sweep case §4 describes, not more file-walking. They are defensive `??`/optional-chain arms at roughly L27, 125, 312, 313, 354, 372, 379, 474, 490, 495, 500, 505, 535, 560, 597, 600, 605, 615, 616, 674, 694, 698, 717, 750, 762, 799, 896, 906, 923, 955, 984; profile them for repeated shapes and expect a real fraction to be traced-dead. Then the four files the Leaflet unlock opened and nothing has touched yet: `services/leaflet-layer-visibility-manager.js` (5/169), `services/service-areas.js` (1/172), `services/leaflet-draw-restriction.js` (4/98), `components/leaflet-draw-control.js` (1/52) — 879 units. `services/map-adapter/google.js` (113/1114) is still the biggest single file and still needs a `window.google` fake.
Notes: three of my assertions were wrong on the first pass and none of them was a product bug. `createGeoJsonFromLayer` returns a bare GeoJSON **geometry** (`new Polygon(coords)`), not a Feature — assert `geoJson.type`, never `geoJson.geometry.type`. Leaflet's `Evented.fire` builds the payload as `extend({}, data, { type, target, sourceTarget })`, so a handler never receives the object the caller passed: `deepEqual` against it always fails, and `type` is overwritten with the event name. That last one matters for `#normalizeDrawEvent`'s `layerType ?? payload.type` — the fallback can only ever yield the event name, so an event missing `layerType` is classified as `draw:created`. leaflet-draw always sends `layerType`, so this is belt-and-braces rather than a bug; it is asserted for what it actually does.

## 2026-09-04 — iteration 43 (Phase B: the Leaflet adapter's branch residue — file complete)
Statements 5297/18624 (28.44%) · Branches 3411/12114 (28.15%) · Functions 1694/5491 (30.85%) · Lines 5073/17665 (28.71%) · 308 files fully covered — tests 1227: 1144 pass / 83 fail (+15 pass, 0 fail change)
Did: **`services/map-adapter/leaflet.js` is at 100% on all four — 474/474 statements, 433/433 branches, 82/82 functions across seventy tests.** The addon's second-largest file is done. This was the pattern sweep §4 describes: the 32 remaining branches were profiled first, then split into what a test could reach and what nothing could. Fifteen tests closed the reachable two-thirds — `ensureInteractive()` with no argument, an overlay drawn with no options at all, a styled route with no shared leaflet options, a `createMarker` returning a truthy non-object, a layer that can neither restyle nor fade, a layer with nothing on the page, a tooltip that was closed and is not permanent, a layer hidden by `display` alone, a draw control shown twice and one with no container, a toggle with no stored config, an `editPolygon` proxy taking the paint defaults, an edit producing no ring, two handlers on one event, `off` for an event never subscribed, a one-shot plain listener, and a first tile layer. DEFECTS #81 covers the rest: the private `#hideOverlays(layer, remember = true)` switch is **deleted** (one call site, always `true`), and six arms carry a traced `istanbul ignore` — `if (polyline)` after the method has already returned for a missing map, the `?? []` tail behind a length guard, both `if (wasVisible)` arms (the `showLayer` two lines above always clears `__hidden`), `?? null` after Leaflet stamps `type` on every payload, and the module-scope `window.leaflet || window.L` where one operand is settled before any test can act.
Next: the four files the Leaflet unlock opened and nothing has touched — `services/leaflet-layer-visibility-manager.js` (5/169 st, 351 units), `services/service-areas.js` (1/172, 268), `services/leaflet-draw-restriction.js` (4/98, 170), `components/leaflet-draw-control.js` (1/52, 90) — 879 units between them, all plain services or a thin component, and all testable now for the same reason the adapter was. `components/leaflet-tracking-marker.js` (51/124, 120) is a fifth in the same family. After those, `services/map-adapter/google.js` (113/1114) is the biggest file left and needs a `window.google` fake rather than a build change; then `orchestrator-workbench` (0/428), `customer/create-order-form` (5/395), `order-config-manager/activity-flow` (4/369), `controllers/operations/scheduler/index` (0/325).
Notes: totals moved oddly and honestly — statements read 5301 → 5297 against a denominator that fell 18628 → 18624, because deleting the `remember` guard removed four statements that were already covered. Branches gained 17 covered against 14 fewer total. A real `L.polygon` inherits Leaflet's style defaults through its options **prototype**, so `delete layer.options.color` does not make the key absent and never reaches a `|| '#3388ff'` fallback; a plain object layer carrying `options: {}` and a `getLatLngs` is what actually exercises those arms. `istanbul ignore else` is the right pragma for a one-armed `if` whose false path is unreachable — `ignore next` on the same statement would drop the covered arm from the denominator too.

## 2026-09-04 — iteration 44 (Phase B: the layer visibility manager, and two findings)
Statements 5446/18609 (29.26%) · Branches 3563/12099 (29.44%) · Functions 1715/5491 (31.23%) · Lines 5195/17652 (29.43%) · 309 files fully covered — tests 1240: 1157 pass / 83 fail (+13 pass, 0 fail change)
Did: **`services/leaflet-layer-visibility-manager.js` is at 100% on all four — 154/154 statements, 152/152 branches, 21/21 functions** on fourteen tests, driving a real Leaflet map through a stubbed `leafletMapManager` (that service is the seam for the no-map cases). Covered: pane creation and reuse with a custom z-index, `assignPane` including a layer with no options and a `redraw` that throws, the category show/hide/toggle cycle and its no-pane and no-map answers, registration by category and by id and the `hidden: true` shortcut, `showAll`/`hideAll` in both soft and hard modes, single-layer soft hide/show for filled shapes, unfilled shapes, opacity-only markers and layers that can do neither, the hard hide with its re-add hook (fired, re-fired, and with the layer shown in between), element resolution through `getElement`/`_path`/`_icon`, tooltip and popup close-and-restore with the permanent-tooltip case and both throwing sides, and the full `isLayerHidden` ladder — flag, element, pane, renderer pane, `includePaneState: false`, and no map. Two findings, each earned before writing it down. **#82 (fixed, own commit ce0e4924):** `assignPane` assigned `ensurePane`'s whole `{ pane, paneName }` pair to `layer.options.pane`; Leaflet's `getPane` returns a non-string unchanged, so the object landed where a DOM parent belongs and the service's own `#getPaneForLayer` then read `undefined` for its display, meaning the category check could never report such a layer hidden. Latent — nothing outside the service calls it. **#83 (NEEDS DECISION):** `services/leaflet-draw-restriction.js` has no consumer anywhere in `addon/`, and reads `this.map` and `this.notifications` which it never declares or injects — so its guards cannot bind and its six user-facing warnings are all no-ops. Wire it up or delete it is Ron's call; I did not spend the iteration covering an inert service.
Dead code removed in the same pass: `#getOrCreateRendererForPane` was a `#private` method with no in-class caller — unreachable by construction, not merely untested — so it and its `#renderersByPane` map are deleted, along with the never-touched `#paneState` and the module-scope `const L` the method was the only user of. Both `if (!set) continue;` guards in `showAll`/`hideAll` are deleted (the key came from `#byCategory.keys()`, so `get` cannot miss), as is `#hideOverlays`'s `remember = true` default (all three call sites pass it). One `istanbul ignore` remains, on the `overlayPane` fallback that Leaflet's `_initPanes` guarantees.
Next: `services/map-adapter/google.js` (113/1114) is now clearly the biggest single target and needs a `window.google` fake — it reads the global **inside** its methods (unlike the Leaflet adapter, which captured it at module scope), so a per-test fake works and no build change is needed. Then `components/orchestrator-workbench.js` (0/428), `components/customer/create-order-form.js` (5/395), `components/order-config-manager/activity-flow.js` (4/369), `controllers/operations/scheduler/index.js` (0/325). Still parked behind their own obstacles: `components/leaflet-draw-control.js` (1/52) extends `ember-leaflet/components/base-layer` and **ember-leaflet is not installed in this package at all** (`require.resolve` fails; ten addon modules import it) — worth its own investigation before anyone spends an iteration on it; and `services/leaflet-draw-restriction.js` (4/98) is parked behind #83.
Notes: the near-duplicate `#hideOverlays`/`#showOverlays` pair here and in the map adapter behave identically, so the adapter's test shape ports over directly — `await settled()` after any hard hide, because the body runs inside `next()`.

## 2026-09-04 — iteration 45 (Phase B: the Google adapter's viewport and markers)
Statements 5673/18609 (30.48%) · Branches 3750/12099 (30.99%) · Functions 1737/5491 (31.63%) · Lines 5402/17652 (30.60%) · 309 files fully covered — tests 1255: 1172 pass / 83 fail (+15 pass, 0 fail change) — **statements past 30% for the first time**
Did: started `services/map-adapter/google.js`, the largest file in the addon at 2,281 lines. It went 113/1114 → 340/1114 statements, 41/791 → 233/791 branches, 6/148 → 27/148 functions on fifteen new tests. The `window.google` fake was the whole unlock and it is cheap, exactly as predicted: the adapter reads the global **inside** each method, so extending the suite's existing `installGoogleMapsStub` with `LatLng`, `LatLngBounds`, `Marker`, `Size`, `Point`, an `AdvancedMarkerElement` and `event.addListenerOnce` was enough — no build change, no module-scope problem. Covered the whole Viewport section (every no-map early return, centre with and without zoom, pan, zoom in/out, `getCenter`/`getBounds` reading through Google's function-style accessors and their empty answers, `flyTo`'s immediate pan and deferred zoom, and `fitBounds` across all four input shapes — a `LatLngBounds`, a corner pair, loose `{lat,lng}` points, arbitrarily nested arrays — plus padding defaults and the `maxZoom` ceiling applied on the `idle` listener, in both the over- and under-ceiling cases). Then the whole Markers section: the classic path with waypoint badge, icon url and size defaults, click and right-click wiring; the advanced path building its own wrapper/img content, labelling it, rotating it, and wiring click, right-click and tooltip; `updateMarkerPosition` outright, at zero duration, animated to completion through `requestAnimationFrame`, cancelling a still-running animation, and jumping when the marker has no position to ease from or refuses `setPosition`; rotation; and removal with animation cancel, tooltip cleanup and both detach styles.
Next: keep going on `services/map-adapter/google.js` — 774 statements, 558 branches and 121 functions still to go, and the sections are laid out at Overlays (L543-765), Layer Visibility (L766-804), Drawing Tools (L805-857), Popups/Info Windows (L858-878), Context Menus (L879-909), Events (L910-965), Utilities (L966-1140) and Private (L1141-2281, the largest block, holding `#attachMarkerTooltip`, `#captureOverlayState`, `#normalizeMapEvent` and the classic-marker tooltip). The stub is now rich enough that the next section should need only small additions to it. After this file: `components/orchestrator-workbench.js` (0/428), `components/customer/create-order-form.js` (5/395), `components/order-config-manager/activity-flow.js` (4/369), `controllers/operations/scheduler/index.js` (0/325).
Notes: nothing surprising. One self-inflicted failure — `waitUntil(() => calls.at(-1).lat === x)` throws on the first poll when the array is still empty, so poll with `at(-1)?.lat`. `flyTo` schedules its zoom on a real `setTimeout`, so the test passes `{ delay: 0 }` and polls with `waitUntil` rather than sleeping. `_supportsAdvancedMarkers` is a plain field set by `initializeMap` from the map id, so a test can set it directly to choose between the classic and advanced marker paths without booting a map.

## 2026-09-04 — iteration 46 (Phase B: the Google adapter's overlays, routing and layer visibility)
Statements 5840/18609 (31.38%) · Branches 3936/12099 (32.53%) · Functions 1761/5491 (32.07%) · Lines 5545/17652 (31.41%) · 309 files fully covered — tests 1268: 1185 pass / 83 fail (+13 pass, 0 fail change)
Did: continued `services/map-adapter/google.js`, which went 340/1114 → 507/1114 statements, 233/791 → 419/791 branches, 27/148 → 51/148 functions on thirteen more tests, all green first time. Overlays: the three shapes with their Fleet-Ops defaults and every caller override, the too-few-points rejections for both polygons and lines, a polygon accepting one flat ring, several nested rings, or a mixed list where a ring too short to close is dropped, the click and right-click that record `_selectedOverlay`, and removal with tooltip cleanup. Routing controls: one polyline per style, the unstyled default at route blue with the bare-colour and polyline-option precedence, a route with no coordinates, unique generated ids, markers suppressed, overridden or skipped by a `createMarker` returning null or false, and removal by id, by handle, of an unknown id and of an empty handle. `positionWaypoints` across the lone-point flyTo with its deferred pan, the two- and three-point zoom ceilings, declared bounds, and an empty list. `removeLayer` across all three detach styles with draft and selection bookkeeping, and the whole Layer Visibility section including a layer carrying a label marker, which moves with it, and the hover tooltip being told to hide.
Next: `services/map-adapter/google.js` still has 607 statements, 372 branches and 97 functions. The remaining sections are Drawing Tools (L805-857), Popups/Info Windows (L858-878), Context Menus (L879-909), Events (L910-965), Utilities (L966-1140), and the Private block (L1141-2281). **The single biggest thing left in it is the polygon-label subsystem** — `#createOverlayLabel` → `#createOverlayLabelDomOverlay` (which builds a `google.maps.OverlayView` with its own `onAdd`/`draw`/`onRemove`), `#attachOverlayLabelHover` (cursor tracking against `map.getDiv().getBoundingClientRect()`), `#getPolygonLabelPosition`, `#extractPolygonPaths` and `#syncOverlayPresentation`'s label branches. That is a coherent group of its own and needs `OverlayView` plus a `getDiv` on the map stub; this iteration deliberately avoided setting `tooltip` on any overlay so the label paths stayed out of scope. After this file: `components/orchestrator-workbench.js` (0/428), `components/customer/create-order-form.js` (5/395), `components/order-config-manager/activity-flow.js` (4/369), `controllers/operations/scheduler/index.js` (0/325).
Notes: nothing surprised this iteration and no new DEFECTS. Google's `Polygon`, `Polyline` and `Circle` share the surface the adapter uses, so one `StubOverlay` class registered under all three names is enough. `addRoutingControl` is `async` here (it awaits `addMarker`), unlike the Leaflet adapter's synchronous one — every call in a test needs awaiting or the handle comes back as a promise.

## 2026-09-04 — iteration 47 (Phase B: the Google adapter's popups, menus, events and utilities)
Statements 5966/18609 (32.05%) · Branches 4009/12099 (33.13%) · Functions 1788/5491 (32.56%) · Lines 5661/17652 (32.07%) · 309 files fully covered — tests 1282: 1199 pass / 83 fail (+14 pass, 0 fail change)
Did: `services/map-adapter/google.js` went 507/1114 → 633/1114 statements, 419/791 → 492/791 branches, 51/148 → 78/148 functions on fourteen more tests. Drawing modes (every shape mapped to its `OverlayType`, an unsupported shape clearing the mode, the create handler armed and cleared, and the no-manager early return), `disableDrawingMode` firing its synthetic `draw:drawstop` with and without a manager, and a listener that throws not stopping the others hearing the event. Popups opening on the map with string or element content and closing by id. Context menus registered, read back and removed with their rendered element, and the DOM renderer driven end to end — the point-less and item-less rejections, a `latlng` exposed as numbers or as functions, an unusable point, the menu placed at the pointer or at the origin, items and separators counted, an item click handing the action the point and closing the menu, and a second menu replacing the first. The whole Events section: subscription under Google's own names (`moveend`→`idle`, `zoomend`→`zoom_changed`, `load`→`tilesloaded`, `contextmenu`→`rightclick`), payload normalisation for an event with a point, an empty event and one without a point, draw listeners held on the adapter rather than the map, unsubscription releasing the Google listener, and one-shot listeners for both kinds. Utilities: `distanceBetween` through the geometry library, geojson layers added, styled and removed, and the custom tile layer built from its url template, named and inserted on top, with a second layer clearing the first.
Next: `services/map-adapter/google.js` has 481 statements, 299 branches and 70 functions left, now concentrated in two blocks. **The draw toolbar** — `showDrawControl`/`hideDrawControl` and their private helpers `#ensureDrawToolbar`, `#applyDrawToolbarConfig`, `#restorePendingDeletedDrafts`, `#getToolbarTopOffset` (which measures `#map-topbar-container`) — and **the polygon-label subsystem**, still untouched: `#createOverlayLabel` → `#createOverlayLabelDomOverlay` (a `google.maps.OverlayView` with its own `onAdd`/`draw`/`onRemove`), `#attachOverlayLabelHover` (cursor tracking against `map.getDiv().getBoundingClientRect()`), `#getPolygonLabelPosition`, `#extractPolygonPaths` and `#syncOverlayPresentation`'s label branches, plus `#captureOverlayState` and the classic-marker tooltip. Either is a coherent group; the label one needs `OverlayView` and a `getDiv` on the map stub. After this file: `components/orchestrator-workbench.js` (0/428), `components/customer/create-order-form.js` (5/395), `components/order-config-manager/activity-flow.js` (4/369), `controllers/operations/scheduler/index.js` (0/325).
Notes: two self-inflicted failures, both instructive. `#renderContextMenu` calls `closeContextMenu()` itself, so a test that spies by monkey-patching that method counts two calls and reads as a bug that is not there — the honest version drives the real renderer and asserts the DOM it produces, which is both simpler and stronger. And a `python` string replacement against a test file that `eslint --fix` has already reformatted will silently not match: re-read the file before the second edit rather than assuming the first shape survived.

## 2026-09-04 — iteration 48 (Phase B: the Google adapter's draw toolbar)
Statements 6042/18609 (32.46%) · Branches 4035/12099 (33.34%) · Functions 1795/5491 (32.68%) · Lines 5735/17652 (32.48%) · 309 files fully covered — tests 1289: 1206 pass / 83 fail (+7 pass, 0 fail change)
Did: took the draw-toolbar block. `services/map-adapter/google.js` went 633/1114 → 709/1114 statements, 492/791 → 518/791 branches, 78/148 → 85/148 functions on seven tests. The toolbar built once into the map's own element and not rebuilt on a second show, with the config remembered even when it arrives before the map exists; the three shape buttons rendered by default, narrowed by a `tools` config, each labelled for screen readers, and starting their drawing mode on click; the edit and delete buttons appearing only when allowed, with the action section hidden when neither is; a `defaultMode` starting a shape straight away; `hideDrawControl` hiding the toolbar, detaching the action bar, putting back a draft that a delete was holding, and switching drawing off; hiding a control that was never shown; and the top offset tracking `#map-topbar-container`.
Next: `services/map-adapter/google.js` has 405 statements, 273 branches and 63 functions left, and what remains is essentially one subsystem plus its neighbours. **The polygon-label and tooltip block**: `#createOverlayLabel` → `#createOverlayLabelDomOverlay` (builds a `google.maps.OverlayView` with its own `onAdd`/`draw`/`onRemove` — the stub needs an `OverlayView` class exposing `getPanes` and `getProjection`), `#attachOverlayLabelHover` (tracks the cursor against `map.getDiv().getBoundingClientRect()`, which the map stub now has), `#getPolygonLabelPosition`, `#extractPolygonPaths`, `#syncOverlayPresentation`'s label branches, `#attachMarkerTooltip` and `#attachClassicMarkerTooltip`. Alongside it sit the draft-overlay editing paths — `#editSelectedOverlay`, `#deleteSelectedOverlay`, `#showEditActionBar`, `#createActionButton`, `#captureOverlayState`, `#geoJsonFromOverlay` — which are reachable through the toolbar buttons this iteration just wired up, so they are the natural follow-on. After this file: `components/orchestrator-workbench.js` (0/428), `components/customer/create-order-form.js` (5/395), `components/order-config-manager/activity-flow.js` (4/369), `controllers/operations/scheduler/index.js` (0/325).
Notes: one self-inflicted failure, and a harness fact worth keeping. `#ember-testing` is **scaled**, so `getBoundingClientRect().height` on an element inside it returns roughly half the CSS height — a test that sets `height: 60px` and expects an offset computed from 60 will read 30. Derive the expectation from what the browser actually measures (`Math.round(measured + 12)`) and assert separately that it cleared the minimum; that keeps the assertion real without hard-coding a number the harness scales.

## 2026-09-04 — iteration 49 (Phase B: the Google adapter's polygon labels and marker tooltips)
Statements 6191/18609 (33.26%) · Branches 4096/12099 (33.85%) · Functions 1815/5491 (33.05%) · Lines 5880/17652 (33.31%) · 309 files fully covered — tests 1298: 1215 pass / 83 fail (+9 pass, 0 fail change)
Did: took the label/tooltip block. `services/map-adapter/google.js` went 709/1114 → 858/1114 statements, 518/791 → 579/791 branches, 85/148 → 105/148 functions on nine tests. A polygon given a tooltip is labelled at the centroid of its ring; the label overlay draws its container into the map pane at the projected point, removes it on detach and survives a draw after removal; the label leaves its anchor to follow the cursor on hover, tracks it on move, returns to the anchor on mouseout and ignores an event with no pointer; the three hover listeners are released when the overlay goes; a polygon with no tooltip gets no label and none is drawn without a map; showing a layer whose overlay was lost rebuilds the label and re-centres it on the shape *as it is now*; a layer whose path cannot be read falls back to the ring it was built with; and both marker-tooltip attachers — the advanced one on the content element, the classic one on the marker itself.
Also opened the campaign PR: **fleetbase/fleetops#310**, as a **draft**, because the gate is still red on purpose (83 scaffolds, 33% coverage). The body carries the baseline-to-now table, the 21 production fixes, and the three decisions waiting on Ron (#37, #38, #83).
Next: `services/map-adapter/google.js` has 256 statements, 212 branches and 43 functions left. What remains is the **draft-overlay editing block**, reachable through the toolbar buttons wired up in iteration 48: `#editSelectedOverlay`, `#deleteSelectedOverlay`, `#showEditActionBar`, `#createActionButton`, `#captureOverlayState`, `#geoJsonFromOverlay`, `#restoreOverlayState`, and the `overlaycomplete` handler that turns a finished shape into a draft. A test drives it by clicking the edit/delete buttons the toolbar renders after `_selectedOverlay` is set and the layer is in `_draftOverlays`; the overlay stub needs `setEditable`, `getPath`, `getCenter`/`getRadius` and `setPaths`. After this file: `components/orchestrator-workbench.js` (0/428), `components/customer/create-order-form.js` (5/395), `components/order-config-manager/activity-flow.js` (4/369), `controllers/operations/scheduler/index.js` (0/325).
Notes: four of my assertions were wrong first time and the most interesting one was not a bug. **The label overlay is deliberately built detached** — `addPolygon` creates it but never calls `setMap`, and the real caller (`components/map/google-live-map.js`) calls `mapManager.hideLayer(createdPolygon)` on the very next line, so the layer starts hidden and `showLayer` is what attaches the label. Assert the detached creation, then show the layer. A `google.maps.OverlayView` renders nothing until `setMap(map)` runs, so the stub calls the adapter's own `onAdd`/`draw` from `setMap` — that is what makes the container assertions possible. The browser normalises `transform: translate(0, 0)` to `translate(0px, 0px)`. And the classic marker tooltip listens on three events (`mouseover`, `mousemove`, `mouseout`), not two.

## 2026-09-04 — iteration 50 (Phase B: the Google adapter's draft editing and deletion)
Statements 6286/18609 (33.77%) · Branches 4135/12099 (34.17%) · Functions 1829/5491 (33.30%) · Lines 5971/17652 (33.82%) · 309 files fully covered — tests 1306: 1223 pass / 83 fail (+8 pass, 0 fail change)
Did: took the draft-overlay editing block, driving it through the toolbar buttons wired up in iteration 48 rather than reaching for the private methods. `services/map-adapter/google.js` went 858/1114 → 953/1114 statements, 579/791 → 619/791 branches, 105/148 → 119/148 functions on eight tests. Editing: the four refusals (nothing selected, the selection is not a draft, the draft has no `setEditable`, its shape cannot be captured), the happy path opening a Save/Cancel bar and firing `draw:edited` with its own geometry, and Cancel writing the captured ring back. Capture and restore for all three shapes — a polygon by its path, a rectangle by its bounds, a circle by its centre and radius. Deletion: both refusals, a draft held aside and hidden with a Save/Cancel/Clear All bar, Save firing `draw:deleted` per held draft and clearing the selection, Cancel bringing the draft back, and Clear All sweeping every draft into the pending set.
Next: `services/map-adapter/google.js` has 161 statements, 172 branches and 29 functions left. The largest remaining piece is the **`overlaycomplete` handler** (~L1189-1240) — the DrawingManager callback that turns a finished shape into a draft, assigns `__overlayType`, calls `#bindOverlaySelection` and fires `draw:created`; `#bindOverlaySelection` is reachable *only* from there, which is why this iteration could not cover it. Around it sit `#overlayToGeoJson`'s per-shape arms, `applyViewSettings`, `destroyMap`, `getOverlay`/`getMarker` and the remaining `initializeMap` branches (map id, styles, the advanced-marker decision). After this file: `components/orchestrator-workbench.js` (0/428), `components/customer/create-order-form.js` (5/395), `components/order-config-manager/activity-flow.js` (4/369), `controllers/operations/scheduler/index.js` (0/325).
Notes: the delete button's class is `fleetops-google-draw-edit-remove` while its aria-label is `delete` — four tests failed on a selector built from the label. One test I wrote had to go rather than be fixed: it claimed to cover `#bindOverlaySelection`, but that method is called only from the `overlaycomplete` handler, so an overlay made by `addPolygon` can never reach it, and `addPolygon`'s own click/right-click selection was already covered by an earlier test. Deleting the duplicate was the honest move; the real coverage comes with the overlaycomplete work.

## 2026-09-04 — iteration 51 (Phase B: the Google adapter's drawing bridge and lifecycle)
Statements 6327/18612 (33.99%) · Branches 4160/12099 (34.38%) · Functions 1838/5491 (33.47%) · Lines 6010/17655 (34.04%) · 309 files fully covered — tests 1314: 1231 pass / 83 fail (+8 pass, 0 fail change)
Did: took the `overlaycomplete` bridge and the remaining lifecycle branches. `services/map-adapter/google.js` went 953/1114 → 992/1117 statements, 619/791 → 639/791 branches, 119/148 → 126/148 functions on eight tests. A finished polygon becoming a selected draft with a closed ring, announced to both `_drawingOnCreate` and `draw:created`, with drawing switched off after; the draft's own click and right-click selection (which is `#bindOverlaySelection`, reachable only from here — the reason iteration 50 could not cover it) and a shape that cannot listen still being selected; each shape turned into its own geometry — polygon, rectangle as a five-point closed ring, circle, polyline as a LineString feature, marker with no shape, and an unmapped type passed through as it came; the drawing library failing to load leaving the adapter usable; `destroyMap` clearing every register; the advanced-marker decision across no map id, the `FLEETOPS_MAP` placeholder and a real id; re-initialisation tearing the old map down; and `getMarker`/`getOverlay`.
Found and fixed in its own commit — **DEFECTS #84 (6ab09c18)**: `destroyMap` cleared every register except `_geojsonLayers`. `grep -c "_geojsonLayers.clear()"` returned 0 while the sibling `map-adapter/leaflet.js` clears it at line 116, so this adapter was the outlier. The layers kept a reference to the destroyed map and their ids survived into the next one, and since `initializeMap` calls `destroyMap()` first, it accumulated on every re-initialisation. Now each layer is detached with `setMap(null)` and the register cleared, in the shape the method already uses for popups.
Next: `services/map-adapter/google.js` has 125 statements, 152 branches and 22 functions left — no large block remains, so this is the pattern-sweep case: profile the residue for repeated shapes rather than working through it method by method. Known stragglers are `applyViewSettings`' remaining arms, `#loadGoogleMapsApi`, `showCoordinates`/`centerMap`, `invalidateSize`, and the `#attachMarkerTooltip` positioning branches. Expect a real fraction to be traced-dead. After this file: `components/orchestrator-workbench.js` (0/428), `components/customer/create-order-form.js` (5/395), `components/order-config-manager/activity-flow.js` (4/369), `controllers/operations/scheduler/index.js` (0/325).
Notes: four assertions were wrong first time and two of them taught something. `initializeMap` **always calls `destroyMap()` first** — there is no "return the existing map" short circuit here, unlike the Leaflet adapter, so a second call rebuilds and the old overlays go with it. `getMarker`/`getOverlay` answer `null` for an unknown id, not `undefined`. The geojson `Circle` helper approximates the circle and returns a **Feature**, not a bare geometry, so assert `geoJson.type === 'Feature'` and check `geometry.type`. And anything put into `_markers`/`_overlays` by hand needs a `setMap`, because `destroyMap` in `afterEach` now detaches whatever it holds.

## 2026-09-04 — iteration 52 (Phase B: the Google adapter's residue sweep, and two real bugs)
Statements 6368/18612 (34.21%) · Branches 4195/12089 (34.70%) · Functions 1846/5491 (33.61%) · Lines 6047/17655 (34.25%) · 309 files fully covered — tests 1322: 1239 pass / 83 fail (+8 pass, 0 fail change)
Did: the pattern sweep the last ledger called for — profiled the residue by *kind* first, then wrote one test per shape rather than walking methods. `services/map-adapter/google.js` went 992/1117 → 1033/1117 statements, 639/791 → 674/781 branches, 126/148 → 134/148 functions on eight tests: the coordinate readers and map centring across a usable point, an unusable one and no event at all; `invalidateSize`; `applyViewSettings` adding, reusing and detaching the traffic and transit layers and appending caller styles to the Fleet-Ops base set; `toggleDrawControl`; `panBy`; a labelled route marker drawn as a badge on the advanced path; and `#loadGoogleMapsApi` both short-circuiting on an API already present and reporting a script that will not load.
**Two real bugs, each fixed in its own commit.** **#86 (a479293b)** — `toggleDrawControl` decided with `display === 'none' || display === ''`, and those are the *only* two values the toolbar's display ever holds (`''` from `showDrawControl`, `'none'` from the container's own `cssText` and from `hideDrawControl`), so `isHidden` was always true and `hideDrawControl()` was unreachable from the toggle. The button wired through `leaflet-live-map` → `geofence` → `map-manager` opened the drawing toolbar and could never close it. **#85 (NEEDS DECISION)** — `showCoordinates`/`centerMap` read `event?.latlng?.lat ?? event?.latlng?.lat?.()`, which cannot work in any case: if the left is nullish the optional call is nullish too, and if it is a function the `??` returns the function rather than calling it. Google's own `LatLng` exposes functions, so a Google-shaped event would read back as `{ lat: <function> }` and `centerMap` would silently no-op. The only callers (`map/leaflet-live-map.js:523,528`) pass Leaflet events with numeric properties, so nothing is broken today. I deleted only the provably-useless tail — which changes no value in any case — and left the question of whether these should accept Google-shaped events to Ron.
Next: `services/map-adapter/google.js` has 84 statements, 107 branches and 14 functions left. The largest single piece is the **public `editPolygon`** (L1062-1110, distinct from the private `#editSelectedOverlay` already covered) with its own action bar, `finalize` and promise; alongside it the `#attachMarkerTooltip`/`#attachClassicMarkerTooltip` positioning branches (L2244-2279), the context-menu `close` handler (L2055), and scattered `??` defaults in `addPolyline` (L599-601), `addRoutingControl` (L669, L676), `#getConfig` (L748) and `showLayer` (L778). After this file: `components/orchestrator-workbench.js` (0/428), `components/customer/create-order-form.js` (5/395), `components/order-config-manager/activity-flow.js` (4/369), `controllers/operations/scheduler/index.js` (0/325).
Notes: three assertions were wrong first time. Turning a view layer on twice calls `setMap` twice, so the detach is the *third* call, not the second. `buildWaypointMarkerContent` is reached from `#buildRouteMarkerOptions`, not from `addMarker` — a `waypointLabel` passed straight to `addMarker` only means something on the classic path. Profiling the residue by branch *kind* before writing anything is what made the `?? …?.()` shape visible as a repeated pattern rather than four unrelated misses; that is the whole point of the sweep.

## 2026-09-04 — iteration 53 (Phase B: the Google adapter's public editPolygon, tooltips and dead defaults)
Statements 6400/18600 (34.41%) · Branches 4223/12080 (34.96%) · Functions 1855/5488 (33.80%) · Lines 6076/17643 (34.44%) · 309 files fully covered — tests 1329: 1246 pass / 83 fail (+7 pass, 0 fail change)
Did: took the ledger's list. `services/map-adapter/google.js` went 1033/1117 → 1065/1105 statements, 674/781 → 702/772 branches, 134/148 → 143/145 functions on seven tests — note the denominators fell too, because five of the gaps turned out to be unreachable and were deleted rather than covered. The public `editPolygon` (the one `geofence` reaches through `map-manager`, distinct from the private `#editSelectedOverlay`): its three refusals, a focus fitted before the edit opens, Save resolving with the ring as it now stands, Cancel writing the captured ring back, and the `settled` guard proving a second click on the detached bar cannot reopen a promise that has already resolved. The classic marker tooltip's pointer tracking — a move before any hover building nothing, a hover building and offsetting it, a move following the pointer, a move carrying no dom event hiding it, `mouseout`, the cleanup, and a marker never hovered still being safe to leave. The context menu's outside-click `close`: a click inside leaves it open, a click outside removes it and unsubscribes. And a defaults sweep — `applyViewSettings()` with no options, `showContextMenu` with no items, an unstyled polyline taking the Fleet-Ops blue, a padding stating nothing filled in on both sides, and an environment config that is missing or throws leaving the map id unresolved.
Deleted as traced-unreachable, recorded as **DEFECTS #87 and #88**: `#ensureTooltipOverlayView`, a `#private` method whose name appears exactly once in the file — its own declaration — so nothing could call it, which also made `_tooltipOverlayView` permanently `null` and `destroyMap`'s teardown of it a no-op; and four defaults whose callers always supply the value (`buildGoogleMapStyles`' two `= {}`, `#loadGoogleMapsApi`'s and `#applyDrawToolbarConfig`'s `= {}`, and `addRoutingControl`'s `?? []`, which sits below a guard that has already proved `route.waypoints` non-empty).
Next: `services/map-adapter/google.js` has 40 statements, 70 branches and 2 functions left, and they are one coherent block: the **polygon label overlay** (L1493-1740) — `#attachMarkerTooltip` and its html/plain arms, `#buildPolygonLabel`, `#setOverlayLabelMap`, the projection-driven `onAdd`/`draw` path, `#syncOverlayPresentation`, `#extractPolygonPaths` and `#polygonCentroid`. Around it sit `#geoJsonFromOverlay`'s rectangle and circle arms (L1903-1911, reachable by giving a draft `getBounds`/`getCenter` instead of `getPath`), `#normalizeDrawEvent` (L1940-1978), `#normalizeLatLngPoint`'s array and refusal arms (L2066-2079), and `#loadGoogleMapsApi`'s success callback (L1174-1176, the `window[callbackName]` the script tag invokes). After this file: `components/orchestrator-workbench.js` (0/428), `components/customer/create-order-form.js` (5/395), `components/order-config-manager/activity-flow.js` (4/369), `controllers/operations/scheduler/index.js` (0/325).
Notes: one assertion was wrong first time — `#overlayToGeoJson` returns a **bare `Polygon` geometry** for a polygon, not a Feature (only the `Circle` and `POLYLINE` arms return Features), so assert `geoJson.type === 'Polygon'` and read `coordinates` directly. Two things made the dead code findable rather than guessable: a `#private` name can only be referenced inside its own class body, so a single grep hit is proof it is unreachable; and a default operand below a guard that has already tested the same value is dead by construction — `addRoutingControl`'s `?? []` was the second kind.

## 2026-09-04 — iteration 54 (Phase B: the Google adapter's polygon label overlay)
Statements 6403/18589 (34.45%) · Branches 4227/12055 (35.06%) · Functions 1855/5488 (33.80%) · Lines 6072/17637 (34.43%) · 309 files fully covered — tests 1336: 1253 pass / 83 fail (+7 pass, 0 fail change)
Did: took the polygon label overlay block the last ledger named. `services/map-adapter/google.js` went 1065/1105 → 1068/1094 statements and 702/772 → 706/747 branches on seven tests; functions were already at 143/145 and stayed there. A marker tooltip carrying markup rather than text, and a `mousemove` with no coordinates at all falling back to the origin. A layer stamped with `__labelText`/`__labelPaths` **after** it was drawn getting its label when it is next shown — the real path in `components/map/google-live-map.js:168-170`, which stamps a name onto a polygon it fetched with `getOverlay`, and which reaches the label-building branch whenever that polygon was first drawn while the service area still had no name. The three ways a label cannot be built: no remembered ring, an empty one, and no map to draw on. A ring read as plain numbers rather than `LatLng` objects, and a ring of rings centred on its first. The overlay left where it was when the projection is missing or gives no point. Bounds collected through a `null` in a loose list of points. And a polygon hovered after its map has gone.
Note the denominators fell again — statements 1105 → 1094, branches 772 → 747 — because most of this block's residue was unreachable. Deleted and recorded as **DEFECTS #89**: `#normalizeDrawEvent`, a second `#private` method whose name appears exactly once in the file (the same shape as #87), and five `if (!x) return` guards that each duplicate a test the immediate caller has already made. One fallback was *not* deleted — `layer.__labelText ?? layer.__labelMarker.title ?? ''` is unreachable today but is the contract for the external stamping in `google-live-map.js`, so it was hoisted into one `const` behind an `istanbul ignore next` naming that reason.
Next: `services/map-adapter/google.js` has 26 statements, 41 branches and 2 functions left, in four small unrelated pieces. (a) `#geoJsonFromOverlay`'s rectangle and circle arms (L1888-1902), reachable by driving the public `editPolygon` with a draft carrying `getBounds`, or `getCenter`/`getRadius`, instead of `getPath`. (b) `#loadGoogleMapsApi`'s remaining arms (L1159-1176) — a load with no API key, the in-flight `google.maps` short circuit, and the `window[callbackName]` the injected script tag invokes on success. (c) the classic marker path: `#createClassicMarker`'s `waypointColor ?? '#2563eb'`, `#setMarkerPosition`'s `marker.position =` else arm (L2117-2118, an advanced marker has no `setPosition`), and `#attachClassicMarkerTooltip`'s html arm and its `mouseover` carrying no dom event. (d) `#normalizeLatLngPoint`'s object form and its refusal (L2041-2054), reachable through `addPolyline` with `{lat, lng}` points and with a point that is neither. Expect (a)'s `if (!overlay)`, L399's `if (!classicMarker)` and the two remaining `= {}` defaults to be traced-dead like the rest. After this file: `components/orchestrator-workbench.js` (0/428), `components/customer/create-order-form.js` (5/395), `components/order-config-manager/activity-flow.js` (4/369), `controllers/operations/scheduler/index.js` (0/325).
Notes: line coverage moved 34.44% → 34.43% while statements and branches rose. That is not a regression — the pragma'd `const` and the deleted guards took covered lines out of the numerator along with the uncovered ones, and the deletions shrank the denominator faster. Two things were worth learning. A guard is only redundant if the caller's check is on the *same* value: `#attachOverlayLabelHover`'s took three steps to trace, because `labelOverlay` is a return value, not an argument the caller tested. And a python regex spanning a JSDoc block plus a method body with `.*?` matched 837 lines instead of 15 — deleting a method is safer done by walking lines from the declaration to its `    }` than by pattern.

## 2026-09-04 — iteration 55 (Phase B: `map-adapter/google.js` reaches 100%)
Statements 6411/18571 (34.52%) · Branches 4238/12026 (35.24%) · Functions 1857/5488 (33.84%) · Lines 6080/17624 (34.50%) · **310 files fully covered** — tests 1347: 1264 pass / 83 fail (+11 pass, 0 fail change)
Did: finished the file. **`services/map-adapter/google.js` is at 100% on all four metrics — 1076/1076 statements, 718/718 branches, 145/145 functions, 995/995 lines.** It is the addon's largest file and took iterations 48-55. Eleven tests closed the four pieces the last ledger named plus the defensive arms underneath them: a rectangle and a circle draft each saving as their own geometry through the public `editPolygon`; a draft carrying no `__overlayType` announced as a polygon by both the edit and the delete payloads, with the delete payload's `toGeoJSON` called; the API loader warning on a missing key and resolving when the injected script calls `window[callbackName]` back; a classic marker taking the route blue for an unstated waypoint colour and a tooltip carrying markup, plus a hover with no dom event; an advanced marker moved by assignment because it has no `setPosition`, and a frame already dispatched when the cancel lands doing nothing — driven through a hand-stubbed `requestAnimationFrame` so no platform gets a vote on that race; a polyline reading `{lat, lng}` objects and stepping over everything it cannot read; `destroyMap` cancelling a running animation and clearing registered context-menu elements; a route marker keeping a custom shape that names no waypoint; a route whose coordinates cannot make a line; a second handler joining the first under one event; an action bar cleanup leaving alone the bar that replaced it; a mouseleave before any hover; and a remembered ring with no points in it.
Deleted as traced-unreachable, recorded as **DEFECTS #90**: `#drawEvent` — the *third* `#private` method in this file whose name appears exactly once, after #87's `#ensureTooltipOverlayView` and #89's `#normalizeDrawEvent` — and nine redundant arms, each duplicating a decision made a few lines earlier. Denominators fell 1083 → 1076 statements and 732 → 718 branches.
Next: **start `addon/components/orchestrator-workbench.js`** (783 missing: 428 statements, 0 covered). 433 files still have gaps, 23,579 missing between them. After it: `components/customer/create-order-form.js` (652), `components/order-config-manager/activity-flow.js` (622), `controllers/operations/scheduler/index.js` (601), `components/map/leaflet-live-map.js` (563), `components/modals/orchestrator-import.js` (497), `components/map/google-live-map.js` (417), `components/orchestrator/order-pool.js` (412), `services/position-playback.js` (412). This is a change of register: those are Glimmer components needing `setupRenderingTest` and their templates, not a service with a stub harness.
Notes: **the batch-picking snippet in the brief (§4) does not work as written** — it filters `f.includes("/addon/")` and splits on `"/addon/"`, but the keys in `coverage-summary.json` are repo-relative (`addon/components/...`), so it reports "files with gaps: 0" and would look like the campaign was finished. Filter on `f !== "total"` and use the key as-is. Two smaller things: a second `on(event, handler)` for a non-draw event registers its *own* Google listener rather than sharing one, so a test must fire every matching entry in `map.listeners`, not just the first; and `#geoJsonFromOverlay` orders its arms `getPath` → `getBounds && !getCenter` → `getCenter && getRadius` on purpose, because a real `google.maps.Circle` has `getBounds()` too and would otherwise be read as a rectangle.

## 2026-09-04 — iteration 56 (Phase B: standing up the orchestrator workbench)
Statements 6685/18571 (36.00%) · Branches 4366/12026 (36.30%) · Functions 1908/5488 (34.77%) · Lines 6334/17624 (35.94%) · 310 files fully covered — tests 1360: 1277 pass / 83 fail (+13 pass, 0 fail change)
Did: opened `components/orchestrator-workbench.js` — 1080 lines, 428 statements, **0 covered, no test file at all** — with a new rendering harness and thirteen tests. It went 0/428 → **274/428 statements, 13/261 → 127/261 branches, 17/94 → 51/94 functions** in one pass; that is the single largest jump of the campaign so far (+274 statements is the whole of this iteration's global delta). Covered: the constructor's five parallel loads and the shape of each request; every load failing, with the three that notify doing so once each and the two that fall back silently staying silent; the phase-builder and card-fields panels, which are mutually exclusive; both side panels collapsing; a run proposing a plan, grouping it by vehicle, drawing one routing control per vehicle threaded through each order's stops in sequence order, and fitting the map to it; the formatters the plan viewer is handed; a run that places nothing surfacing why and being cleared; a run and a commit the server refuses; committing and reloading; and discarding taking the routes off the map with it.
Notes: the harness is the reusable part. `ember-leaflet` is not installed in this package, so `<LeafletMap>` cannot resolve at all — `registerTemplateOnly(owner, 'leaflet-map', hbs`<div data-test-map></div>`)` as a **non-yielding** stub both fixes that and skips the whole map block, which is 150 of the template's 317 lines and needs `{{div-icon}}` from the same missing addon. The five `Orchestrator::*` panels are stubbed the same way. **A stub that calls the callbacks it is handed is the cheap way to cover a component's formatters**: the plan-viewer stand-in renders `{{@formatDuration 3725}}` and friends, which is exactly what the real one does, so the wiring is what is under test rather than an invention of the harness. Four things cost a re-run and are worth writing down. Order stops live under `order.payload.pickup`/`.dropoff`, not on the order — `_getOrderStops` returns `[]` for anything else, so a fixture with the coordinates one level too high yields an empty plan and no routing controls, with every other assertion still passing. `togglePhaseBuilder` closes the card-fields panel and vice versa, so the two are mutually exclusive and a naive open/open/close sequence asserts the wrong thing. Both `.workbench-panel-toggle` buttons are the last button in their *own* parent, so `:last-of-type` matches both — select the right one by its `border-l`. And `await click()` already awaits `settled()`, including a 200ms `later`, so a following `await settled()` is both redundant and an eslint error (`ember/no-settled-after-test-helper`). Selectors for ember-ui `<Button>`: `button.btn-primary` / `.btn-success` by `@type`, otherwise `button:has(.fa-<icon>)` — note FontAwesome 6 resolves aliases, so `@icon="sync"` renders `fa-arrows-rotate` and `@icon="times"` renders `fa-xmark`. Probing the real DOM with a throwaway failing assertion beat guessing.
Next: continue `components/orchestrator-workbench.js` — 154 statements, 134 branches and 43 functions left. Three clusters. (a) **Selections and drag/drop** (L459-543): the six toggle/clear actions and `onDropOnVehicle`, reachable by making the order-pool and resource-panel stubs invoke the callbacks they are handed, the same trick the plan-viewer stub uses for the formatters. (b) **Phases** (L390-415, L249-298): `onPhasesChange`/`onRunPhases` and a multi-phase run — `_executePhases` looping, `autoCommit` committing between phases, `prior_assignments` carrying forward, a phase overriding an earlier assignment by `order_id`, `assign_drivers` setting `hasDriverPhase`, and the driver-tab-to-vehicle resolution in `_runSinglePhase`. (c) **Map and stops** (L545-568, L624-662, L835-908): `onMapLoad`, `_centerMapOnOrders` for one stop versus many, multi-drop orders through `payload.waypoints`, and `_placeCoords`' four accepted shapes. After this file: `components/customer/create-order-form.js` (652), `components/order-config-manager/activity-flow.js` (622), `controllers/operations/scheduler/index.js` (601).

## 2026-09-04 — iteration 57 (Phase B: the workbench's selections, drag-drop and phases)
Statements 6788/18571 (36.55%) · Branches 4413/12026 (36.70%) · Functions 1941/5488 (35.37%) · Lines 6430/17624 (36.48%) · 310 files fully covered — tests 1373: 1290 pass / 83 fail (+13 pass, 0 fail change)
Did: took clusters (a) and (b) from the last ledger. `components/orchestrator-workbench.js` went 274/428 → **377/428 statements, 127/261 → 174/261 branches, 51/94 → 84/94 functions** on thirteen more tests. Selections: all three toggles on and off again, all three clears, and clearing drivers releasing their vehicles with them; then a selection narrowing what the run is asked to place, including the driver-tab resolution that turns a selected driver into its own `vehicle_id`. Drag and drop: an unassigned order dragged onto a vehicle joining the plan behind what is already there, an order already in the plan being rewritten in place rather than added twice, a drop carrying nothing and a drop naming an order nobody knows both changing nothing, and an assigned order dragged back out. Phases: a list edited without running being kept and counted in the toolbar, two phases running in sequence with the second handed the first's `prior_assignments` and falling back to the built-in engine, `assign_drivers` flipping the plan to show by driver, and `autoCommit` landing a commit between the two runs. Plus the run message dismissed, the import modal's `onImportComplete` reloading the pool, card fields saving and reloading, the left panel collapsing, and both resize handles dragged through their minimum and maximum stops and released.
Notes: two stubs had to stop being template-only. **A template-only stand-in cannot hand the component under test a value the *test* chose** — it only has `@args`. The phase builder needs to pass a phase list back through `@onRunPhases`, so it became a real Glimmer component reading that list off a `service:workbench-test-bed` the test writes to; the same service now carries the fake map instance. The map stub also had to become real: `onMapLoad` destructures `{ target: map }` and immediately calls `map.setView`, so a `(hash target=this.mapInstance)` in a template-only stub yields `undefined` and every test fails at once with `setActiveProvider is not a function` — the failure names the *service* method, not the missing target, which reads like a service stub problem rather than a component one. Drag-and-drop needs a `DataTransfer` stand-in with `getData`/`setData` passed through `triggerEvent(el, 'drop', { dataTransfer })`; the real one only exists for a browser-initiated drag. And `@planByVehicle.0.orders.0.order` is an hbs lint error (`no-obscure-array-access`) — nest `get` helpers instead.
Next: `components/orchestrator-workbench.js` has 51 statements, 87 branches and 10 functions left, and they are now **one cluster: everything only the map block reaches**. `tileSourceUrl` (via `<layers.tile @url=...>`), `getOrderStops` and `waypointIconHtml` (via the marker `{{#each}}`), the multi-drop `payload.waypoints` arm of `_getOrderStops`, `_placeCoords`' remaining accepted shapes, `_centerMapOnOrders` for one stop versus many, and `formatIsoTime`/`formatTimeWindow`. Covering them means making `LeafletMapStub` **yield** a `layers` hash of template-only `tile`/`marker` components (and `marker` yielding `popup`/`tooltip`), plus registering the `div-icon` helper that `ember-leaflet` would otherwise provide — it is not installed here. That is the whole of the next iteration's setup; the assertions after it are small. After this file: `components/customer/create-order-form.js` (652), `components/order-config-manager/activity-flow.js` (622), `controllers/operations/scheduler/index.js` (601).

## 2026-09-04 — iteration 58 (Phase B: the workbench's map block)
Statements 6820/18568 (36.73%) · Branches 4462/12026 (37.10%) · Functions 1948/5485 (35.52%) · Lines 6458/17621 (36.65%) · 310 files fully covered — tests 1383: 1300 pass / 83 fail (+10 pass, 0 fail change)
Did: built the yielding map stand-in the last ledger called for, which unlocked the rest of the file. `components/orchestrator-workbench.js` went 377/428 → **409/425 statements, 174/261 → 223/261 branches, and 84/94 → 91/91 functions — every function in the file is now covered**. Ten tests: unassigned orders pinned as green `P` and red `D` badges with their popups and tooltips; a multi-drop order pinned once per waypoint, sorted by its own `order` field rather than the given one, with a waypoint at 0,0 dropped as not-a-place; `_placeCoords` reading a flat `lat`/`lng`, a numeric pair and direct `latitude`/`longitude`, and refusing three shapes that are not places; a driver with a known location pinned with their vehicle and status while one without is not; several stops fitted as a bounding box and a single stop zoomed to instead; the browser location centring the map when there are no orders, and being refused without taking the workbench down; a plan pinning `A1`/`A2` for a two-stop order and `B` for a one-stop one; and a driver matched to a group by `vehicle_id` when the assignment names none.
Deleted as traced-unreachable, recorded as **DEFECTS #91**: `selectedOrderIdsArray`, `selectedVehicleIdsArray` and `selectedDriverIdsArray`. The grep that looks like it finds callers actually finds the sub-components' *own* identically-named getters (`orchestrator/order-pool.js:285`, `orchestrator/resource-panel.js:92` and `:96`), each deriving from the `Set` the workbench passes down; `orchestrator-workbench.hbs` never mentions the three names.
Notes: the map stub is the reusable piece — `LeafletMapStub` now yields `(hash tile=… marker=…)` and the marker yields `(hash popup=… tooltip=…)`, matching ember-leaflet's own shape, plus `div-icon` / `icon` / `point-to-coordinates` registered as helpers that just hand back their named args. **A template-only component cannot yield other components**, because it has no `this` to hold them — the marker had to become a real class with `popupComponent`/`tooltipComponent` fields. Two assertions were wrong first time and one of them was wrong *about the product*: I asserted that geolocation is ignored once orders have centred the map, but `getUserLocation()` here resolves **before** the orders fetch does, so it centres first and the orders re-centre afterwards. The `_mapCenteredOnOrders` guard only bites when the browser answers late — covering it on purpose meant holding the geolocation promise open and resolving it by hand after render, which is the §7 "cover the path deliberately" rule applied to promise ordering rather than to focus. Also `assert.dom('[data-test-marker-tooltip]')` takes the *first* match in the document, so per-marker assertions must scope through that marker's element.
Next: `components/orchestrator-workbench.js` has 16 statements and 38 branches left, no functions. It is now a **pattern sweep**, not a cluster: `??` defaults with no reachable nullish input (`options.engine`, `phase.orderStatuses`, `group.vehicle?.public_id`, `place.address`, the `routeSummaries[vehicleId] ?? {}`), guard arms whose input the caller has already proved, `formatDuration`/`formatDistance` under their thresholds, `formatIsoTime` on a date the browser refuses, and the `_drawRoutingControls` arms for a route handle with no coordinates and a `fitBounds` that throws. Expect a real fraction to be traced-dead, as in the Google adapter. After this file: `components/customer/create-order-form.js` (652), `components/order-config-manager/activity-flow.js` (622), `controllers/operations/scheduler/index.js` (601), `components/map/leaflet-live-map.js` (563).

## 2026-09-04 — iteration 59 (Phase B: the workbench's branch residue, and a latent formatter bug)
Statements 6826/18566 (36.77%) · Branches 4484/12026 (37.29%) · Functions 1948/5485 (35.52%) · Lines 6460/17619 (36.66%) · 310 files fully covered — tests 1391: 1308 pass / 83 fail (+8 pass, 0 fail change)
Did: the pattern sweep the last ledger called for. `components/orchestrator-workbench.js` went 409/425 → **415/423 statements and 223/261 → 246/261 branches**; functions stayed at 91/91. Eight tests, one per shape rather than one per method: a response missing the key it was asked for (`orders`, `engines`) taken as empty without raising anything; a selected driver with no vehicle of their own resolving nothing; a commit that reports no count still being announced; a route the engine returned with no geometry skipped while the other vehicle's is still drawn and fitted; a plan naming a vehicle nobody has still drawn, tagged `orchestrator:unknown` through the whole `??` chain; a waypoint that is *itself* the place read the same as one wrapping it in `place`, and a place with no address contributing none; a group the run reported no timings for getting an empty summary while one with them carries `900` through; a map that mounts *after* the orders re-centring on them rather than on the default; and the formatters at their edges — under an hour, under a kilometre, zero, a window with only a start, one that starts and ends together, and none at all.
Found while covering — **DEFECTS #92, NEEDS DECISION**: `formatIsoTime` wraps its work in `try { … } catch { return '' }`, and neither call inside can throw. `new Date(x)` returns an Invalid Date object for anything unparseable rather than raising, and `toLocaleTimeString` on an Invalid Date returns the *string* `"Invalid Date"` (only `toISOString`/`toJSON` throw). So the `catch` was unreachable and the `''` it exists to produce can never be returned: an unparseable `time_window_start` shows a dispatcher the words "Invalid Date" instead of an empty cell, and because that string is truthy it also wins `formatTimeWindow`'s `s || e || ''`. I deleted the dead `catch` — behaviour-neutral, it never ran — and left the one-line real fix (`Number.isNaN(d.getTime())`) to Ron, since making the intent work is a behaviour change on a path no test exercises against real data. The test asserts what the code actually does today and points at the defect.
Next: `components/orchestrator-workbench.js` has 8 statements and 15 branches left. Profile them by kind before writing anything — after this sweep the survivors are most likely traced-dead, as they were in the Google adapter: `commitPlan`'s `if (!this.proposedPlan?.length) return`, whose only two callers (the toolbar button under `{{#if this.hasProposedPlan}}` and `_executePhases`' `phase.autoCommit && this.proposedPlan?.length`) have both already proved it; the `@tracked` initialisers for fields nothing reads before writing; and the remaining `?? 0` / `?? false` operands in `_groupByVehicle` and `onDropOnVehicle`. Expect to finish the file next iteration. After it: `components/customer/create-order-form.js` (652), `components/order-config-manager/activity-flow.js` (622), `controllers/operations/scheduler/index.js` (601), `components/map/leaflet-live-map.js` (563).

## 2026-09-04 — iteration 60 (Phase B: the workbench's last residue — branches and functions done, two statements short)
Statements 6820/18554 (36.76%) · Branches 4493/12020 (37.38%) · Functions 1948/5485 (35.52%) · Lines 6454/17608 (36.65%) · 310 files fully covered — tests 1398: 1315 pass / 83 fail (+7 pass, 0 fail change)
Did: **`components/orchestrator-workbench.js` reached 255/255 branches and 91/91 functions** — both metrics complete — and 409/411 statements (99.51%). Seven tests: a run reporting no `assignments` key at all; a drop onto a plan of several leaving the others where they were; the tile layer following the dark theme; a stop on the equator pinned but unable to anchor a route leg (lat 0 is a real place, and `_placeCoords` accepts it because the *pair* is not 0,0, but `if (stop.lat && stop.lng)` then drops it); null island rejected in all four shapes it can arrive in; unnumbered waypoints and unsequenced assignments keeping the order they came in; and a card-fields read that raises before it returns a promise, which the chained `.catch` cannot see.
Deleted as traced-dead: `commitPlan`'s `if (!this.proposedPlan?.length) return` (both callers — the toolbar button under `{{#if this.hasProposedPlan}}` and `_executePhases`' `phase.autoCommit && this.proposedPlan?.length` — have already proved it); `onDropOnVehicle`'s two `?? 0` / `?? []` operands (the handler is passed only to `Orchestrator::PlanViewer`, which renders only under `{{#if this.hasProposedPlan}}`); the `_resizing` field, which is declared and never touched again; and `_draggingOrder` with its three writes, which nothing reads in either the JS or the template.
Not finished, and recorded as **DEFECTS #93 (OPEN)**: two `@tracked` initialisers. With the decorator transform an initialiser is lazily evaluated on the field's first **read**, so any field written before it is read shows as uncovered. Five were identified and pragma'd with the specific writer that beats them (`runOrchestration` for four, `onMapLoad` for `leafletMap`). The last two I could not place to the standard §4 demands: the `statementMap` for this file is offset from the source by about two lines so the profiler names a comment, the neighbours suggest `availableEngines` and `cardFields`, and `cardFields` contradicts the theory by being read on first render. **I did not pragma them on that inference** — a pragma carrying an untrue reason is worse than an uncovered line.
Next: **finish DEFECTS #93 first** — read the *generated* instrumented file rather than mapping positions back onto the source, name the two fields exactly, and pragma or cover each on a real trace. That closes `orchestrator-workbench.js`. Then move on: `components/customer/create-order-form.js` (652 missing), `components/order-config-manager/activity-flow.js` (622), `controllers/operations/scheduler/index.js` (601), `components/map/leaflet-live-map.js` (563). Note the §4 batch-picking snippet in the brief is broken — it filters `f.includes("/addon/")` but the summary keys are repo-relative (`addon/…`), so it reports "files with gaps: 0"; filter on `f !== "total"` instead.

## 2026-09-04 — iteration 61 (Phase B: `orchestrator-workbench.js` reaches 100%)
Statements 6821/18553 (36.76%) · Branches 4494/12020 (37.39%) · Functions 1948/5485 (35.52%) · Lines 6455/17607 (36.66%) · **311 files fully covered** — tests 1398: 1315 pass / 83 fail (0 change either way)
Did: closed **DEFECTS #93** and with it the file. **`components/orchestrator-workbench.js` is at 100% on all four — 410/410 statements, 255/255 branches, 91/91 functions, 375/375 lines.** It is the addon's second-largest component and took iterations 56-61 from 0/428.
How the two holdouts were finally identified: not by reading the map, which places their positions on the `// -- … --` divider comments, but by **bisection** — pragma one candidate, re-run, and watch whether the *uncovered* count falls or only the denominator does. A candidate that drops the denominator by one while leaving two uncovered was a covered statement and is innocent; that ruled out `proposedPlan` in one run and confirmed `availableEngines` in the next.
The two were not the same kind of thing. **`availableEngines`** is genuinely unreachable — its only reader is `Orchestrator::PhaseBuilder`, rendered only inside `{{#if this.showPhaseBuilder}}`, which is closed on first paint, so `loadEngines`' assignment is the first access and the lazy `@tracked` initialiser never evaluates. It now carries a pragma naming that gate. **`cardFields` was my own harness**: the real `Orchestrator::OrderPool` reads `@cardFields`, but the stand-in for it did not, and **Ember argument references are lazy — the getter runs only when a child actually consumes the argument**. The stub now renders `{{@cardFields.standard.length}}` as the real component does, and a test asserts the settings endpoint's fields arrive.
Notes: **a stub that accepts an argument without consuming it silently withholds coverage from the parent, and it reads exactly like unreachable code.** That is the trap to carry into the next component — the panels stubbed in this file take twenty-odd arguments between them, and any of them could hide a parent getter the same way. When a getter looks unreachable, check the stub before believing it. Also converted this file's `─` box-drawing dividers to ASCII while chasing this; it realigned the HTML report's source view (worth knowing — `coverage/addon/**/*.html` renders istanbul's own view of the source and is far better than mapping positions by hand) but it was not the cause.
Next: **start `components/customer/create-order-form.js`** — 652 missing (5/395 statements). Then `components/order-config-manager/activity-flow.js` (622), `controllers/operations/scheduler/index.js` (601), `components/map/leaflet-live-map.js` (563), `components/modals/orchestrator-import.js` (497). Reuse the workbench harness shape: stub the panels with `registerTemplateOnly`, make each stub *consume* the arguments it is handed and invoke the callbacks it is given, and put anything the test must choose behind a `service:workbench-test-bed`-style test-bed service. Remember the §4 batch-picking snippet in the brief is broken — filter on `f !== "total"`, not `f.includes("/addon/")`.

## 2026-09-04 — iteration 62 (Phase B: opening `customer/create-order-form.js`, and the first scaffold retired)
Statements 6872/18553 (37.04%) · Branches 4506/12020 (37.49%) · Functions 1957/5485 (35.68%) · Lines 6505/17607 (36.95%) · 311 files fully covered — tests 1403: 1321 pass / **82 fail (was 83)**
Did: stood up the harness for `components/customer/create-order-form.js` (962 lines) and took it 5/395 → **56/395 statements, 7/197 → 13/197 branches, 6/67 → 10/67 functions** on six tests. **This is the first of the 83 blueprint scaffolds to be retired** — `create-order-form-test.js` existed but was the generated `it renders` stub, and it failed because the component reads `this.order.customer` in its constructor while the stub rendered it with no arguments. Replacing it both greened a failure and opened the file, so DEFECTS #4's count is now 82. Covered: the enabled-order-configs filter picking only what the customer is entitled to and applying the first to the order (`order_config`, `order_config_uuid`, and `type` from the config's key) with its custom fields loaded; both settings reads failing and being reported once each; the payments config; and an order carrying its own customer versus falling back to the session.
Notes: three things this component needs that the workbench did not. **`ember-file-upload` and `ember-model-select` are not installed in this package** (the same shape as `ember-leaflet`), so `FileDropzone`, `FileUpload` and their friends need `registerTemplateOnly` stand-ins before the template will render at all — `ModelSelect` and `ContentPanel` and `Overlay` do resolve, they come from `ember-ui`. **`order.customer` is a polymorphic `belongsTo`, so it rejects a plain object** with "is not a record instantiated by @ember-data/store"; leave it unset and let the session stub supply the customer, or create a real `contact` record. And the component calls `this.order.set(...)`, so the order itself has to be `store.createRecord('order')` rather than a POJO — with the real store, `peekRecord('order-config', id)` then needs the configs actually created, which is what the `givenOrderConfigs` helper in the harness does (it also replaces `store.findAll`).
Next: continue `components/customer/create-order-form.js` — 339 statements, 184 branches and 57 functions left. Three clusters, in this order. (a) **The waypoint and entity editing surface** (L441-560): `createPlace`, `editPlace`, `setPayloadPlace`, `sortWaypoints`, `addWaypoint`, `setWaypointPlace`, `removeWaypoint`, `clearWaypoints`, `addEntity`, `removeEntity`, `setEntityDestionation`, `editEntity`, plus `toggleMultiDropOrder`, which moves pickup/dropoff into waypoints and back. (b) **Quoting and creation** (L150-267): `createOrder`, `getQuotes`, `isValid`, `_getSerializedPayload` and the serialisers under it. (c) **The route preview** (L352-440) and the **checkout session** (L730-960), the latter reachable by putting `checkout_session_id` and `service_quote` into the `urlSearchParams` stand-in the harness already drives. After this file: `components/order-config-manager/activity-flow.js` (622 missing), `controllers/operations/scheduler/index.js` (601), `components/map/leaflet-live-map.js` (563).

## 2026-09-04 — iteration 63 (Phase B: the customer order form's abilities gate and entity rows)
Statements 6891/18553 (37.14%) · Branches 4510/12020 (37.52%) · Functions 1959/5485 (35.72%) · Lines 6524/17607 (37.05%) · 311 files fully covered — tests 1404: 1322 pass / 82 fail (0 change either way)
Did: **a thin iteration, and worth saying so plainly** — `components/customer/create-order-form.js` went 56/395 → **75/395 statements, 13/197 → 17/197 branches, 10/67 → 12/67 functions** on one new test. Most of the time went on harness discovery rather than assertions, and two of the three things I set out to cover are deferred. What landed: an `abilities` stub, without which **every control on this form is disabled** (`@disabled={{cannot "fleet-ops create order"}}` gates the entity buttons, the notes textarea and the dropzone), and a `DragSortList` stand-in that yields `|item index|` and reports a reorder through `@dragEndAction`, plus a `ModelSelect` stand-in that reports the choice the test put on a `service:order-form-test-bed`. The one assertion is that adding an item renders one more item row.
What did not land, and why. **`setPayloadPlace` cannot be driven without a real Leaflet map.** It calls `previewDraftOrderRoute`, which builds a `RoutingControl` from `@fleetbase/leaflet-routing-machine` and does `control.addTo(this.map)`; with no `@map` the library throws `Cannot read properties of undefined (reading 'getSize')` from inside its own `onAdd`. The customer portal passes a live map, so the honest fix is to give the harness one — `L.map(element)` on an element with a size, Leaflet being on the global already (`ember-cli-build.js` imports it). That is the next iteration's setup and it unlocks a large block: `setPayloadPlace`, `updatePayloadCoordinates`, `createPlaceArrayFromPayload`, `previewDraftOrderRoute`, `removeRoutingControlPreview` and `getRoute`. The multi-drop toggle is also still unprobed — ember-ui's `Toggle` does not render a `<button>` or a bare checkbox, so its selector needs finding before `toggleMultiDropOrder` and the whole waypoint block can be driven.
Notes: three model facts cost a run each. **`place.latitude`/`longitude` are computed properties** — setting them on `createRecord('place', …)` asserts "Cannot override the computed property"; set `location` instead, or leave them off. The **entity destination select only renders in multi-drop mode**, so counting `[data-test-model-select]` is not how you tell that a row was added — count the "Edit Item" buttons. And a plain helper function has no template scope: `hbs` inside `async function renderWithConfig(test)` cannot see `test`, and fails with "Attempted to resolve a value in a strict mode template" — keep `render` in the test body.
Next: **give the harness a real Leaflet map and take the route-preview block** — create a sized element inside `#ember-testing`, `L.map(el).setView([1.3, 103.8], 12)`, pass it as `@map`, and then drive `setPayloadPlace` through the three `place` selects the template already renders (pickup, dropoff, return). Then probe ember-ui's `Toggle` markup once and use it for `toggleMultiDropOrder`, which opens the waypoint list, `addWaypoint`, `setWaypointPlace`, `sortWaypoints`, `removeWaypoint` and `clearWaypoints`. After that: `createOrder`/`getQuotes`/`isValid` (L150-267) and the checkout session (L730-960). After this file: `components/order-config-manager/activity-flow.js` (622 missing), `controllers/operations/scheduler/index.js` (601), `components/map/leaflet-live-map.js` (563).

## 2026-09-04 — iteration 64 (Phase B: a real Leaflet map unlocks the order form's route preview, and a live bug)
Statements 6945/18553 (37.43%) · Branches 4530/12020 (37.69%) · Functions 1972/5485 (35.95%) · Lines 6576/17607 (37.35%) · 311 files fully covered — tests 1405: 1323 pass / 82 fail (0 change either way)
Did: gave the harness a **real Leaflet map** — a sized `<div>` inside `#ember-testing`, `window.L.map(el).setView([1.3, 103.8], 12)`, passed as `@map` and removed in `afterEach` — which was exactly the unlock the last ledger predicted. `components/customer/create-order-form.js` went 75/395 → **124/395 statements, 17/197 → 38/197 branches, 12/67 → 23/67 functions** on one test. Choosing a pickup and then a dropoff through the two `place` selects now runs `setPayloadPlace`, `updatePayloadCoordinates`, `createPlaceArrayFromPayload`, `previewDraftOrderRoute`, `removeRoutingControlPreview` and `getRoute`, and attaches a real routing control to the map.
Found while covering — **DEFECTS #94 (OPEN), a live bug**: `previewDraftOrderRoute` computes `canPreviewRoute` and, when there is nothing to route, warns the user — **and then carries on**. There is no `return`. It builds the `RoutingControl` and calls `this.map.flyToBounds([])`, and Leaflet's `getBoundsZoom` → `getNorthWest` → `getNorth` reads `.lat` off an undefined corner and throws. The path is live: the template's "remove address" links are `{{on "click" (fn this.setPayloadPlace "pickup" null)}}` (lines 116 and 152), so a customer clearing the only address they had entered takes the exception and the preview stops working for the rest of the session. The fix is a one-line `return`, which is what the `canPreviewRoute` name implies was intended — left for Ron rather than folded into a coverage commit.
Notes: **the defect cannot currently be pinned by a test, and I tried three ways.** The exception escapes Ember's error handling entirely: it does not reject the `click` promise (so `assert.rejects` never sees it) and `setupOnerror` does not intercept it either, because Leaflet throws from inside `flyToBounds`' own deferred animation frame rather than from the action. QUnit's global handler catches it and fails the test outright, so the only honest options were to assert a crash I could not capture or to leave the path uncovered and record it. I left it uncovered and wrote the entry — that is what §6's "report honestly" asks for, and the DEFECTS entry now carries the testing note so the next person does not repeat the three attempts. Once the `return` lands the path becomes ordinary and a "warns once, draws nothing" test will work.
Next: continue `components/customer/create-order-form.js` — 271 statements, 159 branches and 44 functions left. (a) **Probe ember-ui's `Toggle` markup once** (it renders neither a `<button>` nor a bare checkbox — dump `[class*=toggle]` from a rendered form) and use it to drive `toggleMultiDropOrder`, which opens the waypoint list and with it `addWaypoint`, `setWaypointPlace`, `sortWaypoints` (the `DragSortList` stand-in already reports reorders), `removeWaypoint` and `clearWaypoints`, plus `toggleProofOfDelivery`. (b) **`createOrder`/`getQuotes`/`isValid`** (L150-267) — `getQuotes` returns early unless `payloadCoordinates.length >= 2`, which the pickup+dropoff test now sets up, so it should be reachable. (c) The **checkout session** (L730-960), reachable by putting `checkout_session_id` and `service_quote` into the `urlSearchParams` stand-in the harness already drives. After this file: `components/order-config-manager/activity-flow.js` (622 missing), `controllers/operations/scheduler/index.js` (601), `components/map/leaflet-live-map.js` (563).

## 2026-09-04 — iteration 65 (Phase B: the order form's multi-drop and POD toggles, and a second live bug)
Statements 6979/18553 (37.62%) · Branches 4544/12020 (37.80%) · Functions 1979/5485 (36.08%) · Lines 6608/17607 (37.53%) · 311 files fully covered — tests 1408: 1326 pass / 82 fail (0 change either way)
Did: `components/customer/create-order-form.js` went 124/395 → **162/395 statements, 38/197 → 51/197 branches, 23/67 → 31/67 functions** on three tests. **ember-ui's `Toggle` renders a `<span role="checkbox" aria-checked>`** — neither a button nor an input, which is why the earlier probe missed it — so `[role="checkbox"]` is the selector, the first one on this form being multi-drop and the last proof-of-delivery. Covered: switching multiple-dropoffs on, which adds the first waypoint row; a waypoint taking a place through its own select, a second waypoint added, and one of the two removed (removing one of two leaves something to route between, which matters — see below); and proof of delivery taking `scan` as its default method on and giving it up off.
Found while reading, before writing a test — **DEFECTS #95 (OPEN), a second live bug**: `clearWaypoints` line 533 calls `this.previewRoute(false)`, and **`previewRoute` is not defined anywhere in the file**. The grep returns that one call site and no declaration, and the class extends `@glimmer/component`'s `Component` with no mixins. The guard above it is `if (this.isViewingRoutePreview)`, which `previewDraftOrderRoute` sets true, so: turning multiple-dropoffs **off** after entering any waypoint address throws `TypeError: this.previewRoute is not a function`, the waypoints are never cleared, and the pickup/dropoff the user was being switched back to are left half-applied. The intended call is almost certainly `removeRoutingControlPreview()`. Left for Ron.
Notes: two live defects in this one component now (**#94** the missing `return` in `previewDraftOrderRoute`, **#95** this one), and they interact with how the tests have to be written. Anything that reaches `previewDraftOrderRoute` with **nothing usable to route** hits #94's throw, so a removal test must start from *two* waypoints rather than one, and the multi-drop **off** path cannot be driven at all until #95 is fixed. Both are recorded rather than worked around with weakened assertions.
Next: continue `components/customer/create-order-form.js` — 233 statements, 146 branches and 36 functions left. (a) **`createOrder`/`getQuotes`/`isValid`** (L150-267) is now the biggest reachable block: `getQuotes` returns early unless `payloadCoordinates.length >= 2`, which the pickup-plus-dropoff test already arranges, so drive it from there and assert the quote request's shape; `isValid` gates `createOrder`, so cover its refusals first. (b) The **checkout session** (L730-960) — put `checkout_session_id` and `service_quote` into the `urlSearchParams` stand-in the harness drives, and give the store a `service-quote` to restore from. (c) `queueFile`/`removeFile` (L632-662) against the `FileDropzone` stand-in. After this file: `components/order-config-manager/activity-flow.js` (622 missing), `controllers/operations/scheduler/index.js` (601), `components/map/leaflet-live-map.js` (563).

## 2026-09-04 — iteration 66 (Phase B: the order form's creation path)
Statements 7019/18553 (37.83%) · Branches 4561/12020 (37.95%) · Functions 1982/5485 (36.13%) · Lines 6647/17607 (37.75%) · 311 files fully covered — tests 1412: 1330 pass / 82 fail (0 change either way)
Did: `components/customer/create-order-form.js` went 162/395 → **202/395 statements, 51/197 → 68/197 branches, 31/67 → 34/67 functions** on four tests covering `createOrder` end to end. Submitting with no route does nothing and says nothing — `isValid` refuses silently, and the disabled controls are the only feedback, which is worth having pinned. A routed order saves once, fires `fleet-ops.order.creating` and then `fleet-ops.order.created` on the universe, is tracked through the events service, and is handed back through `@onOrderCreated`. Custom fields that fail validation stop the submission and list *every* missing field in one message rather than the first. A save the server refuses is reported and the order kept, with the `creating` event already fired and no `created` event following it.
Notes: the harness needed three additions, all small. The custom-fields manager stand-in now answers `validateRequired()` (steerable per test through `this.customFieldsValidation`) and `saveTo()`, because `createOrder` calls the first directly and the second through `order.cfManager`. The `universe` and `events` stubs record `trigger`/`trackResourceCreated` instead of being empty. And **`order.save` is overridden per test on the record itself** rather than by stubbing an adapter — the component calls `order.save()` directly, so replacing the method is both simpler and closer to what is being asserted.
Next: continue `components/customer/create-order-form.js` — 193 statements, 129 branches and 33 functions left. (a) **`getQuotes`** (L214-267) is the biggest single piece: it returns early while `loadCustomerOrderConfig` is running or when `payloadCoordinates.length < 2`, so drive it from the routed state the `chooseRoute` helper now sets up, and assert the quote request's shape and how `serviceQuotes`/`selectedServiceQuote` are set from the response. (b) The **checkout session** (L730-960) — `isPaymentRequired`, `fetchClientSecret`, `startCheckoutSession`, `displayCompletingOrderDialog`, `checkForCheckoutSession` and `restoreFromServiceQuote`; put `checkout_session_id` and `service_quote` into the `urlSearchParams` stand-in the harness already drives and give the store a `service-quote` to restore from. (c) `queueFile`/`removeFile` (L632-662) against the `FileDropzone` stand-in, and `editEntity`/`setEntityDestionation` (L559-624). Remember **DEFECTS #94 and #95**: anything reaching `previewDraftOrderRoute` with nothing to route throws, and the multi-drop *off* path throws on an undefined `previewRoute` — neither is worked around, both are recorded. After this file: `components/order-config-manager/activity-flow.js` (622 missing), `controllers/operations/scheduler/index.js` (601), `components/map/leaflet-live-map.js` (563).

## 2026-09-04 — iteration 67 (Phase B: the order form's quoting, and a third live bug)
Statements 7049/18553 (37.99%) · Branches 4586/12020 (38.15%) · Functions 1983/5485 (36.15%) · Lines 6677/17607 (37.92%) · 311 files fully covered — tests 1416: 1334 pass / 82 fail (0 change either way)
Did: `components/customer/create-order-form.js` went 202/395 → **231/395 statements, 68/197 → 93/197 branches** on four tests over `getQuotes`. A routed order asks `service-quotes/preliminary` once there are two coordinates, sending the config's key as `service_type` and normalising the response into `service-quote` records; a response that is not a list is taken as no quotes rather than an error; a refused request is reported; and **no quotes are asked for at all while a checkout session is being completed** — `getQuotes` returns early when `urlSearchParams` carries a `checkout_session_id`, which also pulled `restoreFromServiceQuote` into coverage for free.
Found while reading, before writing a test — **DEFECTS #96 (OPEN), a third live bug in this component**: the quote request body is built as `{ payload, distance, time, service, service_type, … }` and **`service` is not a local**. There is no `let/const/var service` anywhere in the file; the only `service` in scope is line 4's `import { inject as service } from '@ember/service'`. So the shorthand sends Ember's **`inject` decorator function**, `JSON.stringify` drops it, and the field never reaches the wire. No linter catches this — `no-undef` is satisfied by the import and `no-unused-vars` sees it used. The test pins it with `assert.strictEqual(typeof body.service, 'function')` and names the entry, so whoever fixes it will see the assertion that has to change.
Notes: **three live defects in this one component now** — #94 (missing `return` after the no-route warning), #95 (`clearWaypoints` calling an undefined `previewRoute`), #96 (this). All three were found by reading or by asserting on what the code actually does, none by a test failing in an obvious way, and all three are recorded rather than fixed. One harness fact: `restoreFromServiceQuote` reads the quote with `.get(...)`, so a plain object from a stubbed `store.findRecord` throws — give it a `get(key) { return this[key]; }`.
Next: continue `components/customer/create-order-form.js` — 164 statements, 104 branches and 32 functions left. (a) The **payment and checkout block** (L730-960) is the largest: `isPaymentRequired` (four conditions to walk), `fetchClientSecret`, `startCheckoutSession`, `displayCompletingOrderDialog` and the rest of `checkForCheckoutSession` — the previous test already reaches the early part of that path, so extend it with a `service-quotes/stripe-checkout-session` response and a `purchaseRate`, and `config.stripe.publishableKey` needs to be a string for `isPaymentRequired` to pass. (b) `queueFile`/`removeFile` (L632-662) against the `FileDropzone` stand-in. (c) `editEntity`/`setEntityDestionation` (L559-624). After this file: `components/order-config-manager/activity-flow.js` (622 missing), `controllers/operations/scheduler/index.js` (601), `components/map/leaflet-live-map.js` (563).

## 2026-09-04 — iteration 68 (Phase B: the payment block — no coverage progress, blocker diagnosed)
Statements 7048/18553 (37.99%) · Branches 4585/12020 (38.14%) · Functions 1983/5485 (36.15%) · Lines 6676/17607 (37.92%) · 311 files fully covered — tests 1417: 1335 pass / 82 fail
**This iteration added a test but no coverage.** `components/customer/create-order-form.js` is unchanged at 231/395 statements, 93/197 branches, 35/67 functions; the one test that landed — returning from a checkout puts up the completing-purchase dialog, which cannot be dismissed by clicking away — asserts real behaviour but crosses only lines the previous iteration's checkout-session test already reached. Global statements moved 7049 → 7048 because the earlier `service` probe test's fixture changed shape. Saying so plainly rather than dressing it up: three of the four tests I set out to write do not work yet.
**The blocker, as far as I established it.** `createOrder` diverts to `startCheckoutSession()` only when `isPaymentRequired()` is true, and that needs all four of: a non-blank `selectedServiceQuote`; `typeof config.stripe.publishableKey === 'string'`; `customerPayment.loaded === true`; and both `paymentsEnabled` and `paymentsOnboardCompleted`. I confirmed by probe that from the *test's* side all four hold — the quote request is posted, the settings response enables payments, `config.stripe.publishableKey` reads as a string through the test's own `ember-get-config` import, and the `customerPayment` stand-in reports `loaded`. Yet the branch is not taken: `createOrder` runs on past it and fails later in `order.setPayload(...).setRoute(route)` with `Cannot read properties of null (reading 'id')`, which surfaces as a `serverError`. Two candidates remain, and I did not get to separate them: **(a)** the addon's `config` may not be the same module instance the test mutates — `ember-get-config` resolves per-app, so mutating the object the *test* imports may not change what the component sees, and that is the only one of the four conditions I could not verify from inside the component; **(b)** `selectedServiceQuote` may be cleared or never set despite the POST landing — worth asserting directly by rendering a stub that reads it back, the same trick that found the lazy-argument problem in the workbench (DEFECTS #93).
Notes: one fixture fact worth keeping — **`getQuotes` reads `serviceQuotes.firstObject`**, so a plain array response leaves `selectedServiceQuote` unset without any error; use `A([...])` from `@ember/array`. That was a real cause and is fixed; it was simply not the *only* cause.
Next: **separate the two candidates above before writing any more payment tests.** Cheapest first step is a stub that renders `selectedServiceQuote` back into the DOM so the harness can see it after the quote round-trip; if it is set, the remaining suspect is `config`, and the honest answer there may be that `isPaymentRequired` is not drivable from a rendering test — in which case record it in `DEFECTS.md` with the trace rather than leaving it looking untested. Then move on to the cheaper remaining pieces of this file: `queueFile`/`removeFile` (L632-662) against the `FileDropzone` stand-in, and `editEntity`/`setEntityDestionation` (L559-624). After this file: `components/order-config-manager/activity-flow.js` (622 missing), `controllers/operations/scheduler/index.js` (601), `components/map/leaflet-live-map.js` (563).

## 2026-09-05 — iteration 69 (Phase B: the order form's files and item photos; the payment blocker resolved as untestable)
Statements 7066/18553 (38.09%) · Branches 4592/12020 (38.20%) · Functions 1988/5485 (36.24%) · Lines 6693/17607 (38.01%) · 311 files fully covered — tests 1421: 1339 pass / 82 fail (0 change either way)
Did: `components/customer/create-order-form.js` went 231/395 → **249/395 statements, 93/197 → 99/197 branches, 35/67 → 40/67 functions** on four tests. The quotes that come back are offered as a radio group with the first already chosen; a dropped file is queued, uploaded under the order-files path and attached to the order on success; a file already sent is ignored (the dropzone calls `onFileAdded` twice, which is what the state check exists for) while a failed one is retried and, on a second failure, taken out of the queue with nothing left on the order; and editing an item opens the entity modal, whose `uploadNewPhoto` holds a new item's photo aside rather than uploading it, showing it immediately from an object URL.
**Closed the payment blocker from iteration 68 — as untestable, recorded as DEFECTS #97.** The two candidates the last ledger named are both eliminated. `selectedServiceQuote` **is** set: the new radio-group test proves the quotes render with exactly one `.is-checked`. And `config` was never the problem — `tests/dummy/config/environment.js` sets `stripe.publishableKey` to `''` at the top level of `ENV`, and `typeof '' === 'string'` is true, so that conjunct passes with no help from a test; **mutating `config` from the test does nothing, and iteration 68 spent time on it for no reason**. What is left is `paymentsEnabled`/`paymentsOnboardCompleted`, and **the template references neither**, so a rendering test cannot observe whether they landed. The block stays uncovered with the trace written down rather than covered by a test that fakes its way in; the entry names the two cheap source-side fixes (surface the flags in the template, or make `isPaymentRequired` a getter so a `setupTest` unit test can call it).
Notes: the harness grew four collaborators, each because the component genuinely uses it — a `file-queue` helper stand-in that parks `onFileAdded` where a test can invoke it (ember-file-upload is not installed), `fetch.uploadFile.perform` recording its file/meta/success/error handles, `currentUser` with `getOption`/`setOption` (`restoreFromServiceQuote` reads options, so a bare stub breaks two earlier tests), and `modalsManager` with `setOption`/`getOption`/`startLoading`/`stopLoading`. Two model facts: **`order.files` is a hasMany**, so an uploaded file must be `store.createRecord('file', …)` and not a POJO; and there is no "Edit Item" button until an item has been added, so the entity tests have to click "Add Item" first.
Next: continue `components/customer/create-order-form.js` — 146 statements, 98 branches and 27 functions left. (a) The **rest of `editEntity`**: the saved-item arm of `uploadNewPhoto` (give the entity an `id` so `isNew` is false — it uploads immediately and sets `photo_uuid`/`photo_url` from the result) and the modal's `confirm`, which saves the entity and then replays a held-back photo. (b) `setEntityDestionation` (L559-562) — the select only renders in multi-drop mode, so turn the toggle on first. (c) `_getSerializedPayload`/`_seriailizeModel`/`_serializeArray` (L701-729) and `saveCurrentOrderForCurrentServiceQuote` (L940). Leave the payment block alone unless #97 is fixed at source. Remember **DEFECTS #94, #95, #96** still bound this file: no-route previews throw, the multi-drop *off* path throws, and the quote request sends the `inject` decorator as its `service`. After this file: `components/order-config-manager/activity-flow.js` (622 missing), `controllers/operations/scheduler/index.js` (601), `components/map/leaflet-live-map.js` (563).

## 2026-09-05 — iteration 70 (Phase B: restoring an order after checkout, and a fourth live bug)
Statements 7118/18553 (38.36%) · Branches 4620/12020 (38.43%) · Functions 1996/5485 (36.39%) · Lines 6744/17607 (38.30%) · 312 files fully covered — tests 1428: 1346 pass / 82 fail (0 change either way, none new)
Did: `components/customer/create-order-form.js` went 249/395 → **301/395 statements, 99/197 → 127/197 branches, 40/67 → 48/67 functions** on seven tests over `restoreFromServiceQuote` and the item surface it unlocks. Coming back from Stripe reads the order the customer had built out of `currentUser`'s options under the quote's id and puts it all back — order config, proof-of-delivery choice, notes, custom field values, both kinds of item (one already saved, one that never was), the attached files fetched one by one, and the pickup/dropoff routed again; a quote whose payload carries waypoints instead comes back in multi-drop mode with a row per stop; and a checkout reported `complete` finishes the order, takes the session id back out of the URL, drops the saved state and closes the dialog.
**Why the restore path had to come before the item work.** `removeEntity`'s `destroyRecord` arm and `editEntity`'s immediate-upload arm both turn on `entity.isNew` being false, and `addEntity` only ever `createRecord`s — so the restore path, whose `fetch.jsonToModel` does `store.push(store.normalize(...))`, is **the only way a saved item can get into `this.entities` at all**. The ledger had these down as two separate batches; they are one.
Found while writing the harness, not by a test failing — **DEFECTS #98 (OPEN), a fourth live bug in this component**: a *new* item's photo is held aside as `pendingFileUpload` and replayed on confirm with `modal.invoke('uploadNewPhoto', pendingFileUpload)`, but ember-ui's `invoke` is `invoke(fn, modalId = null, ...params)` — the file lands in the **modal-id** slot, `getModalById` finds nothing, and `invoke` returns `null` without calling anything. The photo is shown from an object URL and then silently dropped. L618 is the only `.invoke(` call site in the addon, so there was no second example to check the convention against; it surfaced only because the stand-in was written from the real signature rather than from the call. Fix is `invoke('uploadNewPhoto', null, pendingFileUpload)`. The test asserts the current behaviour and names the entry.
Notes: three harness facts. **`waypoint.customer` is polymorphic onto `customer`, not `contact`** — a `contact` record is refused by the relationship graph with a type-mismatch assertion, so restored waypoints need `createRecord('customer', …)`. `modalsManager.done()` is called **twice** on a completed checkout and both are the component's own doing — `displayCompletingOrderDialog` clears the stack before it shows the dialog, and `checkForCheckoutSession` closes it at the end; asserting 1 was my error, not the code's. And the service-quote stand-in needs a **path-aware** `get`, because `restoreFromServiceQuote` reads `meta.preliminary_query.payload.pickup` as one path.
Next: continue `components/customer/create-order-form.js` — 94 statements, 70 branches and 19 functions left, and the two biggest remaining clusters are both testable. (a) The **place and waypoint actions** around L400-499 — `createPlace`/`editPlace` (both just open `modals/place-form`), `sortWaypoints` (the drag-sort stand-in already has `data-test-drag-end` and `data-test-drag-noop` buttons for the reorder and the no-op early return), `setWaypointPlace`, `setWaypointCustomer` and `addWaypoint`'s own arms; roughly 20 statements and 6 functions. (b) The **payload serialisers** L716-729 — `_seriailizeModel`'s `toJSON` and `serialize` arms and `_serializeArray`'s map; reach them from `getQuotes` by giving the payload a real pickup model rather than a POJO. (c) `removeFile` (L662) and the remaining `isValid` arms (L675-693). Leave alone: the payment block (**#97**, not drivable from a rendering test), `saveCurrentOrderForCurrentServiceQuote` and `_getSerializedPayload`'s caller at L943-962 (reachable only through `startCheckoutSession`, so behind #97 too), and `clearWaypoints`' preview arm (**#95**, calls an undefined method). **#94, #95, #96, #98** all still bound this file. After this file: `components/order-config-manager/activity-flow.js` (622 missing), `controllers/operations/scheduler/index.js` (601), `components/map/leaflet-live-map.js` (563).

## 2026-09-05 — iteration 71 (Phase B: the order form's map, config select and reorder; two dead members traced)
Statements 7138/18550 (38.47%) · Branches 4630/12020 (38.51%) · Functions 2004/5483 (36.54%) · Lines 6763/17604 (38.41%) · 312 files fully covered — tests 1435: 1353 pass / 82 fail (0 change either way, none new)
Did: `components/customer/create-order-form.js` went 301/395 → **321/392 statements, 127/197 → 137/197 branches, 48/67 → 56/65 functions** on seven tests. Switching the order type moves the order onto that config and loads its custom fields; the overlay's cancel button hands back to whoever opened the form; a chosen address opens `modals/place-form` for editing; dragging a stop to a new position reorders the list while dropping it back where it started is ignored; the map is nudged clear of the panel once it settles, down both the single-point fly-to arm and the fitted multi-point one; an attached file can be deleted from its own menu; and the quote request's payload is serialised model by model.
**Two members traced as unreachable and dealt with separately — #99 deleted, #100 left for Ron.** `isServicable` (**DEFECTS #99, FIXED**) is a computed getter with no reader: its one apparent caller, `order/form/service-rate.hbs:49`, reads that component's **own** `isServicable`, defined at `service-rate.js:30`. Same name, different component — the same trap as #91. Deleted. `createPlace` (**DEFECTS #100, NEEDS DECISION**) builds a customer-owned place and opens `modals/place-form` titled 'New Place', and nothing reaches it: the template never names it, the class never calls it, and there is no `{{yield}}`; the `createPlace`s the contact/customer/vendor forms call are *service* methods on `*-actions`, a different object with the same name. **I neither wired it nor deleted it** — whether the order form should let a customer add a new address is a product call, and the body is half-built anyway (it shows the modal and never saves, so a `confirm` handler is missing too). It carries an `istanbul ignore` naming the trace so the gate is not blocked meanwhile. Both removals landed on already-uncovered code: denominators fell 395→392 statements and 67→65 functions while covered held at 321/56, which is the check that they were dead rather than merely untested.
Notes: two assumptions of mine were wrong and the tests caught both. The type select only offers the configs the **settings call** says are enabled, so a second config needs adding to `customer-enabled-order-configs` before it appears — a test that just adds it to the store sees one option. And **`sortWaypoints` does not re-price**; it redraws the preview and stops, so asserting a new quote request was asserting a behaviour the code does not have. The honest assertion is the stop order, which needed the drag-sort stand-in to expose each row's place as `data-test-waypoint`. Third fact: the map's `moveend` callbacks only run when the map really settles, so the test fires `moveend` on the real Leaflet map on purpose rather than waiting for an animation — per §7, coverage that arrives incidentally from an event the browser happened to send is the coverage that disappears in CI.
Next: `components/customer/create-order-form.js` has **71 statements, 60 branches and 9 functions left, and every remaining function is blocked** — `clearWaypoints` (**#95**, calls an undefined `previewRoute`), the five in the payment block (**#97**, not drivable from a rendering test), and `saveCurrentOrderForCurrentServiceQuote` plus its `setProperties` callbacks (reachable only through `startCheckoutSession`, so behind #97 too). What is left that is *not* blocked is branch residue — defaulted parameters and `??` arms scattered through `previewDraftOrderRoute`, `updatePayloadCoordinates` and the restore path. **Treat this file as done for now** and pick it up again only if #95/#97 are fixed at source, or as part of a late branch-residue sweep. Move to the next file: `components/order-config-manager/activity-flow.js` (622 missing), then `controllers/operations/scheduler/index.js` (601), `components/map/leaflet-live-map.js` (563), `components/modals/orchestrator-import.js` (497). **#94, #95, #96, #98, #100** all still stand against this file.

## 2026-09-05 — iteration 72 (Phase B: the activity flow, from zero; JointJS made testable)
Statements 7438/18556 (40.08%) · Branches 4742/12024 (39.43%) · Functions 2062/5484 (37.60%) · Lines 7061/17610 (40.09%) · 312 files fully covered — tests 1440: 1359 pass / **81 fail (one scaffold retired, none new)**
Did: **`components/order-config-manager/activity-flow.js` went 0/369 → 283/369 statements, 0/179 → 107/179 branches, 0/79 → 56/79 functions** on six tests, and `components/joint-graph.js` came along for free at 39/56. The panel draws the default created→dispatched→started flow when the config has none, draws a saved flow instead when it has one, zooms the paper in and out and refuses past either limit, resets back to the defaults, serialises the graph back onto the config on save (children by code, no JointJS node on the wire), and redraws when the manager triggers `onConfigChanged`. Largest single-iteration gain of the campaign so far: **+300 statements**, and it took the addon past 40%.
**The blocker was that `joint` did not exist in the dummy app.** `index.js` copies `@joint/core` into the *host* app's public tree, so a real app has `joint` on the global before the engine boots — but the dummy app has no host, so `joint` was undefined and neither `joint-graph.js` nor any of `activity-flow.js` could execute. Fixed the same way Leaflet already was in this repo: one `app.import('node_modules/@joint/core/dist/joint.min.js')` in `ember-cli-build.js`, which builds only the dummy app and leaves the published addon untouched. **Nothing about JointJS is stubbed** — the tests drive a real paper and a real graph, which is the only way the positioning, linking and element-tooling code runs as it does in an app. `@joint/layout-directed-graph` is *not* needed; the component only reaches for `joint.shapes`, `joint.dia`, `joint.elementTools` and `joint.util`.
Found by that change and fixed in its own commit — **DEFECTS #101**: `joint-graph.js`'s `createResponsiveHandler` added a `window` resize listener as an inline arrow with **no `willDestroy`**, so it outlived the component; its `if (!this.el) return` guard never fired because `el` was never cleared, leaving a detached element whose `parentElement` is null, and `getFullGridSize` threw on `parentElement.offsetWidth`. It surfaced as **two unrelated `operations-monitor` tests going red** — they are simply the next tests that dispatch a window resize. Real, not test-only: mounting the activity-flow tab and then resizing after navigating away throws, once per mount. Latent until now only because nothing could construct a `JointGraph` at all. Kept out of the coverage commit per §8.
Notes: one harness trap worth keeping. **Scope DOM queries to `#ember-testing`.** QUnit's own toolbar has a "Reset" button, and it is *disabled while a test runs*, so `document.querySelectorAll('button').find(el => el.textContent.includes('Reset'))` finds QUnit's before the component's and fails with "Can not `click` disabled". The other test files in this package query bare `document` and have been lucky in their button labels; this file uses an `inTest(selector)` helper instead.
Next: continue `components/order-config-manager/activity-flow.js` — **86 statements, 72 branches and 23 functions left**, all of it the graph *editing* surface, which the six tests so far do not touch. In rough order of size: (a) `onActivityClicked` / `editActivity` / `_createContextualActivity` and the `resourceContextPanel` round-trip (L567-582, L1026-1060) — clicking a node opens it in the context panel and changes to it come back through `onContextChanged`; (b) `createNewActivity` / `addNewLinkedActivity` / `addActivityToFlowMap` / `updateActivityInFlowMap` (L684-720, L1015-1025) — the "add" element tool hangs a child off a node; (c) `removeActivityUsingElementView` / `removeActivityFromFlow` / `removeActivityFromParentById` / `repositionActivities` (L920-1014) — the "remove" tool, including the `immutableActivities` guard that refuses to delete created/dispatched/started; (d) `syncHorizontalScrollbarFromGraph` / `syncGraphFromHorizontalScrollbar` / `updateHorizontalScrollbar` / `resizePaperWidthToFitActivities` (L479-553) — drive these by giving the container a real width and firing `scroll`. The element tools are real JointJS `elementTools.Button`s rendered into the SVG, so reach them by clicking the tool markup inside `#ember-testing`, the same way the nodes are counted now. After this file: `controllers/operations/scheduler/index.js` (601 missing), `components/map/leaflet-live-map.js` (563), `components/modals/orchestrator-import.js` (497).

## 2026-09-05 — iteration 73 (Phase B: the activity flow's editing surface)
Statements 7508/18556 (40.46%) · Branches 4772/12024 (39.68%) · Functions 2084/5484 (38.00%) · Lines 7125/17610 (40.45%) · 312 files fully covered — tests 1450: 1369 pass / 81 fail (0 change either way, none new)
Did: `components/order-config-manager/activity-flow.js` went 283/369 → **352/369 statements, 107/179 → 137/179 branches, 56/79 → 77/79 functions** on ten tests over the graph's editing surface — everything the six tests from iteration 72 did not touch. Clicking a node opens it in the resource context panel as a pojo resource; the three immutable activities (created, dispatched, started) refuse to open, and so does anything on a `core_service` config or for someone without `fleet-ops view order-config` — a core-service config also renders no add/remove tools at all. The add tool opens a blank activity and saving it draws and links it; saving one whose code is already taken warns and leaves the graph and the panel alone; closing without saving announces a null context. The remove tool takes the node off the graph **and** out of its parent's `activities`, which a follow-up save proves rather than trusting the drawing. The graph and its horizontal scrollbar follow one another in both directions, and an activity named by `@context` opens as soon as the flow is drawn.
**Nothing about JointJS is stubbed here either** — the add and remove buttons are real `joint.elementTools` rendered into the SVG, and the tests click the actual tool markup. The `resourceContextPanel` stand-in keeps each `open(definition)` so a test can drive the component's own `saveTask` and `onClose` closures rather than reaching inside the component.
Notes: one wrong assumption, caught by a failing test. I tried to learn an activity's `internalId` by rendering once, clicking, and re-rendering with that id as `@context` — but `deserializeActivity` calls `generateUUID()` on every deserialise, so the id from the first render does not exist in the second. The right answer was already in the source: `internalId: activityObject.internalId ?? generateUUID()`, so a **saved flow carries its own ids** and the test just puts one in the fixture. That is also how the real round-trip works. Two lint errors were mine and both were real — a prettier wrap, and an `await settled()` immediately after `render()`, which already settles.
Next: `components/order-config-manager/activity-flow.js` has **17 statements, 42 branches and 2 functions left** and is close to done. The two functions are the `element.addEventListener('scroll', …)` **arrow wrappers** created in `setupGraphScrollContainer` / `setupHorizontalScrollbar` (L109-122) — the listeners fire (the sync test proves both directions work), so what is uncovered is the assignment-time arrow bodies; check the istanbul HTML for this file before writing anything, because these are the kind of positions where the decorator transform shifts reported lines. The branch residue is mostly `??` and default-parameter arms in `deserializeActivity`, `getActivityPositioning` and `createActivityNode`, plus the `activity.get('code') === 'created' || 'dispatched'` arms in `addActivityNodeTools` that a flow with different shapes would reach. **Judgement call for the next iteration: this file is at 95% statements and the remaining work is branch residue — the campaign gets more per iteration from starting `controllers/operations/scheduler/index.js` (601 missing) than from squeezing the last 42 branches here.** Prefer the new file; come back to this one in a late branch-residue sweep. After scheduler: `components/map/leaflet-live-map.js` (563), `components/modals/orchestrator-import.js` (497), `components/map/google-live-map.js` (417).

## 2026-09-05 — iteration 74 (Phase B: the dispatch board's read model; three findings in one controller)
Statements 7609/18556 (41.00%) · Branches 4834/12024 (40.20%) · Functions 2128/5484 (38.80%) · Lines 7210/17610 (40.94%) · 312 files fully covered — tests 1467: 1386 pass / 81 fail (0 change either way, none new)
Did: **`controllers/operations/scheduler/index.js` went 0/325 → 94/325 statements, 0/197 → 55/197 branches, 0/79 → 43/79 functions** on seventeen tests, and three calendar utils came along with it — `create-full-calendar-event-from-order.js` and `to-calendar-date.js` are now **fully covered**. The batch is the board's read model and view state: which orders are active, which are unscheduled, the sidebar search (two-character minimum, matching public id, tracking or destination) and its debounce, the type filter, driver rows and their workload bars, the resource-label and event-tile HTML, selection (one, all, clear), the view-range → view-name mapping, and stepping the calendar back and forth. Drag-and-drop, the modals and the socket subscriptions are untouched and are the next batch.
**A unit test, not a rendering test.** The controller's only contact with `@event-calendar/core` is a small imperative API (`setOption`, `getOption`, `prev`, `next`), so a `setupTest` with a stand-in for that API exercises the controller's own logic without pretending to run the calendar. One test deliberately drives the controller with **no** calendar attached, because `goToPrev`/`goToNext`/`setViewRange` all use `this.calendar?.` and that is the state before the template mounts.
**Three findings, all recorded rather than fixed — and the first two were found by fixtures failing, not by reading.** **#102 (OPEN)**: `calendarResources` reads `driver.max_daily_orders ?? 10`, and `max_daily_orders` appears **on that one line and nowhere else** in `addon/` or `fleetops-data` — `DriverModel` has no such attribute, so every driver's capacity bar is measured against 10 whatever they can actually take. **#103 (OPEN)**: the priority filter compares `o.priority`, and `OrderModel` has no `priority` — only `orchestrator_priority` — so the filter can only ever match orders against `undefined` and empties the board. **#104 (NEEDS DECISION)**: tracing #103 showed `activeFilters` has exactly three references in `addon/` — its declaration, a `@computed` key and the `forEach` that reads it — and **nothing assigns to it**, so the filter block never runs in the product at all. That makes #103 harmless today and makes the real question whether the filter UI should exist; that one is Ron's.
Notes: two harness facts. **An order pushed without `meta` throws in `OrderModel#pickupName`** — it guards `payload` with `?.` but then reads `meta.pickup_is_driver_location` bare, and `createFullCalendarEventFromOrder` reads `pickupName` for every scheduled order, so `givenOrder` defaults `meta` to `{}`. That is in `fleetops-data`, not `addon/`, so it is out of this campaign's scope and is noted here rather than in DEFECTS. And **`searchTask` debounces with a raw `setTimeout`**, which the settled-ness checks do not track — `await settled()` returns before the query lands, so the test uses `waitUntil`.
Next: continue `controllers/operations/scheduler/index.js` — **231 statements, 142 branches and 36 functions left**, in three coherent groups. (a) **Drag-and-drop** (L351-510): `onSidebarDragStart` / `onCalendarDragOver` / `onCalendarDragLeave` / `onCalendarDrop`, which resolves a drop point through `calendar.dateFromPoint(x, y)` and hands off to `scheduling.assignOrder`, plus `rescheduleEventFromDrag` and its `revert()` paths — the calendar stand-in already in the test file needs `dateFromPoint` adding. (b) **The modals** (L512-712): `viewEvent`, `_viewShiftEvent`, `addDriverShift`, `openBulkAssignModal` and `_showConflictModal`, all through `modalsManager.show` with a `confirm`/`done` pair — stub it the way `tests/integration/components/customer/create-order-form-test.js` does, keeping each shown definition so the test can drive the component's own closures. (c) **Sockets and the small helpers** (L713-909): `subscribeToRealTimeUpdates` / `unsubscribeFromRealTimeUpdates` / `_handleOrderSocketEvent` / `_handleDriverSocketEvent`, `undo`/`redo`, and `removeEvent` / `getEvent` / `setEventProperty` / `_reinterpretDateInTimezone`, which are pure enough to drive directly. After this file: `components/map/leaflet-live-map.js` (563 missing), `components/modals/orchestrator-import.js` (497), `components/map/google-live-map.js` (417), `components/orchestrator/order-pool.js` (412).

## 2026-09-05 — iteration 75 (Phase B: the dispatch board's drag-and-drop, sockets and event helpers)
Statements 7755/18556 (41.79%) · Branches 4905/12024 (40.79%) · Functions 2150/5484 (39.20%) · Lines 7342/17610 (41.69%) · 312 files fully covered — tests 1490: 1409 pass / 81 fail (0 change either way, none new)
Did: `controllers/operations/scheduler/index.js` went 94/325 → **240/325 statements, 55/197 → 126/197 branches, 43/79 → 65/79 functions** on twenty-three more unit tests. Dragging a card out of the sidebar, the drag-over highlight and its drop cursor (moved rather than duplicated on a second pass), leaving for real versus moving onto a child, and the drop itself: assigned to the driver whose row it landed on and to the wall-clock time it was dropped at, read back through the company timezone. Then the refusals — nothing dragged, no calendar yet, a point the calendar cannot place, no driver row under the pointer, a scheduler refusal, and a clash that puts up the conflict modal. Rescheduling an event already on the board covers the same four outcomes plus the shift-block arm, which saves the `ScheduleItem` directly and reverts on failure. Sockets: one company channel and one per driver, the handlers actually invoked so they push through the store, location pings dropped, and the channels closed on the way out. Plus undo/redo and the four event helpers (`removeEvent` by object, JSON or id; `getEvent`; `setEventProperty`; `_reinterpretDateInTimezone`, including the `catch` that hands the date back when Intl cannot resolve the zone).
**The timezone assertions are the ones worth keeping.** `_reinterpretDateInTimezone` is the inverse of `toCalendarDate` and it is easy to write a test that passes without proving anything. These pin real instants: 22:30 on screen in Asia/Singapore is `2026-04-06T14:30:00.000Z`, a drop at 14:30 is `2026-03-10T06:30:00.000Z`, and a shift dragged to 08:00 saves `2026-03-11T00:00:00.000Z`. If the conversion inverts, all three fail.
Notes: two of my fixtures were wrong and the tests caught both, neither was a product bug. **An event drawn on the timeline always sits on a resource row**, so `event.resourceIds` carries one — an empty array made `newDriverId` undefined and `_showConflictModal`'s `peekRecord('driver', undefined)` threw, which is a fixture that could not happen rather than a defect. And **`scheduler.shift-updated` does resolve** — the assertion had to be the real string, "Shift updated successfully.", not the key. One harness fact: the drag handlers reach for `#fleet-ops-scheduler-timeline` on the **global document**, not through a component element, so the test appends a real one to `#ember-testing` and removes it in `afterEach`.
Next: `controllers/operations/scheduler/index.js` has **85 statements, 71 branches and 14 functions left, and all fourteen functions are one coherent group** — the modal block at L512-712. `viewOrderAsEvent` / `viewEvent` (with its `reschedule`, `unschedule` and `confirm` closures), `_viewShiftEvent` (`confirm` and `delete`), `addDriverShift`, `openBulkAssignModal` and `_showConflictModal`'s `assignAnyway` / `autoAdjust`. Every one goes through `modalsManager.show(name, { …, confirm, done })`, and the `modals-manager` stand-in in this file already records each shown definition — so drive the component's own closures out of `this.modals.at(-1)[1]`, exactly the way `tests/integration/components/customer/create-order-form-test.js` does. Check what the real `ModalsManager#confirm` passes: it calls `confirm(this, done)`, so the closures take the manager and a `done` callback. That single batch would finish the file. After it: `components/map/leaflet-live-map.js` (563 missing), `components/modals/orchestrator-import.js` (497), `components/map/google-live-map.js` (417), `components/orchestrator/order-pool.js` (412), `services/position-playback.js` (412).
