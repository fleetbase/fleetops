import createFlowActivity from 'dummy/utils/create-flow-activity';
import { module, test } from 'qunit';

module('Unit | Utility | create-flow-activity', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = createFlowActivity();
        assert.ok(result);
    });
});
