import fleetOpsOptions from 'dummy/utils/fleet-ops-options';
import { module, test } from 'qunit';

module('Unit | Utility | fleet-ops-options', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = fleetOpsOptions();
        assert.ok(result);
    });
});
