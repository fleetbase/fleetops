import createCustomEntity from 'dummy/utils/create-custom-entity';
import { module, test } from 'qunit';

module('Unit | Utility | create-custom-entity', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = createCustomEntity();
        assert.ok(result);
    });
});
