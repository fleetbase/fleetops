import getVendorStatusOptions from 'dummy/utils/get-vendor-status-options';
import { module, test } from 'qunit';

module('Unit | Utility | get-vendor-status-options', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = getVendorStatusOptions();
        assert.ok(result);
    });
});
