import getVendorTypeOptions from 'dummy/utils/get-vendor-type-options';
import { module, test } from 'qunit';

module('Unit | Utility | get-vendor-type-options', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = getVendorTypeOptions();
        assert.ok(result);
    });
});
