import createFullCalendarEventFromOrder from 'dummy/utils/create-full-calendar-event-from-order';
import { module, test } from 'qunit';

module('Unit | Utility | create-full-calendar-event-from-order', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = createFullCalendarEventFromOrder();
        assert.ok(result);
    });
});
