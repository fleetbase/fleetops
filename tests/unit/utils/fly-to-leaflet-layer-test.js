import flyToLeafletLayer from 'dummy/utils/fly-to-leaflet-layer';
import { module, test } from 'qunit';

module('Unit | Utility | fly-to-leaflet-layer', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = flyToLeafletLayer();
        assert.ok(result);
    });
});
