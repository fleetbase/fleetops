import getLeafletLayerById from 'dummy/utils/get-leaflet-layer-by-id';
import { module, test } from 'qunit';

module('Unit | Utility | get-leaflet-layer-by-id', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = getLeafletLayerById();
        assert.ok(result);
    });
});
