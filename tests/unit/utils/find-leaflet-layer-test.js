import findLeafletLayer from 'dummy/utils/find-leaflet-layer';
import { module, test } from 'qunit';

module('Unit | Utility | find-leaflet-layer', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = findLeafletLayer();
        assert.ok(result);
    });
});
