import layerCanBindContextmenu from 'dummy/utils/layer-can-bind-contextmenu';
import { module, test } from 'qunit';

module('Unit | Utility | layer-can-bind-contextmenu', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = layerCanBindContextmenu();
        assert.ok(result);
    });
});
