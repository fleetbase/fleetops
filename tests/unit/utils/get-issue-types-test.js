import getIssueTypes from 'dummy/utils/get-issue-types';
import { module, test } from 'qunit';

module('Unit | Utility | get-issue-types', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = getIssueTypes();
        assert.ok(result);
    });
});
