import leaflet from 'dummy/utils/leaflet';
import { module, test } from 'qunit';

module('Unit | Utility | leaflet', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = leaflet();
        assert.ok(result);
    });
});
