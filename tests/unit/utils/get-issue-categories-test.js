import getIssueCategories from 'dummy/utils/get-issue-categories';
import { module, test } from 'qunit';

module('Unit | Utility | get-issue-categories', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = getIssueCategories();
        assert.ok(result);
    });
});
