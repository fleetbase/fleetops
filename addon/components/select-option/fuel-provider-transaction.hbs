<Resource::SelectOption
    @option={{or @option @model @fuelProviderTransaction}}
    @resourceType="fuel-provider-transaction"
    @compact={{@compact}}
    @title={{@title}}
    @details={{@details}}
    @photo={{@photo}}
    @fallbackPhoto={{@fallbackPhoto}}
    @icon={{@icon}}
    @shape={{@shape}}
    ...attributes
/>
