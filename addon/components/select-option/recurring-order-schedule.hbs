<Resource::SelectOption
    @option={{or @option @model @recurringOrderSchedule}}
    @resourceType="recurring-order-schedule"
    @compact={{@compact}}
    @title={{@title}}
    @details={{@details}}
    @photo={{@photo}}
    @fallbackPhoto={{@fallbackPhoto}}
    @icon={{@icon}}
    @shape={{@shape}}
    ...attributes
/>
