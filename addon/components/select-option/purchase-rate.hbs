<Resource::SelectOption
    @option={{or @option @model @purchaseRate}}
    @resourceType="purchase-rate"
    @compact={{@compact}}
    @title={{@title}}
    @details={{@details}}
    @photo={{@photo}}
    @fallbackPhoto={{@fallbackPhoto}}
    @icon={{@icon}}
    @shape={{@shape}}
    ...attributes
/>
