<Resource::SelectOption
    @option={{or @option @model @fleetDriver}}
    @resourceType="fleet-driver"
    @compact={{@compact}}
    @title={{@title}}
    @details={{@details}}
    @photo={{@photo}}
    @fallbackPhoto={{@fallbackPhoto}}
    @icon={{@icon}}
    @shape={{@shape}}
    ...attributes
/>
