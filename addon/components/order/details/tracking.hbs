<ContentPanel @title={{t "order.fields.tracking"}} @prefixTitleRight={{@resource.tracking}} @isLoading={{@isLoading}} @open={{true}} @wrapperClass="bordered-top">
    {{#if this.hasTrackerData}}
        <div class="tracking-intelligence mb-4">
            <div class="tracking-intelligence__status">
                <span class="tracking-intelligence-pill {{this.confidenceToneClass}}">
                    <span class="tracking-intelligence-pill__dot"></span>
                    {{this.confidenceLabel}}
                    confidence
                </span>
                <span class="tracking-intelligence-pill {{if (eq this.driverSignal 'Live') 'tracking-intelligence-pill--good' 'tracking-intelligence-pill--warn'}}">
                    <span class="tracking-intelligence-pill__dot"></span>
                    {{this.driverStatusLabel}}
                    {{#if this.hasDriverLocationAge}}
                        ·
                        {{format-duration @resource.tracker_data.driver.location_age_seconds}}
                    {{/if}}
                </span>
                {{#if @resource.tracker_data.fallback_provider}}
                    <span class="tracking-intelligence-pill tracking-intelligence-pill--muted">Fallback:
                        {{this.fallbackProviderLabel}}</span>
                {{/if}}
                <span class="tracking-intelligence__updated">Updated {{n-a (format-date-fns @resource.tracker_data.generated_at "d MMM HH:mm")}}</span>
            </div>

            {{#if this.operatorWarning}}
                <div class="tracking-intelligence-alert {{if (eq this.driverSignal 'Missing') 'tracking-intelligence-alert--critical'}}">
                    <div class="tracking-intelligence-alert__icon"><FaIcon @icon="triangle-exclamation" @size="xs" /></div>
                    <div class="min-w-0">
                        <div class="tracking-intelligence-alert__title">
                            {{#if (eq this.driverSignal "Missing")}}
                                Pending GPS fix
                            {{else if (eq this.driverSignal "Stale")}}
                                Stale driver location
                            {{else}}
                                Tracking estimate warning
                            {{/if}}
                        </div>
                        <div class="tracking-intelligence-alert__body">{{this.operatorWarning}}</div>
                    </div>
                    {{#if (or (eq this.driverSignal "Missing") (eq this.driverSignal "Stale"))}}
                        <Button
                            @type="default"
                            @size="xs"
                            @icon="bell"
                            @text="Ping driver app"
                            @onClick={{perform this.pingDriver}}
                            @isLoading={{this.pingDriver.isRunning}}
                            @permission="fleet-ops update order"
                        />
                    {{/if}}
                </div>
            {{/if}}

            <TrackingStopProgress @stops={{@resource.tracker_data.stops}} @activeStop={{@resource.tracker_data.active_stop}} />

            <div class="tracking-intelligence__eta-grid">
                <div class="tracking-intelligence-cell tracking-intelligence-cell--accent">
                    <div class="tracking-intelligence-cell__label">
                        Smart adjusted ETA
                        <span class="tracking-intelligence-tag">Live</span>
                    </div>
                    <div class="tracking-intelligence-cell__value">
                        {{#if this.isDueNow}}
                            Due now
                        {{else if this.hasActiveEta}}
                            {{format-duration this.activeEtaSeconds}}
                        {{else}}
                            -
                        {{/if}}
                    </div>
                    <div class="tracking-intelligence-cell__sub">Based on provider route and driver signal</div>
                </div>
                <div class="tracking-intelligence-cell">
                    <div class="tracking-intelligence-cell__label">Reported ETA</div>
                    <div class="tracking-intelligence-cell__value">
                        {{#if this.hasReportedEta}}
                            {{format-duration this.reportedEtaSeconds}}
                        {{else}}
                            Pending GPS fix
                        {{/if}}
                    </div>
                    <div class="tracking-intelligence-cell__sub">
                        Completion
                        {{#if this.hasCompletionEta}}
                            ·
                            {{format-date-fns @resource.tracker_data.eta.completion_at "d MMM HH:mm"}}
                        {{/if}}
                    </div>
                </div>
            </div>

            <div class="tracking-intelligence-confidence">
                <div class="tracking-intelligence-confidence__label">ETA confidence</div>
                <div class="tracking-intelligence-confidence__segments {{this.confidenceToneClass}}">
                    {{#each this.confidenceSegments as |segment|}}
                        <span class="{{if segment.lit 'is-lit'}}"></span>
                    {{/each}}
                </div>
                <div class="tracking-intelligence-confidence__value">{{this.confidenceLabel}}</div>
            </div>

            <div class="tracking-intelligence-destination">
                <div class="tracking-intelligence-destination__marker">{{this.activeStopMarkerLabel}}</div>
                <div class="min-w-0 flex-1">
                    <div class="tracking-intelligence-destination__label">{{this.activeStopLabel}}</div>
                    <div class="tracking-intelligence-destination__address">{{n-a @resource.tracker_data.active_stop.address}}</div>
                    <div class="tracking-intelligence-destination__eta">
                        ETA:
                        <strong>
                            {{#if this.hasActiveEta}}
                                {{format-duration this.activeEtaSeconds}}
                            {{else}}
                                Pending GPS fix
                            {{/if}}
                        </strong>
                    </div>
                </div>
            </div>

            <div class="tracking-intelligence-distance">
                <div class="tracking-intelligence-distance__row">
                    <span>Adjusted total</span>
                    <strong>
                        {{#if this.hasRemainingDistance}}
                            {{format-meters @resource.tracker_data.route.distance_m}}
                        {{else}}
                            -
                        {{/if}}
                    </strong>
                </div>
                <div class="tracking-intelligence-distance__row">
                    <span>Remaining stops</span>
                    <strong>{{n-a @resource.tracker_data.progress.remaining_stops}}</strong>
                </div>
                <div class="tracking-intelligence-distance__foot">Provider context:
                    {{this.providerLabel}}
                    route</div>
            </div>

            <details class="tracking-intelligence-diagnostics">
                <summary>
                    <span>Diagnostics</span>
                    <span>{{if this.hasWarnings "Warnings active" "No blocking warnings"}}</span>
                </summary>
                <div class="tracking-intelligence-diagnostics__body">
                    {{#each this.diagnostics as |item|}}
                        <div class="tracking-intelligence-diagnostics__row">
                            <span>{{item.label}}</span>
                            <strong>{{item.value}}</strong>
                        </div>
                    {{/each}}
                    {{#if this.hasWarnings}}
                        <div class="tracking-intelligence-diagnostics__warnings">
                            {{#each @resource.tracker_data.warnings as |warning|}}
                                <span>{{warning}}</span>
                            {{/each}}
                        </div>
                    {{/if}}
                </div>
            </details>
        </div>
    {{/if}}

    <div class="text-xs uppercase tracking-wide font-semibold text-gray-500 dark:text-gray-400 mb-2">Labels</div>
    <div class="flex flex-col border border-gray-200 bg-gray-50 dark:bg-gray-900 dark:border-gray-700 rounded-md px-3 py-3 shadow-sm">
        <div class="flex flex-row items-center justify-center space-x-3">
            <div class="rounded-md bg-white p-2">
                <img src={{concat "data:image/png;base64," @resource.tracking_number.qr_code}} class="w-12 h-12" alt={{@resource.public_id}} />
            </div>
            <div class="rounded-md bg-white p-2">
                <img src={{concat "data:image/png;base64," @resource.tracking_number.barcode}} class="w-36 h-12" alt={{@resource.public_id}} />
            </div>
        </div>
        <div class="mt-2 flex justify-center items-center">
            <Button @size="xs" @type="default" @icon="barcode" @text="Get Order Label" @onClick={{fn this.orderActions.viewLabel @resource}} @permission="fleet-ops view order" />
        </div>
    </div>
</ContentPanel>