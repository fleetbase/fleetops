import Component from '@glimmer/component';

/**
 * An inspection submission as a linked record: the form it was filled in from,
 * what it came to and when. Built like the order pill, the other resource with
 * no photo of its own, so it takes an icon where the others take an image.
 */
export default class InspectionSubmissionPillComponent extends Component {}
