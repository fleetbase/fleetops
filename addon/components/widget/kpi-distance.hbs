<Widget::KpiTile
    @slug="total_distance_traveled"
    @title={{t "widget.kpi-distance.title"}}
    @icon="route"
    @format="meters"
    @period="30d"
    @showDelta={{true}}
    @showSparkline={{true}}
    ...attributes
/>
