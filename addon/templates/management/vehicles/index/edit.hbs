<Layout::Resource::Panel
    @resource={{@model}}
    @controller={{this}}
    @headerTitle={{concat "Edit: " @model.display_name}}
    @actionButtons={{this.actionButtons}}
    @saveTask={{this.save}}
    @onPressCancel={{this.cancel}}
    @onOverlayReady={{fn (mut this.overlay)}}
>
    <Vehicle::Form @resource={{@model}} @controller={{this}} class="px-4" />
</Layout::Resource::Panel>

