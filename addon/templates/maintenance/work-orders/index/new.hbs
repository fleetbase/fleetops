<Layout::Resource::Panel
    @resource={{this.workOrder}}
    @controller={{this}}
    @actionButtons={{this.actionButtons}}
    @headerTitle={{t "common.create-new-resource" resource=(lowercase (t "resource.work-order"))}}
    @saveTask={{this.save}}
    @onPressCancel={{transition-to "maintenance.work-orders.index"}}
    @onOverlayReady={{fn (mut this.overlay)}}
>
    <WorkOrder::Form @resource={{this.place}} @controller={{this}} />
    <Spacer @height="200px" />
</Layout::Resource::Panel>