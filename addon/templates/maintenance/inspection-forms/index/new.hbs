<Layout::Resource::Panel
    @resource={{this.inspectionForm}}
    @controller={{this}}
    @headerTitle={{t "inspection.form.create"}}
    @saveTask={{this.save}}
    @onPressCancel={{transition-to "maintenance.inspection-forms.index"}}
    @onOverlayReady={{fn (mut this.overlay)}}
>
    <InspectionForm::Form @resource={{this.inspectionForm}} @controller={{this}} @onStructureChange={{this.setStructure}} />
    <Spacer @height="200px" />
</Layout::Resource::Panel>
