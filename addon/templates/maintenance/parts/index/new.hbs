<Layout::Resource::Panel
    @resource={{this.part}}
    @controller={{this}}
    @actionButtons={{this.actionButtons}}
    @headerTitle={{t "common.create-new-resource" resource=(lowercase (t "resource.part"))}}
    @saveTask={{this.save}}
    @onPressCancel={{transition-to "maintenance.parts.index"}}
    @onOverlayReady={{fn (mut this.overlay)}}
>
    <Part::Form @resource={{this.place}} @controller={{this}} />
    <Spacer @height="200px" />
</Layout::Resource::Panel>