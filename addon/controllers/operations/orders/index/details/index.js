import Controller, { inject as controller } from '@ember/controller';

export default class OperationsOrdersIndexDetailsIndexController extends Controller {
    @controller('operations.orders.index.details') parent;
}
