import Controller, { inject as controller } from '@ember/controller';

export default class ConnectivityTelematicsIndexIndexController extends Controller {
    @controller('connectivity.telematics.index') index;
}
